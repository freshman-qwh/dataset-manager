# TODO

更新日期：2026-09-14
已发布基线：`v0.4.1`（2026-09-08）
当前阶段：快速分拣与目录导出，候选版本 `v0.5.0`；功能尚未实现。

本文件只保留当前交付、验收门槛及有价值的后续事项。A–D、UX、F、E 已完成细目移出活动列表；完整旧清单见 [v0.4.1 TODO 归档](docs/todo-v0.4.1-archive.md)，发布历史见 [CHANGELOG](CHANGELOG.md)。旧 roadmap 仅作背景，新执行顺序以本文为准。

设计依据：[快速分拣与目录导出](docs/quick-sorting-directory-export.md)。

状态：`[ ]` 未开始，`[~]` 实施中，`[x]` 已完成。优先级：P0 数据正确性/可用性阻断，P1 核心工作流，P2 补齐与优化，P3 延后工程；属性：新增、优化、Bug 修复、工程。

## 已完成基线（不重新开发）

- [x] 本地增量扫描、样本/标签整理、搜索筛选、批量 split、图片预览。
- [x] 几何标注、连续队列、合法空样本、质量检查与 LabelMe/COCO/YOLO/VOC 导出。
- [x] Alembic 正式迁移、SQLite 元数据备份、本地任务中心与主要长任务迁移。
- [x] revision、不可变轻量快照、差异比较、历史训练标签重建、保存视图。
- [x] SQL 分页/导航及 10k/100k 基准、跨 split SHA-256 精确重复检查。

## 当前产品约束

- 完全 OK 与勉强 OK 分开；待确认代表未作结论。类型和严重程度分别可选，允许勉强 OK 带可接受瑕疵、轻微 NG、只选程度或只选类型。
- 分拣独立于几何标注进度、审核、普通 tags 和 split，适用于所有受支持图片任务。
- 分拣写元数据；导出显式复制原图到新目录或 ZIP，不搬移、不覆盖、不往源目录写 sidecar。
- 优先顺序：Q1 数据→Q2 分拣→Q3 普通目录→Q4 异常检测训练→Q5 试用收敛；Q3 后即可先行试用。
- 本轮文档更新不提升软件版本，不表示分拣功能已上线。模型 API/部署与团队协作不进入本阶段。

## Q0：设计与文档

- [x] `P1` `工程` 核查 v0.4.1 关键模型、队列、快照、任务及产物边界，形成产品/技术设计。
- [x] `P1` `优化` 清理历史 TODO，归档旧清单，保留未完成事项到后续池。
- [x] `P1` `优化` README 区分已实现能力与下一阶段，更新产品主线和文档入口。

## Q1：分拣数据与接口

目标：先在后端形成语义可靠、可查询、可复现的数据闭环。

- [ ] `P0` `工程/新增` Alembic 迁移：samples 分拣判定、OK 等级、程度、备注、版本、依据版本及判定时文件 hash；旧样本回填未分拣。
- [ ] `P1` `新增` 两级缺陷类型目录、样本多类型关系与主缺陷；同数据集/层级/循环校验，使用中的类型停用不硬删。
- [ ] `P0` `新增` 分拣校验：OK 必选等级；完全 OK 与缺陷冲突阻断；NG/待确认可缺省类型和程度；切换/重置不静默清空其他业务数据。
- [ ] `P1` `新增` 数据集判定标准与版本；区分完全/勉强 OK 与严重程度定义；内容 hash 或标准改变后提示旧判定过期。
- [ ] `P0` `工程` 单样本条件更新与版本冲突；无变化不递增、撤销不回退版本，分拣变更同事务更新 dataset revision。
- [ ] `P1` `新增` 至多 200 个明确样本的批量预览/事务写入；保留/设置/清空及类型追加/替换语义明确，冲突整批拒绝。
- [ ] `P1` `优化` 样本列表、分拣统计与 SQL 筛选增加判定、OK 等级、类型、程度、过期状态；父类型聚合按样本去重。
- [ ] `P0` `工程` 新字段纳入 manifest、快照版本 2 和分拣差异；旧快照版本 1 原 hash 校验不变，旧快照分拣显示未记录。
- [ ] `P1` `工程` 新表纳入样本/数据集清理、完整性检查、数据库备份校验；补索引与新旧库回归。

完成门槛：混合语义组合、旧库迁移、条件更新、撤销冲突、原图/标准变化及旧快照兼容测试通过；不修改原图、不改变既有标注/审核含义。

## Q2：快速分拣工作区

目标：用户可以连续分拣一批图片，出错可修正，中断可恢复。

- [ ] `P1` `新增` 独立 TriagePage 与详情页模式入口，复用图片预览/原图缩放和必要公共加载逻辑。
- [ ] `P1` `新增` 完全 OK、勉强 OK、NG、待确认四主动作；仅判定/详细分拣模式，缺陷类型多选、主缺陷、程度和备注。
- [ ] `P0` `优化` 快捷键避开输入框/中文输入/弹窗；图片未就绪禁用判断，保存成功后推进，连按去重与过期响应保护。
- [ ] `P0` `新增` 独立分拣导航：未分拣、待确认、筛选、split；按稳定游标处理当前图出队/环回，跳过不算完成。
- [ ] `P1` `新增` 会话进度与筛选恢复；当前会话单步撤销有条件更新保护，超时先查询服务端保存状态再重试。
- [ ] `P1` `优化` 类型/程度不默认继承上一张；显式会话预设持续可见，完全 OK 不套缺陷预设。
- [ ] `P1` `新增` 网格状态、筛选、批量预览；保存视图新增工作模式与分拣查询，旧几何视图可继续恢复。
- [ ] `P1` `新增` 按目录/旧标签映射分拣的只读预览与显式导入；无等级的旧 OK 必须选映射，不静默当成完全 OK。
- [ ] `P1` `优化` 分拣结果进入现有精细标注，同一样本不重新导入；缺陷到对象类别显式映射，任务切换不丢旧几何。
- [ ] `P1` `工程` 100 张连续分拣、保存失败/超时、恢复/撤销冲突、桌面/窄屏/键盘验收；检查 10k/100k 分页及邻居预取容量。

完成门槛：完全/勉强 OK、类型/程度 NG 都能高效操作；队列和统计一致；仅判定无需填全表；错误后可恢复，旧几何工作流正常。

## Q3：普通分拣目录导出（首个可试用闭环）

目标：用户不再手动创建 OK/NG/defect 文件夹，可直接获取真实图片目录。

- [ ] `P1` `新增` 导出向导：范围、按判定/类型/程度/类型+程度四模板，OK 等级默认分目录，可显式合并。
- [ ] `P0` `新增` 一图一份策略：主缺陷或显式 multi_defect；无类型 unknown、无程度 ungraded；未分拣/待确认默认排除且计数。
- [ ] `P1` `新增` 分页目录预览、文件数/容量、排除原因、过期判定提示、文件名和类型目录映射。
- [ ] `P0` `工程` 服务端一致性读快照冻结样本集合/字段/hash/配置；检测并发导入扫描窗口，不仅依赖 revision；计划大小不受 jobs JSON 限制。
- [ ] `P0` `工程` 计划 hash/有效期/确认前校验，任务与重试只读冻结计划；复制时流式核验源字节 hash。
- [ ] `P1` `新增` `dataset.directory_export` 本地任务：进度、取消、重试、失败说明；真实新目录与等价 ZIP 下载两种交付。
- [ ] `P0` `工程` 输出根目录白名单、真实路径与源/数据库隔离、文件名规范化、同名/大小写/保留名/长路径检查；禁止覆盖与合并。
- [ ] `P0` `工程` 目录专属临时工作区、同文件系统发布与目标竞争保护；仅清理本任务临时产物；发布后数据库失败的完成标记核对。
- [ ] `P1` `新增` manifest.jsonl、export-config.json、export-report.json；任务中心明确后端目录/ZIP、本地与远程路径含义及过期状态。
- [ ] `P0` `工程` 取消、无权限、磁盘不足、源图变化、重启、发布竞争和清理失败注入；原图 hash 与路径不变，输出无半成品误报。

完成门槛：同一计划的目录与 ZIP 图片/清单一致；全程可追溯，一图一份，不覆盖原图；用户独立完成“扫描→分拣→目录导出”。本批不宣称满足异常检测训练协议。

## Q4：异常检测训练模板

目标：导出产物可被用户实际使用的异常检测数据加载器直接读取。

- [ ] `P1` `新增` 默认仅完全 OK 进入 train；勉强 OK 默认排除，可显式作为 val/test 正常样本；NG/待确认/过期判定不进纯正常训练。
- [ ] `P1` `新增` 工件/采集组结构化字段与导入映射；分组固定 seed，重复 hash 同组，混合 OK/NG 组不能进入纯正常训练。
- [ ] `P0` `新增` 使用现有 split 的预检、只作用于导出计划的新划分建议；另行显式写回才修改 split 与 revision。
- [ ] `P1` `新增` 小样本覆盖失败、组不可拆、val/test 不足说明；测试集不用于选阈值，不能静默调整比例掩盖约束失败。
- [ ] `P1` `工程` 选定实际使用的 Anomalib 版本与 Folder 配置，记录依赖版本并用真实导出数据完成加载烟测；允许 NG 无 mask，不伪造像素标注。
- [ ] `P0` `工程` 分组字段纳入快照/清单/差异，补训练规则和旧随机/分层划分回归。

完成门槛：用户的既有算法读取脚本无需手工搬图即可加载；正常训练与验证/测试规则可解释、可复现，分组及重复不跨 split。

## Q5：真实用户试用与发布

- [ ] `P1` `优化` Q3 完成后邀请 2–3 位目标用户；记录首次完成时间、每百张耗时、误分修正、求助点和第二批复用意愿。
- [ ] `P1` `优化` 用相似难度、交叉安排的真实批次对照资源管理器流程，优先修复实际绕开平台的步骤。
- [ ] `P1` `工程` 补易启动/示例数据/最短使用说明；完成必要回归、更新 CHANGELOG 与人工 ACCEPTANCE_TESTS，再确定发布版本。
- [ ] `P1` `优化` 根据实测决定下一阶段：恢复能力、离线 prediction 回流、近重复或团队试点；不按功能数量决定完成。

## 后续池（旧阶段未完成事项，保留但不抢占 Q1–Q3）

| 优先级 | 事项 | 当前边界与启动条件 |
|---|---|---|
| P1 | 元数据离线恢复、备份治理 | 已有备份，缺恢复闭环；多人试用前优先评估 |
| P1 | 易安装、易启动、迁移指引 | Q5 补最短链路，正式交付按目标机器验证 |
| P1 | 离线 prediction 导入/比较/难例队列 | Q4 稳定后，先异常分数/误检漏检，再考虑在线调用 |
| P2 | 近重复与唯一性（原 E2C） | 本地/预计算 embedding 契约尚未实现；发现重复整理痛点后评估 |
| P2 | 删除记录恢复 | 与当前会话分拣撤销不同，独立设计回收站/恢复 |
| P2 | 完整元数据可视化字段映射 | 已有预览/任务/回滚；其余交互按真实导入反馈补 |
| P2 | 通用标签层级/别名贯通 | 现有基础保留；不因新缺陷类型自动声称全部贯通 |
| P2 | 高标注密度统计缓存、虚拟列表 | 已有 SQL/缩略图/小窗口预取；只在实测瓶颈出现时扩展 |
| P3 | 文件监听、通用操作日志、配置中心 | 分拣标准与导出白名单仅提供本阶段必要配置 |
| P3 | 模型服务/API、团队权限与协作 | 独立评审，不随分拣功能隐式引入 |

## 本阶段不做

移动/覆盖源图、硬/软链接导出、目录双向同步、每缺陷复制多份、任意路径表达式、无限批量编辑、跨会话多步撤销、复杂 mask 工具、GPU/大模型依赖、向量数据库、分布式队列和多用户实时协作。
