# TODO

本文件用于每轮迭代审查和范围控制。

状态说明：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 当前基线

- 当前版本：`v0.3.0`
- 基线日期：`2026-04-30`
- 当前能力：数据集创建、增量扫描、样本登记、标签体系、批量 split、搜索筛选、预览增强、重复样本识别、元数据导入、统计、manifest 和导出模板

## 当前短板

- v0.3.0 功能项已收尾，主界面新增数据集体检入口；下一阶段重点转向性能、后台任务和恢复能力
- 导出模板已经具备基础结构，但 COCO/YOLO 的真实标注转换仍需后续定义字段契约
- 元数据导入支持 CSV/JSON 批量写入，但还缺少导入前预览、字段映射 UI 和回滚机制
- 大规模样本场景下的虚拟列表、缩略图缓存、后台任务和错误恢复机制还不够
- 标签层级和别名已有数据模型与管理入口，后续需要结合筛选、统计和导出继续深化
- 目前导出只覆盖样本级标签和格式骨架；真正的 COCO/YOLO 标准导出需要先实现框、多边形、分类等标注数据模型
- 数据集设置后续建议增加：扫描规则、文件类型白名单、导出默认格式、缩略图缓存、数据库备份恢复、危险操作审计日志

## 审查方式

每次评审建议只做三件事：

1. 确认本轮版本目标是否保持聚焦
2. 调整本文件中的优先级和状态
3. 把已完成内容同步写入 `CHANGELOG.md`

## 下一步建议

下一阶段建议进入 `v0.4.0`，重点从整理功能转向性能、任务化和可恢复性。

### v0.2.0 数据可靠性与高频工作流

- [x] 增量扫描：识别新增、修改、缺失文件，而不是只做重复路径跳过
- [x] 扫描报告：返回新增数、变更数、缺失数、失败项和失败原因
- [x] 文件健康状态：为样本增加 `normal`、`missing`、`permission_denied` 等状态
- [x] 批量操作：批量打标签、批量设置 split
- [x] 预览增强：图片/视频状态增强、CSV 前几行预览
- [x] 列表能力：分页、排序、按创建时间/大小/文件名排序
- [x] 创建和扫描时支持本地目录浏览选择
- [x] 扫描时拒绝磁盘根目录和文件系统根目录
- [x] 数据集元数据编辑与二次确认删除
- [x] 数据集字段扩展：`source`、`modality`、`license`、`owner`、`project`、`notes`
- [x] 统计增强：按标签、文件类型、异常状态统计
- [x] 导出增强：支持导出筛选后的子集 manifest
- [x] 测试补强：补 Dataset API、Scan、Export 的测试

### v0.3.0 科研整理与审查流程

- [x] 标签体系：标签颜色、描述、层级、别名
- [x] 审查状态：`unlabeled`、`in_review`、`approved`、`rejected`
- [ ] 保存筛选器：保存常用查询条件和视图
- [x] 重复样本识别：按 hash 检测重复文件
- [x] 缺失文件修复：支持重新定位文件或重新挂载目录
- [x] 数据集划分管理：train/val/test 统计与批量管理
- [x] 数据集体检：集中检查缺失、重复、未标注、未划分和 test 集缺失
- [x] 元数据导入：从 CSV/JSON 批量导入样本标签和属性
- [x] 导出模板：为后续 COCO、YOLO、CSV 导出打基础
- [x] v0.3.0 修复补充：详情页选择已有标签、标签/别名冲突检测、split 筛选、导入示例与导出下载
- [x] v0.3.0 修复补充 2：卡片布局防遮挡、绝对路径导入归一化、导出预览确认
- [x] v0.3.0 修复补充 3：标签区域空白点击修复、绝对路径导入辅助匹配与诊断
- [x] v0.3.0 修复补充 4：无样本导入提示、同名样本路径诊断、CSV 标签引号清理
- [x] v0.3.0 收尾：审查状态底层能力、数据集体检、数据集级随机/分层划分、单样本/批量删除样本记录、单样本/批量缺失文件修复
- [x] v0.3.0 收尾优化：扫描入口归位、异常统计卡详情、重复状态筛选、打开数据集自动扫描设置、验收文档压缩
- [x] v0.3.0 收尾优化 2：多标签分层划分警告修复、统计卡类型筛选、当前页快速选择、扫描目录控件高度优化
- [x] v0.3.0 分层划分修复：验证集/测试集先满足可行的标签覆盖下限，再按比例填充剩余样本
- [x] v0.3.0 分层划分提示优化：极端比例下提示 split 名额不足，并在标签卡展示各标签数量统计

### v0.4.0 性能与工程完善

- [ ] 后台任务化：扫描、缩略图生成、导出任务异步执行
- [ ] 文件监听：目录变化后自动提示重新同步
- [ ] 大数据集性能：虚拟列表、缩略图懒加载、分块查询
- [ ] 本地备份：数据库备份与恢复
- [ ] 操作日志：记录扫描、批量编辑、导出等关键动作
- [ ] 配置中心：UI 中可配置 storage、database、扫描规则

## 本轮完成

`v0.3.0` 本轮优先实现的 5 项已经完成第一版：

- [x] 重复样本识别：按 hash 聚合重复文件，并在统计区显示重复组和重复样本数
- [x] 标签体系：支持标签颜色、描述、父级和别名
- [x] 数据集划分管理：延续批量 split，并补充 train/val/test/未划分统计
- [x] 元数据导入：从 CSV/JSON 按 sample id、相对路径、绝对路径、文件名或 hash 匹配样本
- [x] 导出模板：提供 CSV、COCO、YOLO 模板响应，为后续真实导出做接口基础

## 下一轮建议优先实现

如果下一轮继续开发，建议范围控制在下面 5 项：

- [ ] 后台任务化：扫描、导入、导出改为可查询进度的任务
- [ ] 大数据集性能：虚拟列表、缩略图懒加载和更细分页查询
- [ ] 标准导出契约：定义样本级 CSV、分类数据集、COCO bbox/segmentation、YOLO label txt 的字段和适用边界
- [ ] 删除恢复策略：为误删样本记录提供回收站或撤销机制
- [ ] 保存筛选器：后续如果有复杂查询和团队规范，再做可命名视图；当前 MVP 主界面不保留

## 暂不进入范围

以下内容继续保持在 MVP 外：

- [ ] 登录与多用户权限
- [ ] 云同步
- [ ] AI API 集成
- [ ] 桌面端打包
- [ ] 复杂标注工具
- [ ] 向量数据库与检索系统
