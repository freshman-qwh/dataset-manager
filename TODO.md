# TODO

本文件用于每轮迭代审查、问题归类和范围控制。

状态说明：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

优先级说明：

- `P0`：影响当前可用性或明显 UI 错乱，优先修复
- `P1`：影响核心工作流清晰度或数据审查效率，下一轮优先处理
- `P2`：能力补齐和体验优化，排在 P0/P1 之后
- `P3`：工程完善或长期能力，暂不抢占 MVP 主线

任务属性说明：

- `Bug 修复`：现有能力表现错误、遮挡、错位、状态不一致
- `优化`：现有能力可用，但交互、性能或展示需要改善
- `新增`：当前没有的能力或格式支持
- `工程`：后台任务、缓存、备份、日志等基础设施

## 当前基线

- 当前版本：`v0.4.0 标注 Phase 1`
- 基线日期：`2026-05-18`
- 当前能力：数据集创建、增量扫描、样本登记、标签体系、批量 split、搜索筛选、预览增强、重复样本识别、元数据导入、统计、manifest、导出模板和图片几何标注 MVP。
- 当前目标：P0 布局 Bug 已完成修复；下一步补齐统计卡筛选语义、重复样本对照展示、标注格式兼容和高频标注效率。

## 本轮定位

- `P0` `Bug 修复` 样本卡片信息重叠：右下角标注入口和文件大小在窄卡片/长文件名场景下容易视觉重叠。
  - 定位代码：`frontend/src/components/SampleGrid.tsx:114-122` 的标注按钮使用缩略图右下角绝对定位；`frontend/src/components/SampleGrid.tsx:139-142` 的扩展名/文件大小在卡片信息区右侧展示。
  - 建议方向：为卡片底部操作区预留稳定空间，或把标注入口移入固定 footer/action 区；文件大小文本需要 `min-w-0`、截断或独立列宽约束。

- `P0` `Bug 修复` 标注对象过多时页面滚动和图片缩放异常：右侧对象很多后，页面整体滚动条变长，画布区域随布局变化表现为图片不断放大。
  - 定位代码：`frontend/src/pages/AnnotationPage.tsx:237-279` 使用 `min-h-screen` 和 `flex-1`，但页面主体没有形成固定视口高度与 `overflow-hidden`；`frontend/src/components/annotation/AnnotationObjectList.tsx:30-35` 已给对象列表设置 `flex-1 overflow-y-auto`，但父级高度不稳定时滚动不会被限制在右栏内部。
  - 建议方向：标注页外层采用固定视口高度或 `h-screen`/`max-h-screen`，主体区域 `min-h-0 overflow-hidden`；右侧对象列表单独滚动，画布容器不随对象数量撑高。这个思路是对的。

- `P1` `优化` 统计卡动作的作用域需要明确：点击“视频”后再查看“重复”，当前会保留视频筛选，只在视频样本中查看重复；但统计卡上的重复数量来自全局，容易造成认知不一致。
  - 定位代码：`frontend/src/pages/DatasetDetailPage.tsx:499-502` 的 `filterFileType` 只设置 `fileType`；`frontend/src/pages/DatasetDetailPage.tsx:523-527` 的 `filterDuplicates` 只设置 `fileStatus=duplicate`，不会清除 `fileType`；`backend/app/services/sample_service.py:87-107` 会先应用 `file_type`，再在当前结果内筛重复。
  - 权衡建议：MVP 中将顶部统计卡定义为“全局概览入口”，点击重复/缺失等全局统计卡时清除不相关筛选，只保留该卡代表的条件；搜索筛选栏才表示“当前筛选作用域”。如果后续需要“在当前筛选内体检”，应增加明确的筛选上下文提示，而不是混用全局数字和局部结果。

- `P1` `优化` 重复样本展示需要更贴近“两两对照”的审查逻辑，但不要增加新按钮。
  - 定位代码：`backend/app/services/duplicate_service.py:19-28` 已按 `file_hash` 返回重复组；`frontend/src/components/DatasetIssueModal.tsx:79-84` 和 `frontend/src/components/DatasetQualityModal.tsx:134-141` 目前把同组路径压成一行；`frontend/src/pages/DatasetDetailPage.tsx:883-895` 已有重复弹窗入口和查看重复样本动作。
  - 建议方向：复用现有重复弹窗，把每个 hash 组渲染成可扫读的对照块；两样本组直接左右/上下成对展示，多样本组按“基准样本 + 其他样本”生成多行对照，或在列表中过滤重复后按 hash/路径排序让同组样本相邻。不新增按钮，只重排已有内容。

## P0 当前优先任务

- [x] `P0` `Bug 修复` 修复样本卡片底部信息与标注入口重叠，保证长文件名、小卡片宽度和不同文件大小文本下布局稳定。
- [x] `P0` `Bug 修复` 修复标注对象列表撑高页面的问题，让对象列表独立滚动，画布和工具栏保持稳定视口。

## P1 下一轮优先任务

- [ ] `P1` `优化` 明确统计卡点击行为的筛选作用域：全局统计卡点击应默认进入全局结果，避免保留上一次视频/图片筛选造成数字和结果不一致。
- [ ] `P1` `优化` 重做重复样本弹窗展示结构：从压缩路径列表改为按 hash 组和两两对照展示，保留现有入口，不新增按钮。
- [ ] `P1` `新增` 标注效率：上一张/下一张、批量跳转、更多快捷键提示、自动保存和标注完成状态流转。
- [ ] `P1` `新增` 格式兼容：labelme JSON 导入、COCO/YOLO/Pascal VOC 真实导出。
- [ ] `P1` `优化` 标注质量检查：空标注、越界标注、重复对象和未审核对象统计。

## P2 能力补齐

- [ ] `P2` `优化` 元数据导入体验：导入前预览、字段映射 UI、错误行诊断和回滚策略。
- [ ] `P2` `优化` 标签体系深化：把标签层级和别名继续接入筛选、统计、导出和标注对象类别选择。
- [ ] `P2` `新增` 保存筛选器：保存常用查询条件和视图；当前 MVP 主界面先不保留，待复杂查询稳定后再做。
- [ ] `P2` `优化` 标准导出契约：定义样本级 CSV、分类数据集、COCO bbox/segmentation、YOLO label txt 的字段和适用边界。
- [ ] `P2` `优化` 删除恢复策略：为误删样本记录提供回收站或撤销机制。

## P3 工程完善

- [ ] `P3` `工程` 后台任务化：扫描、缩略图生成、导入、导出改为可查询进度的异步任务。
- [ ] `P3` `工程` 大数据集性能：虚拟列表、缩略图懒加载、缩略图缓存和更细分页查询。
- [ ] `P3` `工程` 文件监听：目录变化后自动提示重新同步。
- [ ] `P3` `工程` 本地备份：数据库备份与恢复。
- [ ] `P3` `工程` 操作日志：记录扫描、批量编辑、导出等关键动作。
- [ ] `P3` `工程` 配置中心：UI 中可配置 storage、database、扫描规则、文件类型白名单和导出默认格式。

## 暂不进入范围

以下内容继续保持在 MVP 外：

- [ ] 登录与多用户权限
- [ ] 云同步
- [ ] AI API 集成
- [ ] 桌面端打包
- [ ] 复杂标注工具
- [ ] 向量数据库与检索系统
