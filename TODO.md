# TODO

本文件用于每轮迭代审查、问题归类和范围控制。

规划依据：`docs/annotation-interface-integration.md`。批次 C 格式兼容与导出契约详见 `docs/annotation-format-export-contract.md`；产品定位、体验收敛、架构和性能评审详见 `docs/product-architecture-ux-performance-review.md`；UX-D 总体验收、各角色异议与后续执行门槛详见 `docs/post-ux-d-roadmap.md`。

状态说明：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

优先级说明：

- `P0`：影响当前可用性或明显 UI 错乱，优先修复
- `P1`：影响核心工作流清晰度或数据审查效率，下一批优先处理
- `P2`：能力补齐和体验优化，排在 P0/P1 之后
- `P3`：工程完善或长期能力，暂不抢占 MVP 主线

任务属性说明：

- `Bug 修复`：现有能力表现错误、遮挡、错位、状态不一致
- `优化`：现有能力可用，但交互、性能或展示需要改善
- `新增`：当前没有的能力或格式支持
- `工程`：后台任务、缓存、备份、日志等基础设施

## 当前基线

- 当前版本：`v0.4.0 F2-2 扫描任务与增量扫描 · 进行中`
- 基线日期：`2026-07-26`
- 当前能力：数据集创建、增量扫描、样本登记、标签体系、批量 split、搜索筛选、预览增强、重复样本识别、元数据导入、统计、manifest、图片几何标注、数据质量工作台，以及 LabelMe、COCO detection/segmentation、YOLO detection/segmentation、Pascal VOC 标注导出。
- 标注基线：已具备 annotation 表、读取/全量替换保存 API、保存前几何校验、统一质量报告、统一导出预检与真实训练格式导出、manifest annotations、SVG 标注页、矩形/polygon/点、对象列表、类别与常用对象属性编辑、隐藏/锁定、删除、撤销/重做、快捷键、适配画布，以及可恢复的连续标注队列、明确的草稿/完成/无目标动作和有界相邻预取。
- 当前目标：`F2-2A/F2-2B` 增量扫描内核、`dataset.scan` runner、异步 API、协作取消和部分结果已落地；继续将详情页手动/自动扫描迁移到任务 API，并保留未迁移旧库兼容路径。

## 评审后的实施顺序

综合同类项目、当前代码路径、现有界面审查和 5,949 文件真实烟测，下一阶段按以下顺序执行：

1. `UX-A` 语义与入口收敛：拆分样本标签、标注进度和审核状态，支持合法空样本，明确数据集任务类型。
2. `UX-B` 连续标注流：形成“继续标注 → 保存并下一张 → 明确完成”的高频闭环，删除重复操作和弱入口。
3. `F1` 查询与性能基础：在 `UX-A` 语义冻结后与 `UX-B` 并行，建立 10k/100k 基准并完成数据库查询下推。
4. `UX-C` 训练准备闭环：把质量、划分和导出收敛为一个可解释的“准备训练”流程。
5. `UX-D / F1` 大数据集响应：建立 10k/100k 基准，将高频查询下推 SQLite，并优化详情页返回体验。
6. `G0 / F2-0` 发布与持久化基础：先提交 UX-D，再建立正式迁移、真实旧库副本升级和显式完整性修复。
7. `F2-1` 本地任务中心基础：jobs、runner、进度、取消、重试、重启恢复和轻量全局 UI。
8. `F2-2 / F2-3` 长任务迁移：先扫描与增量 hash，再迁移训练格式导出和有界内存产物。
9. `F2-4` 次级长任务：按缩略图、导入、备份逐项迁移，不在一次提交中并行铺开。
10. `E1` revision、轻量快照、差异比较和可复现导出。
11. `E2` 保存视图、近重复和离线 prediction 评估；仅在前述门槛通过后进入。

## 已完成基线

- [x] `P0` `Bug 修复` 修复样本卡片底部信息与标注入口重叠，保证长文件名、小卡片宽度和不同文件大小文本下布局稳定。
- [x] `P0` `Bug 修复` 修复标注对象列表撑高页面的问题，让对象列表独立滚动，画布和工具栏保持稳定视口。
- [x] `P1` `新增` 图片标注最小可用闭环：模型、API、标注页、对象列表、保存、manifest annotations 和 labelme JSON 导出接口。
- [x] `P1` `新增/优化` 批次 A 标注效率：上一张/下一张、筛选上下文保留、未保存三选一、绘制草稿互斥、对象列表聚焦和快捷键帮助入口。
- [x] `P1` `Bug 修复/优化` 批次 A 反馈修复：相邻图片导航改为后端单次计算，切换样本时不再全量分页拉取 4800 张图片；增加切换自动保存和 `[`/`]` 上下一张快捷键。
- [x] `P1` `Bug 修复/优化` 批次 A 反馈修复 2：切图加载状态不再占用标注区垂直空间，Esc 明确取消绘制中矩形/多边形草稿，左侧当前类别输入扩宽避免短标签被截断。
- [x] `P1` `优化/新增` 批次 B 主界面审查语义：统计卡全局筛选、重复样本 hash 分组对照、重复列表相邻排序、审查状态筛选入口和标注统计摘要。
- [x] `P1` `Bug 修复` 批次 B 验收修复：标注页当前类别和对象类别改为显式标签下拉，避免 datalist 按当前值过滤导致新标签不可见；图片底图改为独立 HTML 图层并增加失败重试，避免偶发只显示标注框不显示底图。

## 批次 A：标注效率与样本切换

目标：让“连续标注一批图片”成为顺手工作流。该批次优先级最高，完成后再进入格式导入导出。

- [x] `P1` `新增` 上一张/下一张图片：在标注页顶栏加入相邻图片导航，按当前数据集图片顺序跳转，跳过非图片和异常文件。
- [x] `P1` `Bug 修复` 切换样本前处理未保存状态：dirty 时提供保存、放弃、取消三种选择，避免误丢标注。
- [x] `P1` `优化` 标注页入口保留当前筛选上下文：从样本网格进入标注后，上一张/下一张应优先沿用当前图片筛选结果，而不是全数据集盲跳。
- [x] `P1` `优化` 对象列表点击定位：点击对象时画布高亮并可选择性居中/聚焦该对象，参考 CVAT 的对象状态与画布 focus 分层。
- [x] `P1` `优化` 绘制中状态互斥：polygon/rectangle 绘制中切工具、返回、切样本、保存时必须明确取消或提交，避免半成品对象污染历史栈。
- [x] `P1` `优化` 快捷键提示收敛：将 V/R/P/H、Ctrl+S、Delete、Esc 等提示放入稳定位置或轻量帮助入口，避免底栏文本过长。
- [x] `P1` `优化` 大数据集标注切换性能：上一张/下一张不再前端全量分页拉取当前筛选结果，改为后端返回 previous/current/next 和序号。
- [x] `P1` `新增` 切换自动保存：标注页提供“切换自动保存”开关，开启后上一张/下一张遇到 dirty 状态先保存成功再切换。
- [x] `P1` `新增` 上一张/下一张快捷键：支持 `[` / `]` 在标注页切换相邻图片，并纳入快捷键帮助。
- [x] `P1` `Bug 修复` 对象类别显式同步：UX-A2 已取消保存时隐式同步，仅在用户执行“类别同步为标签”时追加样本标签，不替换已有标签。
- [x] `P1` `Bug 修复` 切图加载提示抖动：移除标注区上方流式加载条，加载状态收敛到顶栏状态徽标，避免中间画布区域上下跳动。
- [x] `P1` `优化` Esc 草稿取消语义：Esc 取消绘制中的 polygon 草稿和正在拖拽的 rectangle 草稿；已完成对象继续通过撤销或删除处理。
- [x] `P1` `Bug 修复` 当前类别显示：左侧工具栏当前类别输入扩宽并提供完整 title，避免 `object` 等短标签被截断。
- [x] `P2` `优化` 标注页预取：预加载上一张/下一张图片文件和标注元数据，进一步降低大图文件读取与浏览器解码造成的切换等待。

## 批次 B：主界面审查语义

目标：把数据集详情页的统计、重复和筛选语义理顺，减少“全局统计数字”和“当前局部结果”混淆。

- [x] `P1` `优化` 明确统计卡点击行为的筛选作用域：全局统计卡点击应默认进入全局结果，清除不相关筛选，避免保留上一次视频/图片筛选造成数字和结果不一致。
- [x] `P1` `优化` 重做重复样本弹窗展示结构：从压缩路径列表改为按 hash 组和两两对照展示，保留现有入口，不新增按钮。
- [x] `P1` `优化` 重复筛选排序：进入“重复”样本列表后按 hash 分组相邻显示，便于人工对照，不改变真实 `file_status`。
- [x] `P1` `优化` 标注状态筛选入口：把 `unlabeled / in_review / approved / rejected` 作为审查筛选能力暴露在合适位置，不塞进高频批量工具栏。
- [x] `P1` `新增` 标注统计摘要：数据集统计增加 `annotated_samples`、`annotation_count`、`by_annotation_label`，用于定位未标注和类别分布。

## 批次 C：格式兼容与导出契约

目标：先把单样本标注格式和常见训练格式打通，再扩展复杂格式。

- [x] `P1` `工程/新增` C1 几何工具与导出预检框架：新增 rectangle/polygon bbox、area、YOLO 归一化等纯几何转换工具，并提供后端导出预检 API，能识别 polygon 转 bbox、rectangle 转 segmentation polygon、point/points 不兼容、越界坐标和无可导出对象。
- [x] `P1` `新增` labelme JSON 导入：支持导入单样本或目录下 labelme JSON，支持 dry-run、replace/append，写入 SQLite annotation 元数据，不写回原始图片。
- [x] `P1` `优化` labelme 导出验收：确认 rectangle、polygon、point、points 到 labelme `shapes` 的二维坐标转换和 `imageHeight/imageWidth` 完整可靠，并补充 labelme 数据集 ZIP 导出。
- [x] `P1` `新增` COCO detection/segmentation 真实导出：稳定生成 category/image/annotation id、bbox、segmentation、area 和 iscrowd，并附带导出报告。
- [x] `P1` `新增` YOLO detection/segmentation 导出：输出归一化 bbox 或 polygon 顶点、稳定类别映射、split 标签目录和 data.yaml；有损转换由预检明确 warning。
- [x] `P2` `新增` Pascal VOC bbox 导出：按样本相对路径生成 XML bbox，polygon 以外接框导出并给出预检 warning。
- [x] `P2` `优化` 导出预检与向导：前端支持格式、筛选/全量/split/已选范围和空标注选项，展示 error/warning/info、类别映射并阻断错误导出。

## 批次 D：标注质量检查

目标：让标注结果在进入训练前能被快速体检。

- [x] `P1` `新增` 质量工作台：统一展示 error/warning/info，按问题类型筛选，并支持从问题直接进入样本或标注对象、修复后重新检查。
- [x] `P1` `优化` 空标注检查：统计无 annotation 的图片样本，并支持一键筛选。
- [x] `P1` `Bug 修复` 越界标注检查：保存和体检时识别坐标超出图片宽高、矩形宽高非法、polygon 点数不足等问题。
- [x] `P1` `新增` split 泄漏检查：识别相同 file_hash 同时出现在 train/val/test 的样本，阻止误导性训练评估。
- [x] `P2` `优化` 重复对象检查：识别同一样本中高度重叠或完全相同的对象，先做提示，不自动合并。
- [x] `P2` `优化` 类别与 split 分布检查：提示极少样本类别、只出现在单一 split 的类别和明显分布偏移。
- [x] `P2` `优化` 未审核对象统计：结合 `review_status` 展示待审核、已通过、已拒绝样本数量。
- [x] `P2` `新增` 对象级属性扩展：为 occluded、truncated、difficult 等训练常用属性预留编辑和导出字段。

## 批次 UX：产品体验收敛

产品定位：面向研究人员、小型实验室和隐私敏感场景的本地优先视觉数据集准备工作台。主线服务图片目标检测与多边形分割，兼顾图片分类式标签整理；不扩张为通用多媒体管理器或小型团队协作标注平台。

### UX-A：语义与入口收敛

目标：先让用户准确理解“现在是什么状态、下一步该做什么”，避免在错误语义上继续堆功能。预计 3–5 个开发日。

- [x] `P0` `Bug 修复/优化` 拆分三套状态：样本标签是否存在、标注进度、审核状态不得继续共用“未标注”名称、字段或筛选入口；界面始终显示当前筛选对应的维度。
- [x] `P0` `新增` 支持合法空样本/负样本：用户可显式确认“本图无目标”，确认后计入已完成进度，不再被质量工作台或导出预检当作漏标。
- [x] `P0` `Bug 修复` 分离对象类别与样本标签：取消保存标注时的隐式自动同步；如需同步，提供有来源说明的显式追加操作，避免类别体系和数据整理标签互相污染。
- [x] `P1` `新增` 数据集任务类型：检测仅提供矩形、分割仅提供多边形、分类整理回到样本标签流程；质量规则、状态文案和默认导出跟随任务能力，不支持的旧类型明确阻断并提示修改。
- [x] `P1` `优化` 收敛详情页信息架构：首屏只保留数据集状态、一个上下文主操作和关键阻断问题；低频统计、导入、标签体系等进入明确的二级区域。
- [x] `P1` `优化` 统一产品文案：将“体检、质量报告、审查、未标注、空标注、导出预检”等术语整理为一份 UI 词汇表，并用于筛选、按钮、空状态和错误提示。

### UX-B：连续标注流

目标：让用户不思考页面结构即可连续完成一批图片，减少鼠标往返和状态焦虑。预计 7–10 个开发日；在 UX-A 语义冻结后启动。

- [x] `P0` `优化` 数据集详情页提供唯一且稳定的“继续标注”主入口，自动回到上次队列和样本；新数据集则进入第一个待处理样本。
- [x] `P0` `优化` 提供明确的“完成并下一张”主动作，并区分“保存草稿”“标记完成”“确认无目标”；自动保存只作为可见偏好，不替代完成语义。
- [x] `P1` `优化` 标注页类别/标签管理布局：把当前类别、类别搜索/新建和颜色提示放到对象面板固定区，减少跨画布移动，并保持对象列表独立滚动。
- [x] `P1` `优化` 清理重复保存、删除、返回和快捷键提示：同一作用域只保留一个主入口，次要动作进入一致的菜单或快捷键帮助，危险动作保持撤销或确认路径。
- [x] `P1` `新增` 轻量标注队列：至少支持全部待处理、当前筛选和当前 split 三种来源，顶栏持续显示当前位置、剩余数量和队列范围。
- [x] `P2` `优化` 预取相邻图片和标注元数据，测量“完成并下一张”可交互耗时，避免用无边界预加载换取表面流畅。

UX-B 完成门槛：

- [x] 队列“剩余”统计覆盖当前位置前后全部其他样本，切换队列不污染原筛选 split 和排序上下文。
- [x] 快速切换样本/队列具有过期请求保护，相邻图片与标注预取均有明确容量上限。
- [x] 已完成且未修改的样本不重复保存，空队列与队列末尾动作明确禁用并给出状态反馈。
- [x] 未保存/绘制中确认框具备对话框语义、Esc 关闭、焦点初始位置与 Tab 循环；动态状态可被辅助技术感知。
- [x] 队列末位仍有其他样本时回到队列起点继续处理；筛选条件持续可见，跳过不会与完成动作混淆。
- [x] 已完成数据集的“复查标注结果”继续恢复最后队列与样本，不因主入口文案变化回到首张。
- [x] 完成桌面、390px 窄屏、键盘连续操作和恢复/出队自动浏览器验收；390px 画布约 247px 且无横向溢出。
- [x] 使用 35 张隔离真实图片完成 30 张连续会话：1 张矩形、29 张确认无目标，后 23 次推进 53–192 ms、平均约 84 ms；保存失败保留对象和 dirty，接口恢复可重试，浏览器重开恢复到第 31 张。

### UX-C：训练准备闭环

目标：把“数据能否放心用于训练”转化为一个有顺序、有阻断原因、有明确产物的流程。预计 5–7 个开发日；在 UX-B 与 F1 的关键契约稳定后启动。

- [x] `P1` `优化` 新增统一“准备训练”入口，将数据健康、split 检查和训练格式导出组织为检查 → 修复 → 确认 → 导出四步，而不是三个彼此独立的功能。
- [x] `P1` `优化` 按数据集任务类型推荐默认格式和兼容工具，只突出当前任务真正可用的导出项；其他格式作为可解释的高级选项。
- [x] `P1` `新增` 训练准备摘要：展示完成样本、确认空样本、待审核、阻断问题、split 覆盖和最后导出时间，所有数字都可回到对应样本范围。
- [x] `P1` `优化` 质量问题按“阻止导出/建议修复/仅提示”表达用户影响，技术 issue code 保留为次级诊断信息，并支持从具体问题直接定位样本。
- [x] `P2` `新增` 保存最近一次训练准备配置，包括任务类型、查询范围、split、类别映射和格式；再次打开自动恢复，任务类型变化时不套用旧配置，在 revision 落地前不承诺历史数据完全复现。

### UX-D：大数据集响应

目标：把 `F1` 查询与性能基础转化为用户可感知的稳定响应，在 10k/100k 样本下保持分页级返回体、固定查询数量和可恢复的详情页上下文。

- [x] `P1` `工程` D1 隔离性能基准：新增可配置 samples、annotations/sample 和迭代次数的 SQLite 基准，记录 p50/p95、SELECT 数量、返回体和峰值内存，不读取或写入用户数据集。
- [x] `P1` `工程/优化` D2 样本列表与导航 SQL 下推：搜索、类型、状态、标签/别名、split、审核、标注进度、重复、排序、计数和分页由 SQLite 执行；常规导航按索引定位位置与相邻样本，重复虚拟队列保留分组窗口查询。
- [x] `P1` `工程` D3 统计与重复聚合：统计改用分组/聚合查询，重复报告只加载真实重复组；标签关系按当前页或相邻样本批量加载，并为已有 SQLite 增补可重复执行的复合及文件名表达式索引。
- [x] `P2` `优化` D4 前端返回体验：数据集详情页缓存最近 3 个数据集、5 分钟内的概览、样本页和筛选状态，返回时立即复用并后台刷新；概览与样本请求支持取消和过期响应保护。
- [x] `P1` `工程/优化` D5 高标注密度矩阵：覆盖 10k/100k samples × 1/10/100 annotations/sample；合并对象数与有对象样本数扫描，增加 annotation 的 dataset+sample / dataset+label 索引，10k×100 统计 p95 由约 765 ms 降至约 272–290 ms。
- [x] `P2` `工程` D6 SQLite 并发基线：统一启用 WAL、5 秒 busy timeout、foreign_keys、NORMAL synchronous 和 1000 页自动 checkpoint；隔离基准验证 1 写 4 读短事务、显式锁等待和被动 checkpoint。

UX-D 完成门槛：

- [x] 后端回归包含固定查询数量、空 split 排序、重复分组和现有筛选/队列语义。
- [x] 10k、100k × 1 annotation/sample 隔离基准完成；100k 下列表与导航返回体仍约 32 KB / 1.8 KB，峰值内存保持分页级。
- [x] 完成真实 20,298 样本 API、桌面、390px 窄屏、快速筛选和标注往返缓存浏览器验收；热服务实测样本页约 14 ms、统计约 87 ms、重复报告约 302 ms。
- [x] 10k/100k × 10/100 annotations/sample 补齐；100k×100（1,000 万标注、约 2.42 GiB 隔离库）p95 为列表约 198 ms、导航约 207 ms、统计约 2.35 s，查询数与峰值内存仍保持有界。
- [x] SQLite 并发基准完成 300 次短写事务与 2,111 次并行读取，locked 错误为 0；人为持写锁 150 ms 时，竞争写入等待约 164 ms 后成功，被动 checkpoint 完成 308/308 页。
- [x] 第二阶段真实 20,298 样本热服务 p95：样本页约 37 ms、导航约 61 ms、统计约 94 ms、重复报告约 320 ms（curl，不计 PowerShell 大 JSON 解析）；桌面与 390px 快速搜索、标注往返缓存、无横向溢出和无运行时错误通过。

### 产品验收指标

- 新建数据集后，目标用户无需阅读文档可在 3 分钟内完成首次有效标注或确认空样本。
- 从数据集详情页到继续上次标注不超过一次主操作；连续流程可通过一次“保存并下一张”推进。
- 已确认无目标的负样本不再出现在漏标问题中，样本标签为空也不等于标注未完成。
- 用户可在界面中解释当前队列范围、剩余数量、保存状态和审核状态。
- 用户可在不查阅格式文档的情况下完成扫描 → 标注 → 检查 → 导出，并理解所有阻断原因。

## 批次 E：产品能力补齐

目标：补齐数据整理 MVP 的非标注短板，但不抢占批次 A-D。

- [ ] `P2` `优化` 元数据导入体验：导入前预览、字段映射 UI、错误行诊断和回滚策略。
- [ ] `P2` `优化` 标签体系深化：把标签层级和别名继续接入筛选、统计、导出和标注对象类别选择。
- [ ] `P1` `新增` 数据集 revision 与轻量快照：记录 manifest、annotation、标签、split、class map、查询范围和文件 hash，不复制原始文件。
- [ ] `P2` `新增` 快照比较与可复现导出：展示文件/标注/split 变化，并能按历史 revision 重建训练标签。
- [ ] `P2` `新增` 保存筛选器：保存常用查询、排序、卡片密度和导出范围，作为可复用标注队列。
- [ ] `P2` `新增` 近重复与唯一性分析：先实现跨 split 精确重复，再提供可选本地 embedding 或用户预计算 embedding，不强制 GPU/向量数据库。
- [ ] `P2` `新增` 离线 prediction 导入与样本级评估：导入模型结果，按 false positive/false negative/IoU 返回具体样本，不接外部 AI API。
- [ ] `P2` `优化` 删除恢复策略：为误删样本记录提供回收站或撤销机制。

## 批次 F：工程与性能

目标：服务大数据集和长任务，不在当前标注体验稳定前提前扩张。

- [x] `P1` `工程` 性能基准：已覆盖 10k/100k samples × 1/10/100 annotations/sample，并记录 p50/p95、SQL 数量、返回体、峰值内存、SQLite 设置、WAL 大小和 checkpoint 结果。
- [x] `P1` `工程` 数据库查询下推：样本筛选、tag/duplicate 条件、排序、count、分页、导航和统计改为 SQL，增加复合/表达式索引并消除当前页与相邻样本标签 N+1。
- [x] `P1` `工程` G0 UX-D 发布边界：UX-D 已由提交 `3db6f96` 建立独立历史边界，并通过 60 项后端测试、前端生产构建和差异检查。
- [x] `P1` `工程` F2-0 正式迁移与完整性：拆分为迁移备份基础与完整性显式修复两个独立验收切片。
  - [x] `F2-0A` 引入 Alembic 当前 schema 基线、显式状态/在线备份/离线原子升级命令；迁移先在副本完成，失败不替换原库，备份与升级库均运行 `quick_check`。
  - [x] `F2-0A` 全新库、已对齐未版本化库、缺少历史列旧库和故障注入测试通过；实际旧库在线备份副本升级前后核心表记录数一致，实际源库未迁移。
  - [x] `F2-0B` 增加只读完整性报告、孤立 annotation class/tag/link 等修复预览、过期报告保护、确认文本、修复前备份与结果报告；数据库未到 migration head 时拒绝清理，不在启动时静默修复。
- [x] `P1` `工程` F2-1 本地任务中心基础：按持久化契约、runner/API、轻量 UI 三个切片逐项验收。
  - [x] `F2-1A` jobs 表与 `20260726_0002` migration、queued/running/succeeded/failed/cancelled/interrupted 状态契约、64 KiB 有界 JSON 快照、进度/取消请求/重试服务、列表与详情 API；未迁移数据库返回 409，启动不静默建表。
  - [x] `F2-1B` 单写者 runner、任务处理器注册、协作取消检查点、失败结果、创建/取消/显式重试 API 和启动时 running → interrupted 恢复；服务停止通过检查点中断，不强杀线程。
  - [x] `F2-1C` 全局运行数/失败提示与轻量抽屉，支持阶段、进度、错误、取消和重试；未迁移库显示待启用说明，不新增企业级任务首页。
- [~] `P1` `工程/优化` F2-2 扫描任务：按增量内核、runner/API、前端迁移三个切片逐项验收。
  - [x] `F2-2A` 增量扫描内核：按枚举/stat/hash/写入/缺失检测分阶段；size + mtime 未变化时跳过 SHA-256，最多 4 个只读 hash worker、默认 100 条单写者分批提交，停止时携带已安全提交的部分结果。
  - [x] `F2-2B` 注册 `dataset.scan` 处理器和异步创建 API，固化目录参数，同数据集活动任务原子去重，覆盖协作取消、部分结果、失败、重试与重启恢复；保留同步端点过渡兼容。
  - [ ] `F2-2C` 详情页扫描入口迁移到任务 API，自动扫描去重，任务完成后刷新数据集概览和样本；旧库明确降级。
- [ ] `P1` `工程` F2-3 训练导出任务：固化格式/范围/class map，使用临时文件或流式写入控制内存，覆盖取消、失败清理和结果 hash。
- [ ] `P2` `工程` F2-4 次级长任务：按缩略图、元数据/标注导入、轻量备份逐项迁移，每项独立验收。
- [x] `P2` `工程` SQLite 并发基线：统一 WAL、5 秒 busy_timeout、foreign_keys、短事务和 1000 页自动 checkpoint；隔离 1 写 4 读及显式锁等待基准记录 locked 错误、读写 p50/p95、WAL 大小与被动 checkpoint 页数。
- [ ] `P2` `优化` UX-D 性能余项：真实达到高标注密度时再评估 revision 统计缓存和重复报告分页；不阻止 F2-0/F2-1。
- [~] `P2` `工程` 大数据集前端：已具备缩略图懒加载、旧请求取消和短期页面状态缓存；hash 缓存及无限滚动出现后的虚拟列表仍未进入。
- [x] `P2` `优化` 数据集详情页前端缓存：从标注页返回详情页时复用上一次 dataset/stats/samples/filter 状态并后台刷新，避免数据集名称、统计卡和样本网格重新空白加载。
- [ ] `P3` `工程` 文件监听：目录变化后自动提示重新同步。
- [ ] `P2` `工程` 本地备份：提供轻量 SQLite 元数据备份、校验与恢复，不复制原始文件。
- [ ] `P3` `工程` 操作日志：记录扫描、批量编辑、导出、标注保存等关键动作。
- [ ] `P3` `工程` 配置中心：UI 中可配置 storage、database、扫描规则、文件类型白名单和导出默认格式。

## 暂不进入范围

以下内容继续保持在 MVP 外：

- [ ] 登录与多用户权限
- [ ] 云同步
- [ ] AI API 集成
- [ ] 桌面端打包
- [ ] brush / mask / skeleton 等复杂标注工具
- [ ] 视频帧、track 和任务队列式视频标注
- [ ] 向量数据库与检索系统
