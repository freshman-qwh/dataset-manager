# CHANGELOG

本文件记录每个版本的日期、版本号和主要更新。

## [v0.4.0 F2-3 训练导出任务与有界内存 · 第二阶段] - 2026-07-26

主要更新：

- 新增 `annotation.export` 本地任务与 `POST /api/datasets/{id}/annotation-export-jobs`，当前先支持 LabelMe；任务参数冻结查询范围、split、空样本策略、类别映射摘要和请求指纹。
- 相同数据集、相同参数的活动导出原子去重；不同参数可独立排队，重试沿用原参数快照。
- LabelMe ZIP 直接写入应用临时文件，不再为异步路径构造完整内存归档；每 25 个样本响应协作取消并更新进度。
- 成功结果记录样本/对象数、文件名、媒体类型、字节数与 SHA-256；`GET /api/jobs/{id}/artifact` 使用文件响应下载，不暴露绝对存储路径。
- 写入失败和协作取消会清理 `.part`；未迁移旧库返回 409，原同步导出端点和产物内容契约保持不变。

## [v0.4.0 F2-3 训练导出任务与有界内存 · 第一阶段] - 2026-07-26

主要更新：

- 新增 job 专属产物工作区，先在应用存储内写唯一 `.part` 文件，完整生成后再原子发布，不在原始数据目录生成 sidecar。
- 发布时以 1 MiB 分块读取计算文件大小和 SHA-256；后续任务结果只需保存有界元数据，不把归档内容放入 jobs JSON。
- 生成异常、取消、未显式完成或发布后结果持久化失败时清理本次临时/最终产物；文件名与下载路径拒绝目录穿越。
- 本阶段只交付产物生命周期基础，尚未改变同步训练格式导出 API；LabelMe 任务接入在下一切片完成。

## [v0.4.0 F2-2 扫描任务与增量扫描 · 完成] - 2026-07-26

主要更新：

- 数据集详情页的手动扫描与自动扫描迁移到 `dataset.scan` 任务 API；提交后页面可继续浏览旧数据，活动任务会恢复跟踪并在终态刷新概览与样本。
- 数据集操作菜单新增“更新扫描”，运行期间入口禁用；任务中心把扫描阶段翻译为中文，并展示新增、变更、未变、hash 与错误摘要。
- 扫描结果区补充实际 hash、跳过 hash、批次数和完整错误数，任务完成、取消、中断与失败均提供明确反馈。
- 未迁移数据库保持兼容：任务 API 返回 409 时自动使用原同步扫描端点，任务中心显示待启用，且同步回退完成后给出明确终态。
- 迁移副本和未迁移真实库备份副本均完成浏览器操作验收；异步和兼容路径控制台均无运行时错误。

## [v0.4.0 F2-2 扫描任务与增量扫描 · 第二阶段] - 2026-07-26

主要更新：

- 注册 `dataset.scan` 本地任务处理器和 `POST /api/datasets/{id}/scan-jobs`，创建时冻结已解析目录，不让界面后续修改污染运行范围。
- 同一数据集的 queued/running 扫描使用短 `BEGIN IMMEDIATE` 原子去重；重复请求返回现有任务，不创建并发扫描。
- 扫描阶段和进度写入任务中心；协作取消或服务停止会保留已提交批次的 imported/updated/unchanged/hash/error 摘要，显式重试可幂等补齐。
- 单个错误摘要截断为 1000 字符、列表最多 50 条，同时单独记录完整错误总数，保证 jobs JSON 保持在 64 KiB 边界内。
- 保留原同步扫描端点作为过渡兼容；未迁移真实库对异步端点返回 409，不静默建表或修改 schema。

## [v0.4.0 F2-2 扫描任务与增量扫描 · 第一阶段] - 2026-07-26

主要更新：

- 重构扫描内核为枚举、stat/类型识别、hash、分批写入和缺失检测阶段；原始文件仅被读取，不移动、重命名、覆盖或生成 sidecar。
- 已登记文件在 size 与 mtime 均未变化时跳过 SHA-256；新增或可能变化的文件使用最多 4 个受限只读 worker 并行计算 hash。
- SQLite 写入仍由调用线程单写，默认每 100 个候选提交一个短批次；大目录不再把全部 Sample ORM 对象一次载入内存。
- 扫描结果新增实际 hash 数、跳过 hash 数和已提交批次数；协作停止会携带已安全提交的部分结果，再次扫描可幂等收敛。
- 缺失与权限错误改用显式 stat 分类，避免把不可读文件静默误报为不存在。

## [v0.4.0 F2-1 本地任务中心基础 · 完成] - 2026-07-26

主要更新：

- 新增全局轻量任务入口和右侧抽屉，显示等待/运行数量、失败提示、阶段、进度、错误摘要与创建时间。
- queued/running 任务可提交协作取消，failed/cancelled/interrupted 任务可显式重试；抽屉每 3 秒轻量刷新并支持手动刷新。
- 未迁移数据库显示“任务中心待启用”和离线升级说明，不把 409 当成页面故障，也不持续轮询已知不可用的 schema。
- 真实旧库与迁移副本均完成浏览器验收：旧库保持只读安全降级，副本可查看失败任务并从抽屉创建重试；控制台无运行时错误。

## [v0.4.0 F2-1 本地任务中心基础 · 第二阶段] - 2026-07-26

主要更新：

- 新增本地单工作线程 runner；每次认领、进度和终态写入均使用独立短 Session，并通过 `BEGIN IMMEDIATE` 串行认领 queued 任务。
- 增加处理器注册与隔离失败契约：未注册类型和处理器异常会写入有界错误摘要并成为 failed，不会终止后续任务。
- 增加协作取消检查点与服务停止中断；运行中取消不强杀线程，处理器在检查点落为 cancelled，主动停止落为 interrupted。
- 后端启动时把上一进程遗留的 running 任务标记为 interrupted，不伪装成功或盲目续跑；用户可显式重试为新的 queued 记录。
- 新增任务创建、取消与重试 API；当前仅建立任务基础设施，尚未把扫描或导出业务接入 runner。

## [v0.4.0 F2-1 本地任务中心基础 · 第一阶段] - 2026-07-26

主要更新：

- 新增 `jobs` 持久化模型与 Alembic `20260726_0002`，记录类型、标题、状态、数据集、阶段、进度、错误数、尝试次数、取消请求、参数/结果/错误摘要和重试来源。
- 将任务参数、结果与错误摘要限制为最多 64 KiB UTF-8 JSON，避免任务记录成为无界大对象存储。
- 固化 queued、running、succeeded、failed、cancelled、interrupted 状态转换；进度只能由 running 更新，终态任务不可回到运行态，failed/cancelled/interrupted 可创建新的 queued 重试记录。
- 新增 jobs 列表与详情只读 API，支持状态、类型、数据集和数量过滤；尚未运行正式迁移的数据库返回 409，而不是查询缺表或自动创建。
- 将启动期 `create_all` 兼容范围冻结在 F2-0 基线表；所有新增持久化模型必须由 Alembic 创建，避免应用重启静默修改实际数据库。
- 全新库和未版本化旧库副本可依次升级到 `0002`；实际数据库仍无 jobs 表，等待数据所有者按 F2-0 流程离线升级。

## [v0.4.0 F2-0 正式迁移与完整性 · 完成] - 2026-07-26

主要更新：

- 新增全局“数据库维护”入口与只读完整性报告，展示 SQLite quick check、迁移版本、缺失表/列、外键违规和可修复孤立元数据；默认不选择、不清理。
- 覆盖孤立 sample、annotation、annotation class、tag、sample-tag link、训练准备记录，以及失效 class/tag/父级引用；每类问题提供影响数量、记录 ID 摘要和面向用户的修复说明。
- 清理必须先生成最新预览并输入精确确认文本；报告 token 过期、数据库在备份期间变化、schema 未到 Alembic head 或确认不匹配时均拒绝执行。
- 确认清理会先创建并校验 SQLite 元数据备份，再在单一事务中按依赖顺序删除或解除引用，完成后重新运行完整性报告并返回恢复路径与结果统计。
- 实际数据库只读 API 已识别 1 条历史孤立 annotation class 和对应外键违规；由于实际库尚未登记 migration head，界面先提示离线升级，未执行实际清理。

## [v0.4.0 F2-0 正式迁移与完整性 · 第一阶段] - 2026-07-26

主要更新：

- 引入 Alembic 正式迁移目录和 `20260726_0001` 当前 schema 基线，支持全新 SQLite 数据库、已对齐但未版本化的数据库，以及补齐已知历史可选列。
- 增加显式数据库维护命令：查看版本、创建经 `quick_check` 校验的在线备份，以及在服务停止后执行离线升级；不会在应用启动时自动迁移唯一实际数据库。
- 升级先通过 SQLite backup API 创建元数据备份，再在同目录临时副本上运行迁移与校验；全部成功后才原子替换目标库，迁移失败时保留原库和恢复备份。
- 增加 Windows/WAL 安全边界：初始侧车文件或数据库占用会阻止升级，迁移临时库显式 checkpoint，并关闭全部 SQLite/Alembic 句柄后再替换。
- 实际旧库在线备份副本已从未版本化状态升级到基线；数据集、样本、标签、标签关联、类别、标注和训练准备记录升级前后计数一致，源数据库与原始文件均未修改。

## [v0.4.0 UX-D 大数据集响应 · 完成] - 2026-07-26

主要更新：

- 隔离性能基准新增 10k/100k samples × 1/10/100 annotations/sample 密度矩阵、安全行数上限、SQLite 运行快照和 checkpoint 结果；完整 100k×100 档使用 1,000 万标注行、约 2.42 GiB 临时库，结束后自动清理。
- 标注统计将对象总数与有对象样本数合并为一次扫描，并增加 dataset+sample、dataset+label 复合索引；10k×100 统计 p95 从约 765 ms 降至约 272–290 ms，100k×100 p95 约 2.35 s，列表与导航仍约 198/207 ms。
- SQLite 统一启用 WAL、5 秒 busy timeout、外键约束、NORMAL synchronous 和 1000 页自动 checkpoint；新增隔离并发基准与外键删除回归，避免新数据继续产生孤立元数据。
- 1 写 4 读基准完成 300 次短写事务和 2,111 次读取，locked 错误为 0；人为持锁 150 ms 时竞争写入等待约 164 ms 后成功，被动 checkpoint 完成 308/308 页。
- 大重复报告直接输出已验证模型 JSON，避免路由层重复验证大型嵌套响应；20,298 样本热服务 curl p95 为样本页约 37 ms、导航约 61 ms、统计约 94 ms、重复报告约 320 ms。
- 桌面与 390px 浏览器复验快速连续搜索、25 项最终结果、标注工作区往返和详情页即时缓存恢复；页面宽度与滚动宽度一致，无运行时错误，测试未修改原始文件。

## [v0.4.0 UX-D 大数据集响应 · 第一阶段] - 2026-07-26

主要更新：

- 样本搜索、类型/状态、标签与别名、split、审核、标注进度、重复、排序、计数和分页全部下推 SQLite；列表只加载当前页，标签关系按页批量读取。
- 常规标注队列使用索引计数和相邻定位，不再加载完整 ID 队列；重复虚拟筛选继续按 hash 分组，并保持上一张/下一张、当前位置和剩余数契约。
- 数据集统计改为 SQL 聚合，重复报告只实例化真实重复样本；为现有 SQLite 自动补充数据集筛选、split/审核、hash、创建时间、文件名小写排序和标签关联索引。
- 新增隔离查询基准和固定查询数量回归。10k × 1 annotation/sample p95：列表约 65 ms、导航约 46 ms；100k 优化后 p95：列表约 297 ms、导航约 398 ms，返回体约 32 KB / 1.8 KB，峰值内存保持分页级。
- 详情页缓存最近 3 个数据集、5 分钟内的概览、样本页和筛选状态；从标注页返回时立即恢复可见内容并后台刷新，概览与样本请求支持取消及过期响应保护。
- 真实 20,298 样本热服务 API 验收约为样本页 14 ms、导航 69 ms、统计 87 ms、重复报告 302 ms；重复统计/报告/筛选均为 3,318，末页回退与单样本搜索导航一致。
- 桌面与 390×844 浏览器验收确认快速筛选、标注往返恢复、分页和样本卡片无横向溢出且无控制台错误；所有基准使用隔离 SQLite，不修改原始数据文件。

## [v0.4.0 UX-C 训练准备闭环 · 完成] - 2026-07-25

主要更新：

- 训练准备配置写入 SQLite 元数据，保存任务类型、格式、范围、split、筛选查询、已选样本、空样本选项和类别映射；再次打开向导会恢复最近一次有效配置。
- 摘要“最近导出”改为可信的真实下载记录；统一训练向导、兼容标注格式入口和分类 CSV 预览下载均在产物生成后记录时间，单纯预检不会冒充导出。
- 新增训练配置保存与导出记录 API，按任务类型、split 范围和已选样本执行契约校验；重新保存配置会保留最近导出时间。
- 配置恢复与保存使用轻量状态提示，记录失败不会阻止已生成文件下载，并明确说明下载内容与原始数据不受影响。
- 数据集删除会同步清理训练准备状态；全部记录仅存在 SQLite，不向原始数据目录写 sidecar，也不承诺替代后续 revision 快照。

## [v0.4.0 UX-C 训练准备闭环 · 第二阶段] - 2026-07-25

主要更新：

- “准备训练”升级为可交互的检查 → 修复 → 确认 → 导出四步流程，质量问题、split 检查、格式和范围选择不再分散到多个弹窗。
- 训练准备接口新增可操作问题明细和截断计数；界面按“阻断导出 / 建议修复 / 仅作提醒”解释影响，保留 issue code 并支持定位具体样本。
- 检测与分割任务默认只展示兼容格式，高级格式折叠展示；范围支持当前筛选、全部图片、指定 split 和已选样本，空选择会阻止预检。
- 复用真实训练格式导出预检：展示样本、对象、跳过数、类别映射和中文问题说明，error 阻止下载，成功状态才开放确认下载。
- 分类任务在同一流程中确认 CSV 产物，并在质量阻断存在时禁用生成；前端兼容短暂的前后端契约版本差异，不再因缺少新增字段白屏。
- API、桌面和 390×844 浏览器验收覆盖成功/阻断预检、问题定位、窄屏无横向溢出和分类阻断路径。

## [v0.4.0 UX-C 训练准备闭环 · 第一阶段] - 2026-07-25

主要更新：

- 数据集详情页将独立的质量与划分入口收敛为统一“准备训练”主入口，以检查 → 修复 → 确认 → 导出四步呈现后续流程。
- 新增只读 `GET /api/datasets/{id}/training-readiness`：按任务范围汇总完成样本、确认空样本、待审核、拒绝、问题影响、split 覆盖和推荐格式。
- 目标检测、分割和分类分别返回推荐、兼容及高级格式范围；当前界面只突出推荐和兼容格式，避免将无关格式平铺为同等选项。
- 摘要中的有对象完成、确认空样本、待审核和 split 数字可直接回到对应样本筛选；分类新增“有标签”虚拟筛选，与现有“无标签”范围配对。
- 训练准备入口可继续打开现有质量工作台、数据集划分和训练格式导出，并在接口中断时保留弹窗、提供原地重试。
- 桌面和 390×844 浏览器验收确认步骤条、摘要卡、split、格式推荐及底部操作无横向溢出。

## [v0.4.0 UX-B 连续标注流 · 长会话与恢复完成] - 2026-07-25

主要更新：

- 标注页底栏新增“本次完成”计数，与最近一次推进耗时一起提供长会话反馈；计数只在首次成功完成样本后增加。
- 保存失败时明确说明当前修改仍被保留，页面停留在原样本并允许直接重试；标注写入成功但后续状态刷新失败时不再误报为写入失败。
- 工作区加载失败新增“重新加载工作区”操作，接口恢复后可在原地址、原队列和原样本重新加载；390px 下失败栏无横向溢出。
- 完成 35 张隔离真实图片长会话验收：1 张矩形、29 张确认无目标，连续推进到第 31 张；后 23 次推进平均约 84 ms。
- 验证 API 中断时对象、dirty 状态和样本地址不丢，服务恢复后同一主动作成功；关闭并重开浏览器标签后，“继续标注”恢复到第 31 张。
- 长会话结果经样本列表、统计和标注 API 交叉核对为 1 张已完成有对象、29 张已完成无目标、5 张待处理，测试仍不修改原始数据文件。

## [v0.4.0 UX-B 连续标注流 · 第二轮打磨] - 2026-07-25

主要更新：

- 队列控件直接展示正常/重复、搜索、标签、划分、标注进度、审核状态和排序摘要，当前处理范围无需返回详情页猜测。
- 右侧导航明确为“跳过当前，查看队列下一张”，跳过后说明完成状态未改变；“完成并下一张”继续作为唯一主推进动作。
- 修正恢复在队列末位时误报“队列结束”的问题：只要剩余数量大于 0，就会回到队列起点继续处理，真正清空后才显示“本队列已完成”。
- 已完成数据集的“复查标注结果”复用最近队列和样本位置；待处理队列完成后会安全转换为可复查范围。
- 390px 窄屏收紧路径与自动保存等次要信息，保留队列、剩余、保存和进度语义，实测画布从约 155px 恢复到约 247px 且无横向溢出。
- 完成 20,298 项大队列接口与浏览器烟测，并用 `D:\My Datasets\test` 扫描 5,949 文件确认 0 错误、抽样原图哈希不变。

## [v0.4.0 UX-B 连续标注流 · 验收打磨] - 2026-07-24

主要更新：

- 数据集详情页使用唯一“开始标注 / 继续标注”主入口，并在本地恢复上次队列、筛选上下文与样本位置。
- 新增“全部待处理、当前筛选、当前划分”三种轻量标注队列，顶栏持续显示队列范围、当前位置和剩余数量。
- 对象面板提供“完成并下一张 / 确认无目标并下一张”主动作，同时保留“保存草稿”和“仅完成当前”，完成语义不再被自动保存替代。
- 当前对象类别、搜索/新建、颜色提示和显式追加样本标签集中到对象面板固定区；对象列表继续独立滚动，重复保存与删除入口被收敛。
- 有界预取前后各一张图片和标注元数据，并显示最近一次连续推进耗时；桌面与窄屏均保持无横向溢出。
- 修正恢复到队列中段时的剩余数量，并把当前划分使用的 split 与原筛选上下文分离，来回切换队列不再悄悄污染筛选范围或排序。
- 增加快速切换的过期请求保护、已完成样本免重复保存、空队列/队尾禁用状态，以及确认对话框焦点循环和动态状态播报。
- 常用队列改为只查询必要 ID 和排序字段；在本地 20,298 样本数据集上，五次实测“全部待处理”平均约 127 ms、“当前筛选”平均约 70 ms。
- 导航 API 新增队列范围和剩余数量契约；本批次只写标注与样本状态元数据，不修改原始数据文件。

## [v0.4.0 UX-A 完成入口与文案收敛] - 2026-07-24

主要更新：

- 数据集详情页首屏收敛为任务状态、完成/审核/文件摘要、一个上下文建议操作和关键阻断项，不再并列展示多组低频操作。
- 建议操作会根据任务迁移、扫描目录、样本状态、不可用文件、分类待整理和几何标注进度自动切换。
- 统计与分布进入“数据概览”，质量、划分、标签、导入和导出进入“管理与导出”，样本处理统一进入“样本工作区”。
- 新增 UI 文案词汇表及前端文案常量，统一使用“数据质量、审核状态、标注进度、样本标签、对象类别、无目标图片、导出检查”等名称。
- 桌面和窄屏详情页保持稳定布局与清晰点击区域；本批次仍只写 SQLite 元数据，不修改原始数据文件。

## [v0.4.0 UX-A3 任务能力接入] - 2026-07-24

主要更新：

- 数据集详情页新增任务能力提示：目标检测、分割和分类整理分别说明当前工具、操作入口与默认导出，不支持的旧任务类型明确引导到设置迁移。
- 标注工作区按任务类型限制绘制工具：检测仅显示矩形，分割仅显示多边形；分类整理不再进入几何工作区，而是返回样本标签流程。
- 分类整理隐藏无关的几何标注进度和训练格式入口，统计、质量工作台与空状态改用“已分类 / 无标签”语义，默认导出 CSV 标签表。
- 数据健康检查新增任务形状不匹配、分类样本无标签和旧任务类型未迁移检查；检测/分割默认打开对应 COCO 训练格式。
- 补充任务感知质量规则回归测试，并保持所有扫描、检查和导出操作只读原始数据文件。

## [v0.4.0 UX-A2 完成语义与类别边界] - 2026-07-19

主要更新：

- 标注页新增“完成标注”和“确认无目标”动作：有对象样本进入“已完成 · 有对象”，合法负样本进入“已完成 · 无目标”，两者均与保存草稿明确区分。
- 质量报告单独统计有对象样本和确认无目标样本；确认无目标不再产生漏标问题，导出预检以 `CONFIRMED_EMPTY_SAMPLE_SKIPPED` 说明其被当前空样本选项排除。
- 新增独立 `annotation_classes` 类别表和 `class_id` 关联，旧标注类别在启动时回填；对象类别不再借用样本标签 ID。
- 保存和 LabelMe 导入默认不再修改样本标签；标注页提供“类别同步为标签”显式操作，只追加当前对象类别，不删除或替换已有标签。
- 类别重命名会同步更新关联对象显示名；删除类别仅解除 SQLite 关联并保留对象标签文本，不修改原始数据文件。
- 补充完成模式冲突保护、合法空样本质量/预检、类别迁移以及类别/标签显式同步回归测试。

## [v0.4.0 UX-A1 语义契约] - 2026-07-19

主要更新：

- 样本新增独立 `annotation_progress`，明确区分未开始、处理中、已完成无目标和已完成有对象；样本标签、标注进度与审核状态不再共享“未标注”语义。
- 审核初始值从 `unlabeled` 迁移为 `not_reviewed`，前端统一显示“未审核”；样本标签为空统一显示“无标签”。
- 数据集任务类型限制为目标检测、多边形分割和分类整理，API 返回可用工具、默认导出与支持状态能力契约；旧任务类型会提示用户在设置中迁移。
- 旧 SQLite 数据启动时轻量回填：已有对象的样本标为“处理中”，未审核旧值迁移为 `not_reviewed`，不修改任何原始文件。
- 样本列表、相邻导航、统计与前端筛选统一使用标注进度契约，删除原 `annotation_status=empty|annotated` 的混合语义。

## [v0.4.0 Phase 2 批次 D 标注质量工作台] - 2026-07-19

主要更新：

- 新增 `GET /api/datasets/{dataset_id}/quality-report`，统一返回错误、警告和提示，覆盖空标注、无效/越界几何、重复对象、跨 split 相同 hash 泄漏、极少类别、单 split 类别、明显分布偏移与审查状态。
- 训练格式预检会把当前导出范围内的跨 split 相同 hash 泄漏作为 error，阻止生成可能误导评估的完整训练包；只导出单一 split 时不误报范围外样本。
- 标注保存新增图片边界和 polygon 正面积校验；失败替换会在删除旧对象前返回 400，保留既有 SQLite 标注元数据。
- 样本列表与标注导航新增 `annotation_status=empty|annotated`，质量工作台可一键筛选空标注，并可从问题直接进入样本或定位标注对象。
- 数据集详情页原“体检”升级为宽屏数据健康工作台，支持严重度/问题类型筛选、重新检查、审查状态入口和问题明细内部滚动。
- 标注对象面板新增遮挡、截断、困难属性；LabelMe 导入导出、COCO 和 Pascal VOC 导出保留这些字段。
- 新增隔离 API 契约测试和真实目录只读烟测；5,949 文件烟测的质量报告耗时约 0.149 秒，抽样原图 hash、大小和修改时间保持不变。
- 完成产品定位复审：明确本项目是本地优先的视觉数据集准备工作台，并新增 UX-A 语义收敛、UX-B 连续标注、UX-C 训练准备闭环三阶段路线及产品验收指标。

## [v0.4.0 Phase 2 批次 C 训练格式导出] - 2026-07-19

主要更新：

- 统一 `GET /api/datasets/{dataset_id}/annotation-export`，新增 COCO detection/segmentation、YOLO detection/segmentation 和 Pascal VOC 真实导出，并保留 LabelMe ZIP。
- 所有训练格式导出强制复用预检；错误返回结构化 422，polygon/bbox 与 rectangle/polygon 有损转换保留 warning，point/points 按格式跳过。
- COCO 输出稳定类别、图片与标注 ID；YOLO 输出归一化标签、split 目录、`classes.txt`、`data.yaml`；VOC 输出按相对路径组织的 XML bbox。
- 新增前端“标注训练格式”导出向导，支持当前筛选、全数据集、指定 split、已选样本与空标注范围，并展示问题明细和类别映射。
- 新增隔离 API 契约测试与真实数据只读烟测脚本；`D:\My Datasets\test` 扫描 5,949 个文件后六种格式均导出成功，测试原图哈希、大小和修改时间保持不变。

## [v0.4.0 Phase 2 批次 C2 labelme 导入导出] - 2026-05-19

主要更新：

- 新增 `POST /api/datasets/{dataset_id}/annotations/import-labelme`，支持单文件或目录导入 labelme JSON，提供 dry-run、replace 和 append 策略。
- labelme 导入支持 rectangle、polygon、point、points 四类 shape 映射到 SQLite annotation 元数据，并按 imagePath、相对路径、文件名或 JSON 文件名匹配样本。
- 导入时会跳过不支持的 labelme shape，提示图片尺寸不匹配、重复样本匹配、缺失样本匹配和无效 shape 等问题，不写回原始图片或 labelme 文件。
- 新增 `GET /api/datasets/{dataset_id}/annotation-export?format=labelme`，可导出 labelme JSON ZIP 和 `export_report.json`，默认跳过空标注图片。
- 补充 labelme 导入、单样本导出和数据集 ZIP 导出测试，覆盖 dry-run、replace、append、目录匹配、未知 shape warning 和四类 shape 坐标转换。

## [v0.4.0 Phase 2 批次 C1 导出预检框架] - 2026-05-19

主要更新：

- 新增标注几何转换工具，统一 rectangle/polygon bbox、polygon area、rectangle 转 polygon 和 YOLO 归一化坐标计算。
- 新增 `POST /api/datasets/{dataset_id}/annotation-export-precheck`，为 labelme、COCO detection/segmentation、YOLO detection/segmentation 和 Pascal VOC 后续真实导出提供统一预检。
- 预检会返回类别映射、可导出对象数、跳过对象数、error/warning/info 问题列表，并识别 polygon 转 bbox、rectangle 转 segmentation polygon、point/points 不兼容、越界坐标和无可导出对象。
- 预检 issues 明细按 error、warning、info 优先级截断，避免大数据集中的空样本 info 淹没 polygon/bbox 转换等关键 warning。
- 补充导出预检后端测试，覆盖检测/分割格式兼容、无可导出对象阻断和越界坐标阻断。

## [v0.4.0 Phase 2 标注拖拽稳定性修复] - 2026-05-19

主要更新：

- 标注画布拖拽对象和顶点时会阻止浏览器原生 drag/select 行为，避免出现禁止拖放光标后拖拽中断。
- 已标注对象拖拽预览改为按 `requestAnimationFrame` 合并更新，降低高频 pointer move 导致的父组件重渲染压力。
- 画布坐标状态更新同样按动画帧节流，保持状态提示可用的同时减少拖拽卡顿。

## [v0.4.0 Phase 2 Copilot 审核修复] - 2026-05-19

主要更新：

- 标注页窗口级快捷键监听改为单次注册，通过最新动作引用处理保存、撤销/重做、工具切换和上下张导航，避免指针移动触发重渲染时反复订阅。
- 标注画布对象聚焦命令在目标对象已不存在时会正确清理，避免 stale focus command 反复执行。
- 移除 polygon 草稿状态下 pointer move 的无意义浅拷贝更新，减少画布高频移动时的 React 重渲染。
- 数据集统计中的标注对象数、已标注样本数和标注类别分布改用 SQL 聚合计算，避免加载整批 annotation 行。

## [v0.4.0 Phase 2 批次 B 验收修复] - 2026-05-18

主要更新：

- 标注页“当前类别”和对象类别编辑从 `input + datalist` 改为显式标签下拉，避免浏览器按当前输入值过滤选项导致新建标签看起来缺失。
- 标注画布底图改为由单一 HTML 图片层显示，SVG 只负责标注 overlay，减少底图和标注层分别加载造成的偶发空底图。
- 图片加载失败时隐藏标注层并显示“重试加载”，避免只看到缩小标注框而误判底图位置。
- v0.4.0 Phase 2 批次 B 人工验收已通过。

## [v0.4.0 Phase 2 批次 B] - 2026-05-18

主要更新：

- 数据集详情页的样本、图片、视频等全局统计卡点击后会清空不相关筛选，再进入对应全局结果，避免统计数字和样本列表作用域不一致。
- 重复样本弹窗改为按文件 hash 分组，并以两两对照结构展示样本路径，保留原有统计卡入口。
- “重复”样本筛选结果改为按 hash 分组相邻显示，便于人工对照，且不改变样本真实 `file_status`。
- 样本筛选栏增加审查状态筛选，支持 `unlabeled / in_review / approved / rejected`。
- 数据集统计新增 `annotated_samples`、`annotation_count`、`by_annotation_label`，详情页展示标注样本数、对象数、审查状态和标注类别摘要。
- 补充重复样本分组排序和标注统计 API 回归测试。

## [v0.4.0 Phase 2 批次 A] - 2026-05-18

主要更新：

- 标注页顶栏增加上一张/下一张图片导航，按当前筛选后的正常图片顺序切换，跳过非图片和异常文件。
- 从样本网格或样本详情进入标注页时保留搜索、文件状态、标签、划分和排序上下文，用于连续标注同一批筛选结果。
- 相邻图片导航改为后端单次计算 previous/current/next，不再由前端分页拉取完整图片列表，修复 4800 张图片数据集切换缓慢的问题。
- 切换样本时保留标注页布局，只加载当前图片和标注数据，避免出现整页重新进入工作区的刷新感。
- 增加“切换自动保存”开关，开启后上一张/下一张遇到未保存修改会先保存成功再切换。
- 增加 `[` / `]` 上一张/下一张快捷键，并放入右上角快捷键帮助入口。
- 切换样本或返回数据集前，如果存在未保存标注，弹出“保存并继续 / 放弃修改 / 取消”的三选一确认。
- polygon/rectangle 绘制草稿在切工具、返回、切样本和保存前必须先明确提交或取消，避免半成品对象进入历史状态。
- 对象列表点击对象时同步高亮并居中聚焦画布对象，提升多对象样本定位效率。
- 将快捷键说明收敛到标注页右上角帮助入口，底部状态栏只保留实时状态。
- 保存标注时将对象类别同步到样本标签，使数据集统计、标签筛选和样本卡片标签能反映当前标注类别。
- 移除切图时标注区上方的流式加载提示，加载状态改在顶栏状态徽标显示，避免中央画布区域上下抖动。
- Esc 明确用于取消绘制中的 polygon 草稿和正在拖拽的 rectangle 草稿；已完成对象继续通过撤销或删除处理。
- 左侧工具栏当前类别输入扩宽并提供完整 title，避免短标签名在窄输入框中被截断。
- 将“数据集详情页返回缓存”和“标注页类别/标签管理布局”列入后续 TODO。

## [v0.4.0 P0 布局修复] - 2026-05-18

主要更新：

- 修复样本卡片中标注入口和文件大小信息在窄卡片、长文件名场景下的视觉重叠。
- 将图片标注入口移入卡片信息区的固定操作列，缩略图、文件名、扩展名和大小各自拥有稳定布局空间。
- 修复标注对象过多时右侧列表撑高页面的问题，标注页改为固定视口布局，对象列表独立滚动，画布区域不再随对象数量变化缩放。

## [v0.4.0 标注 Phase 1] - 2026-05-15

主要更新：

- 新增样本级 annotation 元数据表和 REST API，支持读取、全量替换保存图片标注，不写回或修改原始图片文件。
- 支持 rectangle、polygon、point、points 四类基础几何标注，并在保存时自动为新类别补充数据集标签。
- Manifest 导出增加 `annotations` 字段，样本标注可随审计清单一起导出；新增 labelme JSON 结构导出接口。
- 新增图片标注工作区，使用 SVG overlay 实现矩形、多边形和点标注，支持对象列表、类别编辑、备注、隐藏/锁定、删除、撤销/重做和手动保存。
- 样本网格和样本详情侧栏增加标注入口，正常状态的图片样本可直接进入标注页。
- 补充标注 API 测试，覆盖保存、读取、manifest 导出、labelme 导出、非图片拒绝和无效几何校验。

## [v0.3.0 分层划分修复] - 2026-05-03

主要更新：

- 升级按标签分层的数据集划分算法：先为可覆盖的标签在验证集/测试集保留最低覆盖，再按比例和随机种子填充剩余样本。
- 多标签样本会优先用于覆盖多个标签缺口，避免小数据集在比例可行时出现某个标签完全没有进入验证集的误报。
- 当验证集或测试集名额过少导致无法覆盖所有可覆盖标签时，划分结果显示中文容量提示，区分“样本过少/比例过极端”和随机落点问题。
- 标签统计卡增加详情弹窗，展示各标签命中数量、未标注样本数，并支持按标签快速筛选或进入标签管理。
- 补充小样本多标签划分回归测试，覆盖 `father+ok`、单标签和无标签混合场景。

## [v0.3.0 收尾] - 2026-04-30

主要更新：

- 为样本增加审查状态 `unlabeled`、`in_review`、`approved`、`rejected`
- 审查状态保留为底层元数据和统计能力，但不再放入高频批量操作栏，避免干扰主流程
- 增加数据集体检入口，集中检查缺失/无权限文件、重复样本、未标注样本、未划分样本和 test 集缺失
- 将缺失文件修复入口收敛到体检和异常状态栏，样本标题区不再保留独立“修复缺失”按钮
- 将扫描按钮移动到扫描目录旁，并使用蓝色主操作样式；筛选栏保留标签、导入和导出等整理操作
- 增加“打开数据集时自动扫描”设置，用于进入详情页后自动做一次增量扫描并及时标记缺失文件
- 缺失/重复改为统计卡异常提示，有问题时卡片变色，点击后只显示对应问题和操作
- “全部状态”筛选增加“重复”，按重复 hash 过滤样本，不改变样本真实文件状态
- 修复多标签分层划分警告口径，避免样本同时带有多个标签时把真实多样本标签误报为 only 1 sample
- 样本、图片、视频统计卡增加类型筛选入口；样本区增加“选择本页/取消本页”的批量选择方式
- 没有重复样本时，重复详情不再显示无效的“查看重复样本”按钮
- 增加数据集级随机/分层划分工具，支持 train/val/test 比例、是否划出 test、标签分层、随机种子、只处理正常文件或未划分样本
- 增加单样本缺失文件重新定位，更新路径、hash、大小、类型和文件状态
- 增加数据集级缺失文件修复，可按相对路径在新根目录下批量重新挂载缺失样本
- 增加单样本和批量删除样本记录功能，只删除数据库元数据，不删除本地原始文件
- 调整工具栏布局，移除主界面保存筛选器，减少控件堆叠和高度抖动
- 样本卡片聚焦 split 和标签，筛选栏保留文件状态、类型、标签和 split 等高频条件
- 增加未标注样本统计和未标注筛选能力，便于从体检结果直接定位待补标签样本
- 补充审查状态、缺失修复和样本记录删除的 API 测试

## [v0.3.0 修复补充 4] - 2026-04-30

主要更新：

- 明确元数据导入只更新已扫描登记的样本；修改扫描目录后需要先执行扫描
- 数据集没有任何样本时，导入结果直接提示先扫描，而不是逐行报路径不匹配
- 绝对路径不匹配时，如果数据集中存在同名样本，错误信息会附带候选样本的实际绝对路径
- 清理 CSV 中由 Excel 引入的标签外层引号，例如 `"""ok"""` 会按 `ok` 处理

## [v0.3.0 修复补充 3] - 2026-04-30

主要更新：

- 修复样本详情标签区域空白点击会误触发第一个已有标签的问题
- 增强 `absolute_path` 元数据导入匹配，兼容 Windows 路径斜杠、反斜杠和路径归一化
- 当 `absolute_path` 落在数据集扫描目录下时，自动转为相对路径进行辅助匹配
- 绝对路径仍无法匹配时，错误信息增加 normalized 和 relative_to_root 诊断信息

## [v0.3.0 修复补充 2] - 2026-04-30

主要更新：

- 调整样本卡片布局，split 和标签统一进入卡片信息区，避免徽标遮挡文件名、扩展名或标签
- 样本卡片标签超过 3 个时显示 `+N`，减少卡片底部拥挤
- 元数据导入的 `absolute_path` 匹配增加路径归一化，兼容 `D:/...` 和 `D:\...` 写法
- 元数据导入说明明确 `relative_path` 是相对于数据集扫描目录
- 导出流程增加预览弹窗，用户确认后再下载 manifest、CSV 标签表、COCO 骨架或 YOLO 骨架
- 补充绝对路径导入测试

## [v0.3.0 修复补充] - 2026-04-30

主要更新：

- 样本详情侧边栏的标签编辑增加已有标签点选，仍保留输入新标签或别名的能力
- 标签创建和编辑增加名称/别名冲突检测，避免同一数据集内标签名、别名互相重复
- 样本卡片显示 split 标记，搜索筛选栏增加 train/val/test/未划分筛选
- 样本卡片和标签编辑器使用标签颜色，并根据背景色自动选择文字颜色
- 元数据导入弹窗补充 CSV/JSON 示例、字段说明和错误明细展示
- JSON 元数据导入增加格式错误反馈，避免解析失败时前端无感知
- 导出从打开 JSON 页面改为下载文件；CSV 标签表下载为 `.csv`，manifest、COCO 骨架、YOLO 骨架下载为 `.json`
- 导出模板说明调整为“样本级标签表”和“标注格式骨架”，明确当前系统尚未具备 bbox/segmentation 标注导出能力
- 补充 split 筛选、JSON 导入和标签别名冲突测试

## [v0.3.0] - 2026-04-30

主要更新：

- 增加重复样本识别，按文件 hash 聚合重复文件，并在数据集统计中显示重复组和重复样本数
- 扩展标签体系，支持标签颜色、描述、父级标签和别名
- 增加数据集级标签管理弹窗，支持创建、编辑和删除标签
- 增强数据集划分统计，在统计区展示 train、val、test 和未划分样本数量
- 增加元数据导入接口和前端弹窗，支持从本地 CSV/JSON 按 sample id、相对路径、绝对路径、文件名或 hash 匹配样本
- 元数据导入支持批量写入标签、split、备注和自定义属性
- 样本详情侧边栏增加自定义元数据展示
- 增加 CSV、COCO、YOLO 导出模板接口，为后续真实导出和标注格式转换打基础
- manifest 导出补充样本自定义元数据
- 补充 Dataset API、重复样本、标签、元数据导入和导出模板测试

当前状态：

- v0.3.0 指定的 5 项已完成第一版实现
- 后续仍需完善审查状态、保存筛选器、缺失文件重新定位、导入预览与回滚、后台任务化和大数据集性能

## [v0.2.0] - 2026-04-29

主要更新：

- 修复切换每页数量时旧页码导致的分页显示不一致问题，后端同步钳制越界页码
- 增加只读本地目录浏览器，用于创建数据集和扫描时选择目录
- 扫描接口拒绝扫描磁盘根目录或文件系统根目录，避免误扫整盘
- 数据集详情页增加设置入口，支持编辑元数据字段
- 增加数据集删除入口，二次确认后只删除数据库元数据，不删除原始文件
- 增加增量扫描能力，扫描报告包含新增、变更、未变、缺失、跳过和错误数量
- 为样本增加文件状态、文件修改时间和最近扫描时间
- 增加样本批量操作接口和前端批量栏，支持批量添加标签与批量设置 split
- 增加样本预览接口，CSV 样本支持表格预览，图片和视频预览展示文件状态
- 扩展数据集元数据字段：source、modality、license、owner、project、notes
- 样本列表支持分页、每页数量调整、排序字段和升降序
- manifest 导出支持沿用当前搜索、文件类型、标签和排序条件导出筛选子集
- manifest 导出包含新增的数据集元数据、筛选条件、导出样本数和样本扫描状态
- 补充 Dataset API、Scan、Batch Update、Preview、Export 主流程测试

当前状态：

- v0.2.0 重点 5 项和剩余计划项已完成第一版实现
- 后续仍需继续完善缩略图缓存、后台任务化、文件重新定位和更细的元数据导入导出

## [v0.1.0] - 2026-04-29

主要更新：

- 初始化后端和前端项目骨架
- 实现 FastAPI + SQLModel + SQLite 的基础数据管理能力
- 实现 Dataset、Sample、Tag 模型及核心 REST API
- 实现本地文件夹扫描，登记文件大小、扩展名、hash、相对路径
- 实现数据集列表页、详情页、样本网格、详情侧边栏、标签编辑、搜索筛选、基础统计
- 实现 manifest 导出
- 增加基础测试、`.gitignore` 和 `.gitattributes`

当前状态：

- MVP 可本地启动
- 已具备最小可演示与可继续迭代的基础
