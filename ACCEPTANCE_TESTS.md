# ACCEPTANCE TESTS

本文件记录每次更新后需要人工验收的功能和测试方式。每次更新追加到文件开头。

## [v0.3.0 修复补充 4] - 2026-04-30

验收范围：

- 无样本数据集导入提示
- 绝对路径导入诊断信息
- Excel CSV 标签引号清理

验收步骤：

1. 新建数据集并只设置扫描目录，不执行扫描，直接导入 CSV，确认提示需要先扫描。
2. 执行扫描后再导入同一 CSV，确认可按 `relative_path` 匹配样本。
3. 使用与样本实际路径不一致但文件名相同的 `absolute_path` 导入，确认错误中显示同名样本的实际路径。
4. CSV 标签列如果显示为 `"ok"` 或 `"ok;father"`，导入后确认标签名为 `ok`、`father`，没有额外双引号。

通过标准：

- 用户能明确区分“扫描目录已设置”和“样本已登记”。
- 绝对路径不匹配时，错误信息能指出数据集中同名文件的真实保存路径。
- Excel 保存的 CSV 不会把外层引号变成标签名。

## [v0.3.0 修复补充 3] - 2026-04-30

验收范围：

- 标签区域空白点击
- Windows 绝对路径导入匹配

验收步骤：

1. 打开一个样本详情，确保该数据集已有多个标签。
2. 在“已有标签”区域内点击空白位置，确认不会自动删除或添加第一个标签。
3. 点击某个具体标签按钮，确认只有点击标签本身时才会切换选中状态。
4. 准备包含 `absolute_path` 的 CSV，分别使用 `D:\test\1.jpg` 和 `D:/test/2.jpg` 风格路径。
5. 使用 `absolute_path` 匹配导入，确认路径在扫描目录下时可以匹配到样本。
6. 如果仍提示无法匹配，检查错误中的 `relative_to_root` 是否等于样本卡片/详情里的相对路径。

通过标准：

- 标签区域空白点击无副作用。
- 绝对路径导入兼容 Windows 常见路径写法。
- 无法匹配时，错误信息能帮助判断扫描目录、后缀或相对路径是否不一致。

## [v0.3.0 修复补充 2] - 2026-04-30

验收范围：

- 样本卡片 split/标签布局
- 绝对路径元数据导入
- 导出预览再下载

验收步骤：

1. 给同一个样本同时设置 split 和多个标签，确认卡片底部不再出现 split 与标签重叠。
2. 给文件名较短或较长的样本设置 `train`，确认 `train` 不会遮挡文件名或扩展名。
3. 准备 CSV/JSON 元数据，使用 `relative_path` 导入，确认路径相对于数据集扫描目录。
4. 将同一条记录改为 `absolute_path`，路径使用 `D:/...` 形式，确认可以匹配数据库中的 Windows 绝对路径。
5. 点击导出 manifest、CSV 标签表、COCO 骨架、YOLO 骨架，确认先弹出预览，再点击“下载”才保存文件。

通过标准：

- 样本卡片在 split、多个标签、长文件名组合下不发生遮挡。
- `relative_path` 和 `absolute_path` 都能按说明匹配样本。
- 导出预览内容可查看，下载行为由用户显式确认触发。

## [v0.3.0 修复补充] - 2026-04-30

验收范围：

- 单个样本详情选择已有标签
- 标签名和别名冲突检测
- 样本卡片 split 展示与 split 筛选
- CSV/JSON 导入格式提示和错误反馈
- 导出文件下载行为

验收步骤：

1. 打开一个已有标签的数据集，进入任一样本详情侧边栏。
2. 在“标签”区域点击已有标签，保存后确认样本详情和卡片都显示该标签。
3. 在标签体系中尝试创建一个名称与已有标签相同或别名冲突的新标签，确认界面显示冲突错误且不会创建重复标签。
4. 批量选择几个样本设置为 `train` 或 `val`，确认卡片左下角显示对应 split。
5. 使用筛选栏的“全部划分/train/val/test/未划分”筛选，确认样本网格和总数变化正确。
6. 打开“导入”弹窗，确认能看到 CSV 和 JSON 示例。
7. 使用格式错误的 JSON 文件导入，确认界面显示明确错误；使用合法 JSON/CSV 文件导入，确认显示总行数、匹配、更新、跳过和错误明细。
8. 分别导出 manifest、CSV 标签表、COCO 骨架、YOLO 骨架，确认浏览器触发文件下载而不是只打开一个 JSON 页面。

通过标准：

- 单样本编辑和批量编辑都可以复用已有标签。
- 标签名称和别名在同一数据集中不会产生重复或互相冲突。
- split 在统计区、卡片和筛选结果中保持一致。
- 导入失败有可读错误，导入成功后样本详情能看到标签、split、备注和自定义元数据。
- 当前 COCO/YOLO 仍是格式骨架，不应被当作完整标注导出。

## [v0.3.0] - 2026-04-30

验收范围：

- 重复样本识别
- 标签体系管理
- train/val/test 划分统计与批量管理
- CSV/JSON 元数据导入
- CSV、COCO、YOLO 导出模板

准备数据：

1. 在一个测试目录中放入至少 3 个支持文件，其中 2 个文件内容完全相同但文件名不同，用于验证 hash 重复识别。
2. 准备一个 CSV 元数据文件，建议包含 `relative_path,tags,split,notes,source_id` 等列。
3. 如需验证 JSON 导入，准备一个对象数组，每个对象包含 `relative_path`、`tags`、`split` 和任意自定义字段。

验收步骤：

1. 创建或打开一个测试数据集，扫描准备好的目录。
2. 进入数据集详情页，确认统计卡片中显示重复组数和重复样本数。
3. 在重复样本提示区域中确认能看到重复 hash、重复数量和示例路径。
4. 点击“标签”，创建一个父标签，填写颜色、描述和别名。
5. 再创建一个子标签，选择父级标签，保存后重新打开标签弹窗，确认层级、颜色、描述和别名保留。
6. 编辑刚创建的标签颜色或别名，确认保存后列表更新。
7. 勾选多个样本，使用批量栏分别设置 `train`、`val`、`test`，确认统计区的划分数量同步变化。
8. 点击“导入”，选择 CSV 文件路径，按 `relative_path` 匹配，设置标签列为 `tags`，执行导入。
9. 打开被导入的样本详情，确认标签、split、备注和自定义元数据已经写入。
10. 使用 JSON 文件重复导入一次，确认可以匹配样本并更新自定义元数据。
11. 在导出选择中分别选择 `CSV template`、`COCO template` 和 `YOLO template`，点击导出，确认返回 JSON 模板且无后端 500 错误。
12. 再导出 manifest，确认样本对象中包含导入的自定义元数据。

通过标准：

- 重复样本统计与实际相同 hash 文件一致。
- 标签创建、编辑、删除不会影响原始数据文件。
- 批量 split 后统计卡片和样本详情一致。
- 元数据导入只更新数据库元数据，不移动、重命名、删除或覆盖原始文件。
- 导出模板接口可稳定返回结构化 JSON，为后续真实导出保留扩展空间。

## [v0.2.0 修复补充] - 2026-04-29

验收范围：

- 分页切换稳定性
- 本地目录选择
- 扫描根目录保护
- 数据集元数据编辑
- 数据集二次确认删除

验收步骤：

1. 打开一个约 600 个样本的数据集，切到 `120 / 页` 并翻到最后一页。
2. 再切换到 `200 / 页`，确认页面回到第 1 页或后端返回合法页码，不出现“第 4 / 4 页但显示 200 个样本”的状态。
3. 新建数据集，点击扫描目录旁的“选择”，在目录浏览器中选择一个具体子目录。
4. 在目录浏览器中进入 `D:\` 或 `/` 这类根目录，确认不能直接使用根目录作为扫描目录。
5. 手动输入 `D:/` 或 `/` 后执行扫描，确认后端拒绝扫描并显示失败提示。
6. 进入数据集详情页，点击“设置”，补充或修改项目、负责人、来源、模态、许可、描述、备注和扫描目录。
7. 保存后确认详情页展示的字段同步更新。
8. 在设置中尝试删除数据集，确认未输入完整数据集名称时删除按钮不可用。
9. 输入完整数据集名称后删除，确认回到数据集列表，原始文件仍保留在磁盘。

通过标准：

- 切换每页数量时不会保留旧页数据。
- 目录选择只读取目录结构，不修改文件。
- 扫描根目录被拒绝，具体数据集目录可以扫描。
- 删除数据集只删除元数据，不删除原始文件。

## [v0.2.0 补充] - 2026-04-29

验收范围：

- 样本分页
- 样本排序
- 筛选子集 manifest 导出
- 缺失文件恢复路径

验收步骤：

1. 进入任一包含多个样本的数据集详情页。
2. 调整每页数量为 `24 / 页`、`60 / 页` 或 `120 / 页`，确认样本网格数量和页码变化正常。
3. 使用上一页、下一页按钮切换页面，确认页面不会报错，样本总数保持稳定。
4. 分别按创建时间、文件名、文件大小、文件类型、文件状态排序，并切换升序/降序，确认样本顺序变化符合预期。
5. 输入搜索关键词或标签筛选，再点击导出 manifest。
6. 检查导出的 JSON，确认 `filters` 中包含当前筛选条件，`exported_sample_count` 等于筛选后的样本数量。
7. 对缺失样本，把原文件放回原路径后重新扫描，确认该样本状态恢复为 `normal`。

通过标准：

- 分页、排序、筛选组合使用时无前端报错、无后端 500 错误。
- 导出的 manifest 只包含当前筛选条件命中的样本。
- 缺失文件恢复到原路径后，重新扫描能自动恢复状态。

## [v0.2.0] - 2026-04-29

验收范围：

- 增量扫描与扫描报告
- 样本批量标签和批量 split
- 图片/视频/CSV 预览增强
- 数据集元数据字段扩展
- Dataset API / Scan / Export 测试补强

准备数据：

1. 在 `storage/datasets/manual-check/` 下放入至少 1 张图片、1 个视频、1 个 CSV 文件。
2. CSV 建议包含表头，例如 `id,value,label`。
3. 启动后端和前端，打开 `http://127.0.0.1:5173`。

验收步骤：

1. 创建数据集，填写名称、扫描目录、项目、负责人、来源、模态、许可和描述。
2. 进入数据集详情页，确认顶部能看到新增元数据字段。
3. 点击扫描，确认扫描报告显示扫描数、新增数、变更数、未变数、缺失数、跳过数和错误数。
4. 修改一个已扫描文件内容后再次扫描，确认报告中的变更数增加。
5. 删除或临时移走一个已扫描文件后再次扫描，确认缺失数增加，样本网格显示缺失状态。
6. 勾选多个样本，在批量栏输入标签并选择 `train`、`val` 或 `test`，点击应用。
7. 打开任一样本详情，确认批量标签和 split 已生效。
8. 打开 CSV 样本详情，确认能看到表格预览。
9. 打开图片样本详情，确认图片可预览；打开视频样本详情，确认视频控件可播放。
10. 点击导出 manifest，确认 JSON 中包含数据集扩展元数据、样本 `file_status`、`file_modified_at`、`last_scanned_at`、标签和 split。

通过标准：

- 上述步骤无前端报错、无后端 500 错误。
- 扫描不会移动、重命名、删除或覆盖原始数据文件。
- manifest 内容和界面展示一致。
