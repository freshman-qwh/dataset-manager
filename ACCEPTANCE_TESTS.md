# ACCEPTANCE TESTS

本文件记录人工验收重点。最新一轮放在最前，历史步骤合并为核心回归清单，避免验收文档过长。

## [v0.4.0 F2-3 训练导出任务与有界内存 · 第一阶段] - 2026-07-26

验收范围：job 产物临时写入、原子发布、大小/hash 元数据、异常清理、路径安全和原始目录隔离。

步骤：

1. 在隔离应用存储中创建 job 产物工作区并写入内容；确认生成期间仅存在唯一 `.part` 文件。
2. 显式完成产物；确认 `.part` 原子变为最终文件，返回字节数和 SHA-256 与实际内容一致。
3. 分别在写入中抛错、未调用完成、完成后模拟结果持久化失败；确认临时文件、未登记最终文件和空 job 目录均被清理。
4. 使用 `../`、嵌套路径、反斜杠和 Windows 盘符形式作为文件名；确认创建与解析均拒绝目录穿越。
5. 确认产物根位于应用 storage 的 `job-artifacts`，数据集原始目录无新增、移动、重命名或覆盖。
6. 运行产物服务定向测试、后端全量测试、前端生产构建和 `git diff --check`。

通过标准：

- 只有完整产物可见；取消和失败不会留下 `.part` 或未登记归档。
- jobs 只记录文件名、大小、hash 等有界元数据，不保存完整 ZIP/JSON 字节。
- F2-3A 不改变现有同步导出响应，代表性格式任务和下载 API 在 F2-3B 单独验收。

## [v0.4.0 F2-2 扫描任务与增量扫描 · 完成] - 2026-07-26

验收范围：详情页扫描迁移、活动任务恢复、终态刷新、任务中心摘要、旧库同步回退、浏览器控制台和数据安全。

步骤：

1. 在已迁移数据库的数据集详情页打开“管理与导出 → 其他操作 → 更新扫描”，提交扫描；确认按钮立即变为“扫描任务运行中”，页面提示任务已提交且仍可浏览原有样本。
2. 扫描运行时确认全局任务入口显示进行中数量；任务结束后确认详情页自动恢复按钮并显示新增、变更、未变结果，概览和样本网格无需整页刷新即可更新。
3. 打开任务抽屉；确认 `dataset.scan` 阶段使用中文，进度达到 100%，结果摘要显示新增、变更和未变数量。
4. 展开数据概览；确认扫描结果包含扫描、新增、变更、未变、缺失、实际 hash、跳过 hash、不支持和完整错误数。
5. 刷新或重新打开存在 queued/running 扫描的数据集详情页；确认页面恢复活动 job id 和轮询，不重复创建第二个扫描任务。
6. 使用未迁移真实数据库的在线备份副本打开详情页；确认任务中心显示“待启用”，点击扫描后异步端点返回 409 并自动回退同步端点。
7. 等待兼容扫描完成；确认页面明确显示“兼容扫描完成”，样本、概览和结果同步刷新，不永久停留在运行提示。
8. 检查两条浏览器路径的控制台；确认无运行时 error，仅保留既有 React Router v7 future flag 警告。
9. 运行后端全量测试、前端生产构建和 `git diff --check`。

通过标准：

- 已迁移库默认使用可恢复、可取消、可观察的本地扫描任务；详情页不被长扫描请求冻结。
- 未迁移库不静默建表或改 schema，且在一个过渡周期内仍可通过同步端点完成扫描。
- 扫描只更新 SQLite 元数据，不移动、重命名、覆盖原始文件，也不在原始目录创建 sidecar。

## [v0.4.0 F2-2 扫描任务与增量扫描 · 第二阶段] - 2026-07-26

验收范围：异步扫描创建、活动任务去重、runner 阶段/进度、协作取消部分结果、显式重试、旧库安全降级和真实 API。

步骤：

1. 在迁移库调用 `POST /api/datasets/{id}/scan-jobs`；确认返回 201、`created=true`、类型为 `dataset.scan`，参数快照保存规范化目录。
2. 对同一数据集并发提交两个扫描请求；确认仅一个返回 created=true，另一个复用相同 job id，数据库只有一个 queued/running 扫描。
3. 等待首次任务完成；确认状态依次经过 queued/running/succeeded，阶段覆盖 enumerating/hashing/writing/missing_detection/completed，最终结果与同步扫描契约一致。
4. 在首个 100 条批次 hash 期间请求取消；确认任务先记录 cancel_requested_at，批次安全提交后成为 cancelled，result 包含 imported=100、batches_committed=1。
5. 对取消任务显式重试；确认新任务保留 retry_of_id，已提交 100 条计入 unchanged，只补入剩余记录，最终样本总数正确。
6. 在处理器运行时停止服务；确认检查点把任务置为 interrupted 并保留部分结果；重启不盲目续跑，用户可显式重试。
7. 使用未迁移真实库调用异步端点；确认返回 409、jobs 表仍不存在、健康检查和同步扫描端点保持可用。
8. 使用迁移副本真实 API 扫描 2 个受支持文件和 1 个不支持文件；确认首次 hashed=2/imported=2，二次 hashed=0/hash_skipped_unchanged=2。
9. 运行后端全量测试、前端生产构建和 `git diff --check`。

通过标准：

- SQLite 仅有一个任务 worker 和一个扫描元数据写通道；hash worker 只读原始文件。
- 取消、停止、失败和重试均有可解释终态与有界结果，不产生永久 running 或重复活动扫描。
- F2-2B 不改变详情页现有调用；前端迁移与旧库兼容提示在 F2-2C 单独验收。

## [v0.4.0 F2-2 扫描任务与增量扫描 · 第一阶段] - 2026-07-26

验收范围：增量判断、hash 并发边界、分批提交、缺失/权限分类、协作停止部分结果和原始文件安全。

步骤：

1. 在隔离目录放入多种受支持文件并首次扫描；确认全部新文件计算 SHA-256，结果记录 `hashed` 和实际提交批次数。
2. 不修改文件再次扫描；确认 size + mtime 相同的记录全部计入 `hash_skipped_unchanged`，SHA-256 函数调用数为 0。
3. 修改一个文件内容和 mtime、保持其他文件不变；确认只重算该文件 hash，内容变化计入 updated，其他文件计入 unchanged。
4. 使用 6 个新文件和 2 个 hash worker；确认峰值并发恰为 2，不创建额外数据库写线程。
5. 每批 2 条扫描 5 个文件，在第一批提交后触发检查点停止；确认停止结果包含 imported=2、batches_committed=1，再次扫描补齐其余 3 条且前两条不重复。
6. 删除一个已登记文件并制造一个不可读取路径；确认前者标记 missing，后者标记 permission_denied，不删除数据库记录或原始文件。
7. 对扫描前后的隔离原始文件逐个比较内容；确认无移动、重命名、覆盖和新增 sidecar。
8. 运行后端全量测试、前端生产构建与 `git diff --check`。

通过标准：

- 未变化二次扫描跳过绝大多数 hash；变化检测结果与首次全量 hash 语义一致。
- 写事务保持短批次和单写者；取消只停在检查点，已提交元数据可解释且可幂等续扫。
- F2-2A 不启动异步扫描，不改变详情页入口；runner/API 和 UI 分别在后续切片验收。

## [v0.4.0 F2-1 本地任务中心基础 · 完成] - 2026-07-26

验收范围：全局任务入口、轻量抽屉、状态与进度表达、取消/重试操作、未迁移安全降级和浏览器回归。

步骤：

1. 使用未迁移真实库打开任意页面；确认右下角显示“任务中心待启用”，抽屉解释需停止后端并离线升级，不自动改表、不显示全局错误。
2. 使用迁移副本准备 queued、running、succeeded、failed、cancelled、interrupted 记录；确认入口优先显示进行中数量，否则显示需查看的失败/中断数量。
3. 打开抽屉；确认任务按最新优先排列，显示标题、类型、中文状态、阶段、时间、可用进度条和有界错误摘要，列表内部滚动。
4. 对 queued/running 点击取消；确认操作期间按钮禁用，后端状态随后刷新。对 failed/cancelled/interrupted 点击重试；确认出现新的 queued 记录并保留来源。
5. 在抽屉外点击、按 Escape、点击关闭按钮；确认均可关闭且不会改变任务状态。点击手动刷新；确认加载反馈明确。
6. 在桌面和窄屏检查抽屉；确认宽度不超过视口、无横向溢出、入口不改变主页面布局。
7. 在真实旧库和迁移副本各完成一次浏览器操作；确认控制台无运行时 error，仅保留既有 React Router future flag 警告。
8. 运行后端全量测试、前端生产构建与 `git diff --check`；确认真实数据库、原始文件和原始目录无修改。

通过标准：

- 任务状态全局可感知，但不增加常驻企业级任务首页，不挤压数据集和标注主流程。
- schema 未启用、API 暂时错误、取消或重试失败均有克制且可恢复的反馈。
- F2-1 完成后再进入 F2-2 扫描任务迁移；本批不改变现有同步扫描行为。

## [v0.4.0 F2-1 本地任务中心基础 · 第二阶段] - 2026-07-26

验收范围：单写者 runner、处理器注册、隔离失败、协作取消、主动停止、重启恢复，以及创建/取消/重试 API。

步骤：

1. 注册一个会更新阶段和进度的测试处理器，创建 queued 任务并启动 runner；确认任务依次成为 running、succeeded，结果摘要和进度正确。
2. 创建未注册类型和主动抛错的任务；确认各自成为 failed、错误 code/type/message 有界，runner 继续处理后续任务。
3. 让处理器停在明确检查点，对 running 任务请求取消；确认 API 先记录 `cancel_requested_at`，检查点随后将任务置为 cancelled，不强杀线程。
4. 在处理器运行时停止 runner；确认 stop event 在检查点变成 interrupted，runner 可在时限内退出。
5. 预置上一进程遗留的 running 任务并执行启动恢复；确认其成为 interrupted、错误 code 为 `process_restart`，queued 和终态记录不被改写。
6. 调用 `POST /api/jobs`、`POST /api/jobs/{id}/cancel` 和 `POST /api/jobs/{id}/retry`；确认创建返回 201，queued 可直接取消，重试生成 attempt+1 且保留 retry_of_id 的新记录。
7. 在未迁移真实库上重启服务；确认 runner 不建表、不启动，任务 API 返回 409，健康检查和已有业务继续可用。
8. 在迁移副本启动服务并创建未注册类型；确认任务快速成为 failed，再重试生成新记录；原有 1 条完整性问题和核心表计数保持。
9. 运行后端全量测试、前端生产构建和 `git diff --check`；确认实际数据库、原始文件和原始目录无修改。

通过标准：

- 任一时刻仅一个本地 worker 认领写任务；处理器不共享请求 Session。
- 取消、停止和进程重启都有可解释终态，runner 异常不让任务永久悬挂为 running。
- F2-1B 只交付任务基础设施，不迁移扫描/导出，不向原始数据目录写入 sidecar。

## [v0.4.0 F2-1 本地任务中心基础 · 第一阶段] - 2026-07-26

验收范围：jobs migration、启动不静默建表、状态转换、进度约束、取消请求、重试来源、JSON 快照和只读 API。

步骤：

1. 对全新数据库运行 upgrade；确认依次到达 `20260726_0001` 与 `20260726_0002`，jobs 表和 status/created、dataset/status、type/status 索引存在。
2. 对未版本化实际旧库副本升级；确认升级前备份 `quick_check=ok`，原有核心表计数保持，新增 jobs 为空。
3. 只执行应用启动兼容建表逻辑；确认 F2-0 七张基线表可创建，但 jobs 不存在，证明新增表只由 migration 管理。
4. 创建 queued job；确认参数 JSON、数据集、总进度和 attempt=1 持久化，列表与详情读取一致。
5. 执行 queued → running，更新阶段、当前/总进度和错误数，再转为 interrupted；确认开始/完成时间、错误摘要和状态正确。
6. 对 interrupted job 重试；确认原记录保持终态，新记录为 queued、attempt+1、retry_of_id 指向来源且参数快照不漂移。
7. 对 queued 请求取消；确认直接成为 cancelled。对 running 请求取消；确认只记录 cancel_requested_at，等待 runner 在检查点结束，不强杀线程。
8. 尝试终态 → running、非 running 更新进度、进度超过总数或负数；确认返回状态错误且数据库不产生非法状态。
9. 调用 `GET /api/jobs` 与 `GET /api/jobs/{id}`；确认过滤、总数、JSON 字段和 404 正确。对未迁移实际库调用列表；确认返回 409 并提示先升级。
10. 尝试写入超过 64 KiB 的参数快照；确认服务拒绝该任务且数据库中不产生残留记录。
11. 运行后端全量测试、前端生产构建、真实已迁移副本 API 和 `git diff --check`；确认实际数据库没有新增 jobs 表，原始文件不变。

通过标准：

- 状态转换由服务层唯一契约控制，终态不可原地复活，重试创建可追溯的新记录。
- 启动、列表或详情请求不得绕过 Alembic 创建 jobs；未迁移时明确返回 409。
- 参数、结果和错误只保存有界 JSON 摘要，不把样本全集、文件内容或大产物写入 jobs/SQLite。
- 本阶段不启动后台线程、不迁移扫描/导出，不把模型持久化完成冒充 runner 已可用。

## [v0.4.0 F2-0 正式迁移与完整性 · 完成] - 2026-07-26

验收范围：只读完整性报告、缺失 schema、外键违规、孤立元数据分类、清理预览、精确确认、过期保护、修复前备份、事务修复和非技术 UI。

步骤：

1. 调用 `GET /api/system/database-integrity`；确认返回 quick check、当前/head revision、缺失表列、外键违规、可修复动作、受影响记录摘要和报告 token，且该请求不产生备份或数据写入。
2. 使用完整 schema 但构造孤立 sample、annotation、annotation class、tag、sample-tag link、训练准备记录，以及缺失 class/tag/父级引用的隔离库；确认报告逐类识别，记录数量和 ID 摘要正确。
3. 使用缺表或缺列、未到 Alembic head 的数据库；确认状态为“需要维护”，修复预览返回 422，界面要求先停止后端完成正式升级。
4. 在数据集列表打开“数据库维护”；确认默认只读加载，显示 SQLite、结构版本和外键状态，所有清理项目默认未选择，且明确说明不读取或删除原始文件。
5. 选择一个或多个清理项目并点击“生成清理预览”；确认显示本次影响总数、自动备份说明和动态确认文本，未输入完全一致的文本时最终按钮禁用。
6. 使用过期 report token、不精确确认文本或预览后发生变化的数据库调用修复接口；确认分别返回 409/422，数据库不变且不生成无效修复结果。
7. 在隔离库输入精确确认并执行；确认先生成 `quick_check: ok` 的备份，再按依赖顺序清理关联，事务成功后重新报告，外键违规和所选可修复问题归零。
8. 检查修复结果；确认包含每项主记录数、删除数、更新数、修复前后问题数和恢复备份路径，备份可独立打开。
9. 对实际数据库只调用报告和预览前检查；确认识别 1 条历史孤立 annotation class，但因实际库尚未登记 migration head 而不开放清理，实际记录保持不变。
10. 运行后端全量测试、前端生产构建、真实 API 和桌面/390px 浏览器验收；确认入口、弹窗内部滚动、复选框、确认输入和禁用状态无溢出或控制台 error。

通过标准：

- 报告本身始终只读；修复必须经过选择、最新预览、精确确认、备份与事务五道边界。
- 缺失 schema、未迁移、数据库变化、备份失败或事务失败均不允许返回伪成功，且不得静默删除孤立元数据。
- 修复仅影响 SQLite 元数据；样本记录清理也不得移动、覆盖或删除原始文件。
- 实际历史问题在数据所有者完成离线迁移并确认前保持原样，F2-0 功能验收不冒充实际数据清理签收。

## [v0.4.0 F2-0 正式迁移与完整性 · 第一阶段] - 2026-07-26

验收范围：Alembic 基线、全新库创建、旧库副本升级、升级前在线备份、失败保护、Windows/WAL 离线边界和记录计数保持。

步骤：

1. 在 `backend` 运行 `python -m app.cli.database status --database <数据库路径>`；确认输出目标绝对路径、当前版本、目标版本和是否需要升级，不写入数据库。
2. 对正在使用的实际数据库运行 `python -m app.cli.database backup --database <实际数据库> --backup-dir <隔离目录>`；确认返回备份路径与 `quick_check: ok`，实际数据库版本和记录不变。
3. 停止目标数据库的所有使用者，对上一步备份副本运行 `python -m app.cli.database upgrade --database <备份副本> --backup-dir <升级前备份目录>`；确认先产生第二份可恢复备份，再到达 `20260726_0001`。
4. 对空路径运行同一升级命令；确认创建数据集、样本、标签、关联、标注类别、标注、训练准备和 `alembic_version` 表，且不产生无意义的空库备份。
5. 使用缺少已知历史可选列的旧 schema 副本升级；确认补齐数据集、样本、标签和标注兼容列，并把空任务类型回填为检测任务。
6. 在迁移执行中注入失败；确认目标数据库字节不变、版本仍未更新，升级前备份保留，可按错误提示恢复。
7. 在目标库存在 `-wal`、`-shm`、`-journal` 或被其他连接占用时尝试升级；确认命令阻止执行并提示先停止后端。
8. 对实际旧库在线备份副本记录升级前后核心表计数；确认 datasets、samples、tags、sample_tag_links、annotation_classes、annotations 和 training_readiness_states 数量一致，前后 `quick_check` 均为 `ok`。
9. 再次对已到达 head 的数据库运行升级；确认返回 `changed: false`，不重复备份或修改数据。
10. 运行后端全量测试、前端生产构建和 `git diff --check`；确认现有 API 与界面无回归，原始数据目录没有新增 sidecar。

通过标准：

- 迁移只由显式命令触发；实际源库可在线备份，但升级目标必须离线，不在应用启动时静默替换。
- 迁移失败、版本不符、校验失败、WAL 未 checkpoint 或文件占用时，原目标库不被替换，并保留明确恢复路径。
- 全新库与实际旧库副本都能到达唯一 head；核心记录计数保持，历史孤立元数据不在本阶段自动删除。
- 所有产物只涉及 SQLite 元数据、应用临时目录和备份目录，不复制、移动、重命名、覆盖或删除原始数据文件。

## [v0.4.0 UX-A 至 UX-D 总体验收签收] - 2026-07-26

验收范围：语义与入口、连续标注、训练准备、大数据集响应、数据安全和后续批次准入。

步骤与证据：

1. 运行后端语法检查和全量 pytest，确认 60/60 通过；运行前端生产构建和 `git diff --check`，确认无编译或差异格式错误。
2. 读取合法空样本的列表、导航和标注 API；确认 `annotation_progress=completed_empty`、`review_status=in_review`、对象数量为 0，三套语义未重新耦合。
3. 调用数据集、样本页、统计、导航、标注、质量报告、训练准备和重复报告 API；确认均返回 200，质量与训练准备摘要一致。
4. 在浏览器打开已完成检测数据集；确认首屏只突出“复查标注结果”，显示 10/10 完成、10 待审核和无主流程阻断。
5. 打开“准备训练”；确认检查 → 修复 → 确认 → 导出四步存在，摘要为 2 个有对象完成、8 个确认空样本、10 个待审核、0 个阻断，split 为 train 6 / val 4。
6. 点击“复查标注结果”；确认恢复到当前筛选第 6/10 张、剩余 9、已保存、已完成无目标，并保持队列范围可见。
7. 返回详情页；确认数据集标题、10 项样本和缓存内容立即恢复。设置 390×844，确认 document/body 滚动宽度不大于客户区宽度。
8. 检查控制台；确认没有运行时 error，仅保留 React Router v7 future flag 警告。
9. 运行 SQLite 隔离并发基准；确认 300/300 次写入成功、并行读取期间 locked 为 0、150 ms 持锁后竞争写入成功、checkpoint 完整推进。
10. 复核原始文件安全边界；确认本轮只读 API、浏览器和隔离库验证未移动、重命名、删除或覆盖原始文件。

签收结论：

- UX-A 至 UX-D 工程验收通过，不存在阻止进入下一批的 P0/P1 功能问题。
- UX-D 尚未提交，进入 F2 前先建立独立 Git 历史边界。
- 旧库 1 条孤立 annotation class 不自动删除，纳入 F2-0 正式迁移与显式修复。
- “新用户 3 分钟完成首次标注”尚无外部新手研究证据，不将工程验收冒充真实用户研究。
- 后续执行顺序与停止条件见 `docs/post-ux-d-roadmap.md`。

## [v0.4.0 UX-D 大数据集响应 · 第二阶段] - 2026-07-26

验收范围：高标注密度矩阵、统计索引、SQLite WAL/锁等待/外键/checkpoint、真实 API 与详情页缓存响应。

步骤：

1. 运行 `backend/benchmarks/sample_query_benchmark.py --matrix --iterations 3`；确认默认完成 10k/100k × 1/10/100 中不超过 100 万标注行的组合，并把 100k×100 明确标为安全上限跳过，而不是静默缺失。
2. 单独运行 100k×100 档；确认生成 1,000 万标注行的隔离临时库，输出列表、导航、统计、重复报告、SQLite 设置、WAL 大小和 checkpoint 页数，结束后临时数据库自动清理。
3. 对比 10k×100 优化前后结果；确认标注统计 SELECT 从 14 降为 13，p95 由约 765 ms 降至约 272–290 ms，列表、导航和重复报告不随标注密度成比例变慢。
4. 运行 `backend/benchmarks/sqlite_concurrency_benchmark.py`；确认 `journal_mode=wal`、`foreign_keys=true`、`busy_timeout_ms=5000`、`synchronous=1`、`wal_autocheckpoint_pages=1000`。
5. 检查并发输出：1 个写者与 4 个读者并行期间 locked 错误为 0；人为持写锁后竞争写者应等待并在 5 秒前成功；被动 checkpoint 的 busy 为 0 且 checkpointed pages 等于 log pages。
6. 在启用真实外键的隔离库删除带 legacy tag、annotation class、sample 和 annotation 的标签及数据集；确认 tag_id 先置空，标注与 annotation class 正确清理，不遗留新的外键违规。
7. 重启后端并读取实际 SQLite 设置；确认 WAL、外键和超时生效。对已有库运行 `PRAGMA foreign_key_check`，历史孤立元数据只报告，不在未确认时自动删除。
8. 对 20,298 样本数据集分别热调用样本页、导航、统计和重复报告；使用不解析响应体的客户端计时大 JSON，确认状态、数量和响应契约正确，避免把 PowerShell JSON 解析时间误算为服务耗时。
9. 在桌面详情页连续输入两个搜索条件，确认最终只显示后一条件；进入标注页再返回，确认数据集名称、搜索值和样本卡立即恢复且不先闪回空白。
10. 设置 390×844 视口复验相同筛选结果，确认 document/body 滚动宽度不大于客户区宽度；检查控制台无运行时 error，并在结束前恢复默认视口。

通过标准：

- 10k/100k × 1/10/100 annotations/sample 均有可重复结果；100k×100 下列表、导航、返回体和峰值内存保持有界，统计耗时按标注行数增长但不把全部对象加载进 Python。
- SQLite 新连接统一满足 WAL、外键和锁等待策略；短事务读写无 locked 错误，被动 checkpoint 可观测，测试不写入用户数据库或原始数据目录。
- 真实 API 数量与契约正确；桌面与窄屏的快速筛选、缓存恢复和布局通过，旧请求不覆盖新条件。

## [v0.4.0 UX-D 大数据集响应] - 2026-07-26

验收范围：SQL 查询下推、固定查询规模、10k/100k 隔离基准、快速筛选、详情页返回缓存、过期请求保护和响应式布局。

步骤：

1. 运行 `backend/benchmarks/sample_query_benchmark.py`，分别使用 10,000 和 100,000 样本、每样本 1 个标注；确认输出包含列表、导航、统计和重复报告的 p50/p95、SELECT 数量、返回体与峰值内存。
2. 调用样本列表 API，组合搜索、文件类型、正常/重复状态、标签或别名、无标签/有标签、split、审核状态、标注进度、排序和分页；确认总数、末页回退、重复 hash 相邻分组及返回标签与改造前语义一致。
3. 调用样本导航 API，分别验证全部待处理、当前筛选、当前划分和重复虚拟队列；确认当前位置、总数、剩余数、上一张/下一张正确，非图片及不可用文件不进入标注队列。
4. 对比统计与重复报告 API：样本数、容量、类型、扩展名、状态、split、标注进度、审核、标签、重复组、对象数和类别分布与样本数据一致；重复报告不包含唯一 hash。
5. 在详情页连续快速输入两个不同搜索条件，确认较早请求即使晚返回也不会覆盖最新结果；离开页面时未完成请求被取消，不出现误报“样本加载失败”。
6. 设置搜索、标签、split、审核、标注进度、页码、每页数量和排序后进入标注页，再返回详情页；确认数据集名称、统计卡和样本网格立即显示上次内容，筛选状态保持，并在后台刷新为服务端最新数据。
7. 在桌面和 390×844 视口重复快速筛选、翻页、打开样本详情及标注往返；确认样本网格、筛选栏和分页无横向溢出，图片继续按需加载。
8. 确认测试只使用隔离数据库与测试图片；索引和缓存只影响 SQLite 元数据与浏览器内存，不移动、重命名、删除或覆盖原始文件。

通过标准：

- 列表、导航、统计和重复报告不再把整个数据集及全部标签关系装入 Python；列表/导航查询数量不随样本数增长。
- 10k/100k 基准可重复运行，100k 下返回体和峰值内存保持分页级；实际时间作为后续硬件基线记录，不以单次最快值作为承诺。
- 返回详情页保留上次可见内容与筛选，同时后台刷新；旧请求不能覆盖新条件，主动取消不显示为错误。
- API、桌面和窄屏交互通过，且原始数据安全边界保持不变。

## [v0.4.0 UX-C 训练准备闭环 · 完成] - 2026-07-25

验收范围：最近配置恢复、真实导出时间、任务契约校验、兼容入口记录和响应式反馈。

步骤：

1. 打开检测数据集的“准备训练”，在第三步选择非默认格式、指定 split 或已选样本范围，并调整“包含确认空样本”；运行预检后关闭弹窗，再次打开并进入第三步，确认格式、范围、split、已选样本和选项均恢复，并显示保存时间。
2. 只运行预检后回到第一步，确认“最近导出”仍保持原值或“尚无记录”；预检保存配置，但不得冒充真实下载。
3. 对预检通过的范围点击“确认并下载”，重新打开“准备训练”，确认第一步显示本次真实导出时间，第三步继续恢复刚才的类别映射与范围。
4. 从原有“标注训练格式导出”入口完成一次真实下载，再打开“准备训练”，确认最近导出时间同步更新。
5. 打开无阻断的分类数据集，经“准备训练”生成 CSV，在预览弹窗点击“下载”；重新打开向导，确认最近导出时间更新，配置固定恢复为 CSV 与全数据集范围。
6. 调用 `PUT /api/datasets/{id}/training-readiness/config`，确认兼容格式和完整 split/selected 范围返回 200；检测任务提交 CSV、split 范围不提供 split、selected 范围不提供样本 ID 时均返回 422。
7. 调用 `POST /api/datasets/{id}/training-readiness/exports` 后再读取 `GET /api/datasets/{id}/training-readiness`，确认 `last_config` 与 `last_export_at` 一致；随后只更新 config，确认最近导出时间不被清空。
8. 在 390×844 视口打开恢复过配置的第三、四步，确认恢复/保存提示、步骤条、滚动区和底部按钮无横向溢出；桌面视口下信息密度保持清晰。

通过标准：

- 最近配置在重开浏览器或服务后仍可恢复；任务类型不匹配时不套用旧配置。
- 只有真实文件下载路径记录最近导出，预检与关闭弹窗不改变时间；记录失败时已生成下载仍可用，并提供明确提示。
- 所有状态仅写入 SQLite；不移动、重命名、覆盖原始文件，也不在原始目录生成 sidecar。
- API、桌面和窄屏交互均通过，且旧标注导出与分类 CSV 下载不会绕过最近导出记录。

## [v0.4.0 UX-C 训练准备闭环 · 第二阶段] - 2026-07-25

验收范围：四步训练准备流程、问题影响与定位、格式/范围确认、真实导出预检、分类阻断和响应式布局。

步骤：

1. 打开检测或分割数据集详情页，展开“管理与导出”并点击“准备训练”，确认步骤条为检查 → 修复 → 确认 → 导出，摘要中的完成、空样本、待审核、阻断和 split 数字仍可回到对应样本范围。
2. 进入“修复”，确认问题按“阻断导出 / 建议修复 / 仅作提醒”分组，标题和影响说明优先于技术 issue code；点击包含样本的问题后应进入对应样本或标注工作区。
3. 进入“确认”，确认只平铺当前任务兼容格式，其他格式收在“高级格式”；依次选择当前筛选、全部图片、指定 split 和已选样本，未选择任何样本时“运行预检”必须禁用并说明原因。
4. 选择有可导出对象的范围运行预检，确认显示通过状态、样本数、可导出对象数、跳过数、类别映射和中文问题说明，且“确认并下载”可用。
5. 选择无图片的 split 或只含不兼容对象的范围运行预检，确认显示阻断状态和 issue code，“确认并下载”禁用；返回修改范围后必须重新预检。
6. 打开分类数据集并进入第三、四步，确认产物固定为 CSV 标签表；存在缺失文件或其他阻断问题时显示阻断数量并禁用“生成 CSV”。
7. 在 390×844 视口重复第一、三、四步，确认步骤导航、滚动内容和底部操作均可见，页面 `scrollWidth` 不大于视口宽度。
8. 调用 `GET /api/datasets/{id}/training-readiness`，确认问题计数、`issues` 明细和 `truncated_issue_count` 一致；调用 `POST /api/datasets/{id}/annotation-export-precheck`，确认空范围返回 `blocked=true`。

通过标准：

- 质量、划分、格式、范围和预检在一个稳定弹窗内完成；切换步骤不会丢失本轮选择，改变格式或范围会使旧预检失效。
- 问题影响面向用户表达，issue code 仅作为诊断信息；具体样本问题可定位，集合级问题可打开完整质量报告。
- error 必须阻止真实导出，warning/info 不误阻止；导出仍只生成响应内容，不写入或修改原始数据目录。
- 桌面与窄屏均无横向溢出；新增 API 字段在前后端短暂版本不同步时不会导致前端白屏。

## [v0.4.0 UX-C 训练准备闭环 · 第一阶段] - 2026-07-25

验收范围：统一准备训练入口、实时摘要、数字回跳、任务格式推荐、流程衔接与失败恢复。

步骤：

1. 打开已扫描的目标检测数据集详情页，展开“管理与导出”；确认质量与划分不再作为两个平级按钮，主要操作为“准备训练”。
2. 打开“准备训练”，确认顶部显示检查 → 修复 → 确认 → 导出四步，摘要显示任务范围、完成样本、确认空样本、待审核、阻断问题、split 覆盖和推荐格式。
3. 对照 `GET /api/datasets/{id}/training-readiness`，确认界面数字、状态、推荐格式和 split 计数一致；目标检测默认推荐 COCO 检测。
4. 点击“有对象完成”“确认空样本”“待审核”以及 train/val/test/未划分数字；确认弹窗关闭并回到样本工作区，筛选条件与数量对应所点范围。
5. 从准备训练弹窗依次打开“查看质量问题”“配置划分”和“继续选择导出”；确认分别进入现有质量工作台、数据集划分和训练格式导出，导出默认格式与任务推荐一致。
6. 对分类数据集调用摘要并点击“已分类样本”“待分类样本”；确认分别使用“有标签”和“无标签”虚拟范围，不把几何标注进度当作分类完成度。
7. 暂停后端服务后重新打开准备训练，确认弹窗显示加载失败和“重试”，导出操作不可用；恢复服务并点击重试，确认原弹窗加载成功。
8. 在 390×844 和桌面宽度检查弹窗，确认步骤、摘要、split、推荐格式和底部操作可读，页面无横向溢出。

通过标准：用户从一个入口即可理解当前数据能否进入训练、为什么需要修复、split 是否覆盖以及应使用何种格式；摘要数字可追溯到样本范围；本阶段只读取 SQLite 元数据和原始文件，不修改原始数据。

## [v0.4.0 UX-B 连续标注流 · 长会话与失败恢复] - 2026-07-25

验收范围：30+ 张连续处理、会话反馈、保存失败、工作区中断与浏览器重开恢复。

步骤：

1. 建立至少 35 张正常图片的隔离检测数据集，从“开始标注”进入全部待处理队列；确认底栏显示“本次完成 0”。
2. 第 1 张绘制有效矩形，在保存请求不可用时点击“完成并下一张”，确认地址和样本不变、对象仍在、状态仍为有未保存修改，并提示“修改仍保留，可修正后重试”。
3. 恢复 API 后再次点击同一主动作，确认矩形只写入一次，进入第 2 张，底栏变为“本次完成 1”并显示推进耗时。
4. 对后续 29 张使用“确认无目标并下一张”，确认来到第 31 张、队列剩余 5、本次完成 30；过程中页面无运行时错误，计数不跳变或重复增加。
5. 通过样本列表、统计和标注 API 交叉核对：1 张已完成有对象、29 张已完成无目标、5 张待处理，第 1 张只有一个矩形对象且进入待审核。
6. 在第 31 张关闭浏览器标签，新建标签打开数据集详情页；确认主入口为“继续标注”，点击后恢复全部待处理队列和第 31 张。新标签中的“本次完成”从 0 重新开始，不冒充跨会话累计。
7. 在加载工作区时中断 API，确认页面显示“标注工作区加载失败”和“重新加载工作区”；恢复 API 后点击重载，确认回到原地址、队列和样本。
8. 在 390×844 下检查加载失败状态，确认提示和重载按钮保持可见，页面宽度与滚动宽度均为 390px，主工作区不横向溢出。

通过标准：连续处理 30 张时主路径稳定；保存是否成功具有可信反馈；失败不丢未保存对象；接口恢复与浏览器重开均可回到原工作位置；所有写入只发生在 SQLite 元数据与隔离测试副本。

## [v0.4.0 UX-B 连续标注流 · 第二轮打磨] - 2026-07-25

验收范围：队列末位连续推进、筛选范围可解释、跳过语义、完成后复查恢复和窄屏空间。

步骤：

1. 进入带搜索、划分、标注进度或审核筛选的“当前筛选”队列，确认队列控件第二行持续显示生效条件与排序；窄屏可以截断显示，但完整说明仍具有可访问名称和悬浮提示。
2. 打开队列最后一张且确认“剩余”大于 0，检查主动作仍为“下一张 / 完成并下一张 / 确认无目标并下一张”；操作后应回到队列起点，而不是显示队列完成或把剩余数错误改成 0。
3. 点击顶栏右箭头或按 `]`，确认提示为“已跳过上一张，样本完成状态未改变”；有未保存修改时仍先出现保存、放弃、取消三选一，不允许静默丢失。
4. 在未保存确认框中检查初始焦点位于取消，`Shift+Tab` 循环到“保存并继续”，`Tab` 返回取消，`Esc` 关闭并把焦点还给触发控件。
5. 在干净的已完成样本按 `Ctrl+Enter`，确认不重复保存而直接打开队列下一张，底栏显示本次推进耗时。
6. 从任意样本返回一个全部完成的数据集，再点击“复查标注结果”，确认恢复刚才的队列、排序和样本；若最近队列是已清空的待处理队列，应转换为可复查范围而非进入空队列。
7. 分别以 1440×900 和 390×844 验收：页面无横向/整页纵向溢出，对象列表内部滚动；390px 下队列、剩余、保存和进度仍可见，画布高度不少于约 240px，主动作保持 48px 高。
8. 对 10 张人工集和 20k 级数据集读取导航接口，确认末位 `remaining` 包含前面的其他样本，大队列首屏可在可接受时间内返回；运行真实目录只读烟测并确认原图哈希不变。

通过标准：用户能说清当前处理范围、跳过与完成的区别以及真正的队列结束状态；恢复入口不丢位置；桌面与 390px 都能连续操作；测试不修改原始文件。

## [v0.4.0 UX-B 连续标注流 · 验收打磨中] - 2026-07-24

验收范围：可恢复标注入口、轻量队列、草稿/完成语义、类别面板和有界预取。

步骤：

1. 打开尚有待处理图片的数据集，确认首屏只有一个“开始标注”主入口；进入任意队列和样本后返回详情页，确认入口变为“继续标注”，再次点击可恢复上次队列、筛选上下文和样本。
2. 在标注页依次切换“全部待处理、当前筛选、当前划分”，确认顶栏同步显示队列范围、当前位置和剩余数量；当前划分只包含当前 split，当前筛选继续保留搜索、标签、进度、审核等上下文。
3. 绘制对象后点击“保存草稿”，刷新确认对象仍在、标注进度为“处理中”，且样本仍属于待处理队列；自动保存开关不应把样本自动标记完成。
4. 对有对象样本点击“完成并下一张”，确认保存成功、当前样本进入“已完成 · 有对象 / 待审核”，随后自动打开队列中的下一张，并在底栏显示最近一次推进耗时。
5. 对空对象样本点击“确认无目标并下一张”，确认样本进入“已完成 · 无目标 / 待审核”并自动推进；“仅确认无目标”只完成当前样本，不改变当前页面。
6. 在右侧固定区搜索或新建对象类别，确认颜色提示可见，新对象使用当前类别；点击“将对象类别追加到样本标签”时只追加标签，不删除已有标签，也不会在普通保存时隐式同步。
7. 制造未保存修改后尝试返回、切换队列或上/下一张，确认仍经过保存、放弃、取消路径；绘制草稿存在时，完成、保存与切换仍需先处理草稿。
8. 在桌面宽度和约 390px 窄屏下检查标注页，确认无横向溢出、对象列表内部滚动、主完成动作保持可见且画布仍有可操作空间；页面无新增运行时错误提示，相邻预取仅覆盖前后各一张，不修改原始图片。
9. 从一个未限制 split 的“当前筛选”切换到“当前划分”，再切回“当前筛选”，确认原筛选范围和排序恢复；恢复到队列中段时，“剩余”应等于除当前样本外的全部队列项，而不只是当前项之后的数量。
10. 在已完成且没有新修改的样本上确认主动作变为“下一张”，不会再次写入完成状态；到队尾或空队列时主动作禁用并显示明确说明。
11. 快速连续切换队列或样本，确认最终页面始终对应地址栏中的样本；用键盘触发未保存/绘制中确认框，确认焦点进入对话框、Tab 不会跑到背景、Esc 可返回编辑。

通过标准：从详情页到恢复标注不超过一次主操作；连续处理可由一次“完成并下一张”推进；三种队列范围准确；保存草稿、完成有对象和确认无目标互不替代；类别与样本标签边界清晰；测试只修改 SQLite 元数据且可恢复。

## [v0.4.0 UX-A 完成入口与文案收敛] - 2026-07-24

验收范围：详情页首屏信息架构、上下文主操作、二级功能分区和统一产品文案。

步骤：

1. 打开含正常图片的目标检测或分割数据集，确认首屏只突出一个“继续标注”或“复查标注结果”按钮，并显示完成度、审核状态、文件状态和关键阻断项。
2. 打开含不可用文件的数据集，确认首屏主操作切换为“处理不可用文件”；点击后进入缺失/无权限文件处理路径，而不是直接开始标注。
3. 打开未设置扫描目录、尚无样本或旧任务类型数据集，确认主操作分别切换为“设置扫描目录”“扫描样本”“修改任务类型”。
4. 打开分类整理数据集，确认主操作为“整理未分类样本”或“查看分类结果”，点击后滚动到样本工作区并应用对应样本标签筛选。
5. 首次打开详情页时确认“数据概览”和“管理与导出”保持折叠；展开后分别看到统计/分布/扫描结果，以及数据质量/划分/标签/导入/导出入口。
6. 检查筛选、按钮、空状态和导出界面，确认统一使用“数据质量、审核状态、标注进度、无标签、已完成 · 无目标、无目标图片、导出检查”，不再显示旧的并行术语。
7. 在桌面宽度与约 390px 窄屏下检查详情页，确认无横向溢出，首屏主按钮和折叠区标题具有清晰点击区域，样本工作区不会与二级功能重复入口。

通过标准：首屏能够回答“当前状态、下一步、关键阻断”三个问题；同一作用域只有一个主操作；低频功能具有明确归属；三套状态与类别/标签术语保持独立；界面操作不移动、重命名、删除或覆盖原始数据文件。

## [v0.4.0 UX-A3 任务能力接入] - 2026-07-24

验收范围：任务类型驱动标注工具、状态语义、质量规则与默认导出。

步骤：

1. 创建或打开“目标检测”数据集，确认详情页显示“使用矩形框标注目标”；进入标注工作区后只显示选择、矩形和平移工具，快捷键帮助不显示多边形。
2. 展开“管理与导出”，打开“其他操作 → 标注训练格式”，确认默认格式为 `COCO detection`；保存一个 polygon 等历史不兼容对象后运行数据质量检查，确认出现“对象形状与任务不匹配”并可定位对象。
3. 把任务类型改为“多边形分割”，确认详情页说明和标注工作区只提供多边形工具，进度文案显示“分割进度”，训练格式默认 `COCO segmentation`。
4. 把任务类型改为“分类整理”，确认样本卡和详情面板不再显示几何标注入口，筛选栏不显示几何标注进度，“其他操作”中的标注训练格式不可用，普通导出默认选择 CSV 标签表。
5. 在分类整理数据集中给部分样本添加标签，确认统计卡显示“已分类”，数据质量工作台显示“已分类 / 无标签”和“待分类样本”；点击待分类问题后打开对应样本详情，而不是几何工作区。
6. 直接访问分类整理数据集的 `/annotate` 地址，确认显示任务说明和“返回数据集”，不会加载或保存几何对象。
7. 使用旧任务类型数据库记录打开详情页，确认显示迁移提示和“修改任务类型”，不会静默套用任一标注工具。
8. 在窄屏和桌面宽度下检查任务提示卡、筛选栏、统计卡和标注页顶栏，确认无横向溢出、按钮可点击区域清晰、文字层级稳定。

通过标准：任务类型不再只是设置字段，而是实际控制入口、工具、质量语义和默认导出；分类整理不会被几何“未开始”误报；任何检查与测试均不移动、重命名、删除或覆盖原始数据文件。

## [v0.4.0 UX-A2 完成语义与类别边界] - 2026-07-19

验收范围：合法空样本、保存/完成语义、标注类别与样本标签独立边界。

步骤：

1. 打开未开始的图片，保持对象列表为空并点击“确认无目标”，刷新后确认标注进度为“已完成 · 无目标”、审核状态为“待审核”。
2. 打开数据质量工作台，确认该图片计入“确认无目标”，且不出现“标注尚未开始”“标注仍在处理中”或进度冲突问题。
3. 对同一范围执行训练格式导出检查并关闭“包含无目标图片”，确认显示“确认无目标样本被跳过”，而不是“未完成/漏标样本”。
4. 在另一张图片绘制对象，点击“保存草稿”，确认进度为“处理中”；再点击“完成标注”，确认变为“已完成 · 有对象”。
5. 尝试用空对象列表执行“完成标注”，或在已有对象时执行“确认无目标”，确认操作被禁用或 API 返回 400，刷新后既有对象未丢失。
6. 新输入对象类别并保存，确认数据集出现独立标注类别，但样本标签保持不变；此时用该名称筛选样本标签不应命中。
7. 保存完成后点击“类别同步为标签”，确认只追加当前对象类别，已有样本标签不被删除；再次同步不产生重复标签。
8. 通过类别 API 重命名类别，确认关联对象显示名更新；删除类别后对象文字保留、`class_id` 为空，原始图片内容、大小和修改时间均不变。

通过标准：保存草稿、完成有对象和确认无目标三种动作互不替代；确认无目标是合法完成态；对象类别与样本标签只有显式、可解释、追加式同步关系。

## [v0.4.0 UX-A1 语义契约] - 2026-07-19

验收范围：任务类型、标注进度、审核状态和样本标签四套独立语义。

步骤：

1. 创建数据集，确认任务类型只提供“目标检测 / 多边形分割 / 分类整理”，默认选择目标检测，并显示各任务的用途说明。
2. 打开数据集设置，将任务类型依次改为分割和分类整理，确认列表卡片显示中文任务名；旧 `tabular/other` 数据集应显示迁移提示而不是静默映射。
3. 扫描两张图片，确认标注进度均为“未开始”、审核状态均为“未审核”，样本没有标签时显示“无标签”，三者互不替代。
4. 给其中一张图片保存一个对象草稿，确认标注进度变为“处理中”、审核状态仍为“未审核”；点击“完成标注”后审核状态才进入“待审核”，另一张仍保持“未开始 / 未审核”。
5. 在筛选栏分别选择四种标注进度和四种审核状态，确认请求结果和选项维度一致；输入“无标签”只筛选样本标签为空的记录。
6. 重启使用旧 SQLite 数据库的后端，确认旧 `unlabeled` 审核值迁移为“未审核”，已有对象的旧样本迁移为“处理中”，原始图片大小、时间和内容不变。

通过标准：任务类型只接受三个正式值；API、统计、筛选和 UI 不再用同一个“未标注”表达标签、进度或审核；迁移只写 SQLite 元数据。

## [v0.4.0 Phase 2 批次 D 标注质量工作台] - 2026-07-19

验收范围：

- 统一质量报告与数据健康工作台
- 空标注、几何、重复对象、split 泄漏、类别分布与审查状态检查
- 问题筛选、样本/对象定位和复检
- occluded、truncated、difficult 对象属性与格式导出
- 原始数据只读边界

步骤：

1. 准备至少三张图片：一张空标注、一张含两个完全相同的同类 rectangle、一张正常 polygon；把内容相同的两张图片分别设为 train 和 val。
2. 在数据集详情页点击“数据健康”，确认显示错误/警告/提示汇总，并出现 `EMPTY_ANNOTATIONS`、`DUPLICATE_ANNOTATION`、`SPLIT_LEAKAGE`、极少类别或单 split 类别提示。
3. 按严重度和问题类型筛选，点击“重新检查”，确认汇总和列表保持一致；点击具体几何问题的“定位对象”，确认进入正确样本并聚焦对应对象。
4. 对包含 train/val 相同 hash 的全数据集执行训练格式预检，确认出现 `SPLIT_LEAKAGE` error 并阻断下载；改为仅导出 train 后确认不会因为范围外 val 样本而误报。
5. 点击“空标注图片”卡片，确认工作台关闭，主列表自动切换为“图片 + 空标注”，样本数量与报告一致；从网格进入标注页后，上一张/下一张仍限制在当前空标注范围。
6. 尝试保存超出图片宽高的 rectangle 和零面积 polygon，确认 API 返回 400，刷新后原有标注仍存在。
7. 在对象面板勾选遮挡、截断、困难并保存，刷新确认状态保留；导出 LabelMe、COCO 和 Pascal VOC，确认对应 flags、attributes 或 XML 节点为真值。
8. 运行 `cd backend && .\.venv\Scripts\python.exe scripts\smoke_quality_api.py --root "D:\My Datasets\test"`，确认扫描 5,949 文件、0 错误，报告与空标注筛选计数一致且 `sha256_unchanged=true`。

通过标准：

- 质量报告问题计数不受返回明细截断影响，明细按 error、warning、info 优先展示。
- split 泄漏只比较 train/val/test 中相同文件 hash；重复对象只提示，不自动合并或删除。
- 保存校验发生在替换旧标注之前，非法请求不会导致数据丢失。
- 质量工作台可在桌面宽度内稳定滚动，不挤压详情页；空标注与审查筛选会重置不相关条件。
- 质量检查、属性编辑和导出只修改 SQLite 元数据或响应内容，不移动、重命名、删除、覆盖原始文件，也不在原始目录生成旁车文件。

## [v0.4.0 Phase 2 批次 C 训练格式导出] - 2026-07-19

验收范围：

- COCO detection/segmentation 真实 JSON 导出
- YOLO detection/segmentation 标签 ZIP 导出
- Pascal VOC XML ZIP 导出
- 前端预检与下载向导
- 原始数据只读边界

步骤：

1. 准备包含 rectangle、polygon、point 和空标注图片的数据集，在详情页打开“更多操作 → 标注训练格式”。
2. 依次选择六种格式，切换当前筛选、全数据集、指定 split 和已选样本范围，确认预检样本数随范围变化。
3. 对 detection/VOC 预检确认 polygon 转 bbox 显示 warning；对 segmentation 预检确认 rectangle 转四点 polygon 显示 warning；point/points 显示跳过提示。
4. 构造越界坐标或只有不兼容对象的范围，确认预检显示 error 且下载按钮禁用；修正后重新预检才能下载。
5. 下载 COCO detection/segmentation，确认 category/image/annotation id 稳定，bbox、segmentation、area、iscrowd 字段符合契约。
6. 下载 YOLO detection/segmentation，确认 ZIP 含 `labels/<split>/*.txt`、`classes.txt`、`data.yaml` 和 `export_report.json`，坐标均按图片宽高归一化。
7. 下载 Pascal VOC，确认 ZIP 含 `annotations/*.xml` 和 `export_report.json`，文件名、尺寸、类别与 bbox 正确。
8. 运行 `cd backend && .\.venv\Scripts\python.exe scripts\smoke_annotation_export_api.py --root "D:\My Datasets\test"`，确认六种格式均为 200 且 `sha256_unchanged=true`。

通过标准：

- 导出只生成响应内容，不把图片复制进导出包，也不在原始数据目录写入旁车文件。
- 预检 error 阻断导出，warning 可在用户明确查看后继续，类别映射稳定且只包含可导出对象类别。
- 当前筛选、全量、split 和已选样本范围与后端实际导出内容一致。
- 真实数据烟测使用内存 SQLite，仅读取扫描目录；选中原图的 SHA-256、大小和修改时间保持不变。

## [v0.4.0 Phase 2 批次 C2 labelme 导入导出] - 2026-05-19

验收范围：

- labelme JSON 单文件导入
- labelme JSON 目录导入
- dry-run、replace、append 策略
- labelme 单样本导出和数据集 ZIP 导出

步骤：

1. 准备一个包含正常图片样本的数据集，并准备一个 labelme JSON，包含 rectangle、polygon、point、points 四类 shape。
2. 调用 `POST /api/datasets/{dataset_id}/annotations/import-labelme`，传入 `mode=file`、`sample_id` 和 `dry_run=true`，确认响应中 `created_annotations` 数量正确，但刷新样本标注仍为空或保持原状。
3. 用同一 JSON 再调用一次，设置 `dry_run=false`、`strategy=replace`，确认样本标注被写入 SQLite，四类 shape 坐标能在标注 API 中恢复；默认不修改样本标签，只有明确传入 `sync_sample_tags=true` 才追加类别标签。
4. 调用 `POST /api/samples/{sample_id}/annotations/export-labelme`，确认导出的 `shapes` 中 rectangle、polygon、point、points 坐标均为 labelme 二维点数组，`imageWidth/imageHeight` 与图片尺寸一致。
5. 准备一个 labelme JSON 目录，至少包含一个 `imagePath` 为相对路径的文件，以及一个包含不支持 shape_type 的文件；调用目录导入，确认匹配到对应样本，不支持 shape 被 warning 跳过。
6. 对已有标注的样本用 `strategy=append` 导入一个新 shape，确认旧对象仍存在，新对象追加到对象列表末尾。
7. 调用 `GET /api/datasets/{dataset_id}/annotation-export?format=labelme`，确认下载 ZIP 中包含 `annotations/*.json` 和 `export_report.json`，默认跳过空标注图片。

通过标准：

- labelme 导入只写 SQLite 标注元数据；仅在显式开启同步时追加样本标签关系，不移动、重命名、删除、覆盖原始图片，也不修改导入 JSON 文件。
- dry-run 不产生任何数据库写入。
- replace 会用导入 JSON 替换目标样本现有 annotation；append 会保留旧对象并追加新对象。
- rectangle 会规范为 `[xtl, ytl, xbr, ybr]`，导出 labelme 时再转回两个二维点；polygon、point、points 坐标保持原图像素坐标语义。
- 目录导入能按 `imagePath` / 相对路径 / 文件名匹配样本；无法匹配或匹配歧义时返回 error。
- ZIP 导出默认只包含有标注图片，报告中统计导出文件数和跳过的空标注样本数。

## [v0.4.0 Phase 2 批次 C1 导出预检框架] - 2026-05-19

验收范围：

- 标注导出预检 API
- YOLO detection 与 YOLO segmentation 的格式兼容提示
- 越界坐标和无可导出对象阻断

步骤：

1. 准备一个包含正常图片样本的数据集，至少创建 rectangle、polygon 和 point 三类标注对象。
2. 调用 `POST /api/datasets/{dataset_id}/annotation-export-precheck`，请求体为 `{"format":"yolo_detection"}`。
3. 确认响应中 `blocked=false`，rectangle 和 polygon 计入 `exportable_object_count`，point 计入 `skipped_object_count`。
4. 确认 issues 中包含 `POLYGON_TO_BBOX` warning 和 `INCOMPATIBLE_SHAPE_SKIPPED` warning，说明 polygon 会有损转 bbox，point 不导出。
5. 调用同一接口，请求体为 `{"format":"yolo_segmentation"}`，确认 rectangle 和 polygon 可导出，issues 中包含 `RECTANGLE_TO_POLYGON` warning。
6. 准备一个只有 point 标注的样本，调用 `yolo_detection` 预检，确认 `blocked=true` 且包含 `NO_EXPORTABLE_OBJECTS`。
7. 准备一个坐标超出图片宽高的 rectangle 标注，调用任一导出预检，确认 `blocked=true` 且包含 `COORDINATES_OUT_OF_BOUNDS`。
8. 在大数据集上运行预检时，如果 `info_count` 很大且 `truncated_issue_count > 0`，确认 `issues` 明细仍优先展示 error/warning，而不是只展示 `EMPTY_SAMPLE_SKIPPED`。

通过标准：

- 预检只读取样本、图片尺寸和 SQLite 标注元数据，不移动、重命名、删除或覆盖原始图片。
- detection 格式下 polygon 会被识别为 bbox 有损转换，而不是静默当作 polygon 导出。
- segmentation 格式下 polygon 可直接导出，rectangle 转四点 polygon 会提示 warning。
- 无可导出对象、越界坐标、图片尺寸不可读等问题会阻断后续真实导出。
- 响应中的 class map 稳定按类别名排序，为后续 COCO/YOLO/VOC 真实导出复用。
- issues 明细最多返回有限条数时，关键 error/warning 不会被大量空样本 info 淹没。

## [v0.4.0 Phase 2 标注拖拽稳定性修复] - 2026-05-19

验收范围：

- 已标注对象整体拖拽
- 已标注对象顶点拖拽
- 画布高频 pointer move 下的交互稳定性

步骤：

1. 打开一张已有矩形或 polygon 标注对象的正常图片样本，切换到选择工具。
2. 按住对象填充区域连续拖动较长距离，确认对象跟随鼠标移动，不出现浏览器禁止拖放光标，也不会拖动一小段后停止响应。
3. 按住对象顶点控制点拖动，确认顶点可持续跟随鼠标，松开后对象形状稳定保留。
4. 连续快速拖动对象多次，再点击保存并刷新页面，确认最终坐标按最后一次拖动结果恢复。

通过标准：

- 对象和顶点拖拽不会触发浏览器原生拖放。
- 拖拽预览保持流畅，拖动过程中不会因为父组件高频重渲染而中断。
- 松开鼠标后只提交一次最终拖拽结果，保存后的 annotation 元数据正确。

## [v0.4.0 Phase 2 Copilot 审核修复] - 2026-05-19

验收范围：

- 标注页快捷键监听和画布高频 pointer move 性能
- 对象列表聚焦命令的失效清理
- 数据集标注统计聚合结果

步骤：

1. 打开一个包含多张正常图片的标注页，移动鼠标经过画布和 polygon 草稿状态，确认底部坐标状态正常更新，页面没有明显卡顿。
2. 使用 `Ctrl+S`、`Ctrl+Z`、`Ctrl+Y`、`V/R/P/H` 和 `[` / `]`，确认保存、撤销/重做、工具切换和上下张导航仍按原规则工作。
3. 在包含多个对象的样本中点击右侧对象列表，确认画布仍能高亮并聚焦目标对象；删除对象后再次操作其他对象，确认不会出现重复聚焦或卡住的状态。
4. 保存包含多个标注对象的样本后返回数据集详情页，确认标注样本数、标注对象数和按标注类别统计仍与保存结果一致。

通过标准：

- 快捷键监听不会随画布状态刷新反复注册，交互行为保持不变。
- polygon 空移动不会制造无意义的草稿状态更新。
- 失效的对象聚焦命令会被清理，不影响后续对象定位。
- 标注统计字段保持正确，且统计过程不依赖加载全部 annotation 行到内存。

## [v0.4.0 Phase 2 批次 B 验收修复] - 2026-05-18

验收范围：

- 标注页类别下拉完整显示数据集标签
- 标注画布底图加载稳定性

步骤：

1. 在数据集详情页进入标签管理，确认数据集内存在多个标签，例如 `object`、`666`、`777`。
2. 打开任意正常图片样本的标注页，点击左侧“当前类别”下拉，确认所有数据集标签都可选，不会因为当前值是 `666` 而只显示 `666`。
3. 选择不同类别并新建矩形、polygon 或点对象，确认新对象类别跟随当前类别。
4. 选中已有对象，在右侧对象类别下拉中切换为另一个标签，保存并刷新，确认类别保留。
5. 连续打开或切换多张图片，确认底图和标注层一起显示；图片加载中不应只显示缩小标注框。
6. 如果图片请求失败，确认画布显示加载失败提示和“重试加载”按钮，而不是展示无底图的标注层。

通过标准：

- 标注类别来源与数据集标签字典一致。
- 类别选择不再依赖浏览器 datalist 的模糊过滤行为。
- 底图未加载完成或加载失败时不会单独显示标注 overlay。
- 修复只影响前端显示与标注元数据编辑，不移动、重命名、删除或覆盖原始图片文件。

## [v0.4.0 Phase 2 批次 B] - 2026-05-18

验收范围：

- 统计卡点击后的全局筛选作用域
- 重复样本 hash 分组弹窗
- 重复样本列表相邻排序
- 审查状态筛选入口
- 标注统计摘要

步骤：

1. 打开包含图片、视频或 CSV，且存在搜索、类型、状态、标签或 split 筛选条件的数据集详情页。
2. 先设置一个局部筛选，例如只看视频或某个标签；再点击顶部“图片”“视频”“样本”等统计卡，确认样本列表进入对应全局结果，旧的搜索、状态、标签、split 和审查筛选被清空。
3. 准备至少两组重复文件，点击“重复”统计卡，确认弹窗按 hash 分组展示，并以左右对照方式显示同组样本路径。
4. 在重复弹窗点击“查看重复样本”，确认样本列表只显示重复样本，且同一 hash 的样本相邻出现；卡片上的真实文件状态仍显示为正常、缺失或无权限等原始状态，而不是被改写成重复。
5. 在筛选栏选择“未标注 / 待审核 / 已通过 / 已拒绝”，确认列表按 `review_status` 过滤，并可与其他手动筛选条件组合。
6. 保存一张图片的多个标注对象后返回详情页，确认“标注”统计卡显示已标注样本数和对象数，审查状态摘要更新，标注类别摘要显示对象类别分布。
7. 点击审查状态摘要或标注类别摘要中的条目，确认会进入对应全局筛选结果，不保留先前不相关筛选。

通过标准：

- 全局统计数字对应的点击结果不会被旧筛选条件污染。
- 重复样本弹窗能以 hash 组为单位帮助人工对照。
- “重复”仍是虚拟查询条件，不改变数据库中的 `file_status`。
- 审查状态作为筛选能力暴露在主筛选栏或摘要区，而不占用批量工具栏。
- 标注统计字段和详情页摘要能反映已保存的 annotation 元数据。

## [v0.4.0 Phase 2 批次 A] - 2026-05-18

验收范围：

- 标注页上一张/下一张图片导航
- 大数据集标注切换性能
- 标注入口保留筛选上下文
- 未保存修改三选一确认
- 切换自动保存
- 上一张/下一张快捷键
- 绘制草稿提交/取消互斥确认
- 对象列表点击聚焦
- 标注对象标签同步样本标签
- 切图加载状态不造成标注区抖动
- 当前类别输入显示
- 快捷键帮助入口

步骤：

1. 打开包含多张正常图片、至少一个非图片或异常文件的数据集，筛选为图片、正常状态，并可叠加搜索、标签或 split 条件。
2. 从样本网格点击某张图片的标注入口，确认 URL 中保留筛选与排序上下文，标注页顶部显示当前图片在筛选结果中的序号。
3. 点击“下一张/上一张”，确认只在当前筛选后的正常图片中切换，不跳到视频、CSV、缺失或无权限样本。
4. 新增或编辑一个标注对象但不保存，点击下一张或返回，确认出现“保存并继续 / 放弃修改 / 取消”三种选择。
5. 选择“取消”后确认仍停留在当前样本；选择“放弃修改”后确认可以切换且原样本未写入新标注；选择“保存并继续”后确认保存成功再切换。
6. 开始绘制 polygon 但不完成，尝试切换工具、保存、返回或切换样本，确认先出现草稿处理弹窗，可提交草稿、取消草稿或返回继续绘制；点数不足时提交按钮不可用。
7. 在已有多个对象的样本中点击右侧对象列表不同对象，确认对象被高亮，画布视图居中聚焦到对应对象附近。
8. 打开“切换自动保存”，新增或编辑对象后点击下一张，确认不再弹出 dirty 三选一，而是先保存成功再切换；关闭后恢复三选一确认。
9. 使用 `[` 和 `]` 确认可以切换上一张/下一张，并且草稿与 dirty 状态仍按上述规则处理。
10. 保存带有类别的标注后返回数据集详情页，确认标签统计、标签筛选和样本卡片标签能看到刚保存的对象类别。
11. 连续点击上一张/下一张，确认不会在顶栏下方插入“正在加载当前图片和标注”的横条，中央画布区域不会因加载提示上下跳动。
12. 开始拖拽矩形但不松开鼠标时按 Esc，确认矩形草稿被取消；松开鼠标已生成的矩形应使用撤销或删除处理。
13. 在左侧当前类别输入中输入或选择 `object`，确认完整可见；较长类别可通过悬停查看完整 title。
14. 打开右上角快捷键帮助，确认 V/R/P/H、`[`/`]`、Ctrl+S、Delete、Esc 等提示可见；底部状态栏不再显示长串快捷键文本。

通过标准：

- 连续标注导航遵守当前筛选上下文，并只处理正常图片样本。
- 大数据集切换不再因前端全量分页拉取当前筛选结果而等待数秒。
- dirty 状态切换样本/返回不会无提示丢失标注。
- 自动保存只在保存成功后切换样本，保存失败时保留在当前样本。
- 绘制中草稿不会在未选择提交或取消时污染保存和历史状态。
- 切图加载状态不改变标注区布局高度。
- 当前类别输入至少能完整显示常见短标签，后续标签管理布局按 TODO 继续优化。
- 标注对象类别能同步成为样本标签，外部标签统计和筛选随保存结果更新。
- 对象定位、保存、撤销/重做和原始文件安全策略保持可用。

## [v0.4.0 P0 布局修复] - 2026-05-18

验收范围：

- 样本网格卡片底部信息布局
- 图片标注入口位置
- 标注页对象列表独立滚动
- 标注画布稳定视口

步骤：

1. 打开一个包含正常图片样本的数据集，选择每页 60 或 120 项，并使用较窄浏览器宽度查看样本网格。
2. 确认图片样本卡片的信息区中文件名、扩展名、文件大小和标注按钮互不重叠。
3. 点击卡片缩略图或文件名区域，确认仍能打开样本详情；点击标注按钮，确认进入标注工作区。
4. 打开一个已有 10 个以上标注对象的图片样本，或连续创建多个点/框对象。
5. 确认右侧“标注对象”列表出现自己的滚动条，页面整体滚动条不会随对象数量异常变长。
6. 滚动右侧对象列表时，确认中央画布、左侧工具栏、顶部保存区和底部状态栏保持稳定，不随列表滚动或对象数量变化缩放。

通过标准：

- 样本卡片在长文件名和窄列宽下没有文字/按钮遮挡。
- 标注对象数量增加只影响右侧列表内部滚动。
- 画布尺寸和当前缩放状态不会因为对象列表高度变化而异常放大。
- 修复只调整前端布局，不移动、重命名、删除或覆盖原始数据文件。

## [v0.4.0 标注 Phase 1] - 2026-05-15

验收范围：

- 图片标注工作区入口
- 矩形、多边形和点标注
- 对象列表、类别编辑、删除、撤销/重做
- 标注保存、刷新恢复和 manifest 导出
- 原始文件安全

步骤：

1. 打开一个包含正常图片样本的数据集，确认图片卡片右下角有标注按钮。
2. 点击标注按钮进入 `/datasets/{id}/annotate?sample={sampleId}`，确认图片能在中央画布显示。
3. 在左侧工具栏选择矩形，拖拽画一个框；选择多边形，连续点 3 个以上点后点击“完成多边形”；选择点，在图片上点选一次。
4. 在右侧对象列表中切换对象，修改类别和备注，尝试隐藏/显示、锁定/解锁和删除对象。
5. 使用撤销/重做按钮确认对象变化能回退和恢复。
6. 点击保存，刷新浏览器页面，确认标注对象仍然存在。
7. 返回数据集详情页，导出 manifest，确认对应样本包含 `annotations`，且每个对象有 `label`、`shape_type`、`points`。
8. 对 CSV/视频样本确认不会出现可用的几何标注工作区。

通过标准：

- 标注页能完成矩形、多边形、点三类对象的创建和编辑。
- 保存后刷新仍能从数据库恢复标注。
- Manifest 包含标注对象，坐标为原始图片坐标。
- 所有标注操作只写 SQLite 元数据，不移动、重命名、删除或覆盖原始图片。

## [v0.3.0 分层划分修复] - 2026-05-03

验收范围：

- 小样本多标签数据集的按标签分层划分
- 验证集标签覆盖下限
- 随机种子和 train/val 数量稳定性
- 极端比例下的容量不足提示
- 标签数量统计

步骤：

1. 打开数据集 `1`，进入“数据集划分”。
2. 设置 `train=70`、`val=30`，不勾选“划分 test”，勾选“按标签分层”和“只处理正常文件”，随机种子填 `42`。
3. 点击应用划分，确认结果为 `处理：15 / 15`、`train：11`、`val：4`、`test：0`。
4. 确认不再出现 `ok: 3 eligible samples, but none landed in validation split.`。
5. 在样本筛选中分别查看 `ok`、`father`、`ng` 标签，确认每个标签至少有一个样本位于 `val`。
6. 再设置 `train=90`、`val=10`，不划分 test，确认结果提示验证集名额不足，而不是随机落点失败。
7. 勾选“划分 test”，设置 `train=80`、`val=10`、`test=10`，确认测试集名额不足时会提示至少需要的名额数量。
8. 点击顶部“标签”统计卡，确认能看到每个标签的样本命中数、未标注样本数，并可点击单个标签筛选样本。

通过标准：

- 比例计数保持稳定，且可覆盖标签不会在验证集中完全缺失。
- 多标签样本可作为多个标签的验证集覆盖保底。
- 极端比例导致不可覆盖时，提示说明 split 名额不足。
- 标签统计清楚说明多标签会重复计数。
- 操作只更新样本 split 元数据，不移动、重命名、删除或覆盖原始文件。

## [v0.3.0 收尾优化] - 2026-05-02

验收范围：

- 扫描按钮与扫描目录绑定
- 体检内修复缺失
- 缺失/重复统计卡详情
- 重复样本筛选
- 样本/图片/视频统计卡筛选
- 样本网格选择本页
- 打开数据集自动扫描设置

步骤：

1. 打开任一数据集，确认顶部扫描目录旁有蓝色“扫描”按钮，筛选栏里不再出现扫描按钮。
2. 点击蓝色“扫描”，确认执行当前数据集扫描目录的增量扫描，扫描报告数量正常更新。
3. 打开“设置”，勾选“打开数据集时自动扫描”并保存；重新进入该数据集，确认会自动执行一次扫描。
4. 制造缺失文件后重新扫描，确认“缺失”统计卡变色；点击统计卡后只显示缺失/无权限信息和“查看/修复”操作。
5. 点击“缺失”详情里的“修复”，确认打开缺失修复弹窗；样本标题区外部不再有独立“修复缺失”按钮。
6. 准备两个内容完全相同的文件并扫描，确认“重复”统计卡变色；点击统计卡后只显示重复摘要和“查看重复”。
7. 在“全部状态”下拉中选择“重复”，确认列表只显示重复 hash 涉及的样本。
8. 没有重复样本时点击“重复”统计卡，确认不显示“查看重复样本”按钮。
9. 点击“样本/图片/视频”统计卡，确认分别切到全部类型、图片、视频筛选。
10. 打开“体检”，确认“数据划分”块使用蓝色系，且仍可进入划分工具；文件状态块中仍能进入缺失修复。
11. 在样本区域点击“选择本页/取消本页”，确认只影响当前页样本，批量栏数量同步变化。
12. 对多标签样本执行按标签分层划分，确认某标签实际有多个可处理样本时，不再错误提示该标签 only 1 sample。

通过标准：

- 扫描、体检、统计卡、筛选栏没有重复入口或布局重叠。
- 自动扫描只读取文件并更新元数据，不移动、重命名、删除或覆盖原始文件。
- “重复”是查询筛选条件，不改变样本真实 `file_status`。
- “选择本页”不跨页隐式选择，避免误批量编辑不可见样本。

## 核心回归清单

每轮发布前至少抽查：

1. 数据集：创建、编辑、删除元数据，根目录扫描保护有效。
2. 扫描：新增、变更、缺失、无权限和不支持文件能生成报告。
3. 样本：分页、排序、搜索、文件类型/状态/split/标签筛选稳定。
4. 标签：创建、编辑、删除、颜色、层级、别名和冲突检测可用。
5. 批量：批量标签、批量 split、单选/多选删除样本记录可用，且不删除本地文件。
6. 预览：图片、视频、CSV 详情预览可用；缺失文件有明确提示。
7. 导入：CSV/JSON 可按 `relative_path`、`absolute_path`、`filename`、`file_hash` 匹配并写入标签、split、备注和自定义元数据。
8. 导出：manifest、CSV 标签表、COCO/YOLO 骨架先预览再下载，图片样本 manifest 可包含 annotations。
9. 划分：随机/分层 train/val/test 支持 test 开关、随机种子、只处理正常文件和只处理未划分样本。
10. 安全：所有扫描、导入、修复、删除记录操作都不修改、不移动、不覆盖原始数据文件。
