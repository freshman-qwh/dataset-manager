# ACCEPTANCE TESTS

本文件记录人工验收重点。最新一轮放在最前，历史步骤合并为核心回归清单，避免验收文档过长。

## [v0.4.0 UX-B 连续标注流 · 长会话与失败恢复] - 2026-07-25

验收范围：30+ 张连续处理、会话反馈、保存失败、工作区中断与浏览器重开恢复。

步骤：

1. 建立至少 35 张正常图片的隔离检测数据集，从“开始标注”进入全部待处理队列；确认底栏显示“本次完成 0”。
2. 第 1 张绘制有效矩形，在保存请求不可用时点击“完成并下一张”，确认地址和样本不变、对象仍在、状态仍为有未保存修改，并提示“修改仍保留，可修正后重试”。
3. 恢复 API 后再次点击同一主动作，确认矩形只写入一次，进入第 2 张，底栏变为“本次完成 1”并显示推进耗时。
4. 对后续 29 张使用“确认无目标并下一张”，确认来到第 31 张、队列剩余 5、本次完成 30；过程中页面无运行时错误，计数不跳变或重复增加。
5. 通过样本列表、统计和标注 API 交叉核对：1 张已完成有对象、29 张已完成无目标、5 张待处理，第 1 张只有一个矩形对象且进入待审核。
6. 在第 31 张关闭浏览器标签，新建标签打开数据集详情页；确认主入口为“继续标注”，点击后恢复全部待处理队列和第 31 张。新标签中的“本次完成”从 0 重新开始，不冒充跨会话累计。
7. 在加载工作区时中断 API，确认页面显示“标注工作区加载失败”和“重新加载工作区”；恢复 API 后点击重载，确认回到原地址、队列和样本。
8. 在 390×844 下检查加载失败状态，确认提示和重载按钮保持可见，页面宽度与滚动宽度均为 390px，主工作区不横向溢出。

通过标准：连续处理 30 张时主路径稳定；保存是否成功具有可信反馈；失败不丢未保存对象；接口恢复与浏览器重开均可回到原工作位置；所有写入只发生在 SQLite 元数据与隔离测试副本。

## [v0.4.0 UX-B 连续标注流 · 第二轮打磨] - 2026-07-25

验收范围：队列末位连续推进、筛选范围可解释、跳过语义、完成后复查恢复和窄屏空间。

步骤：

1. 进入带搜索、划分、标注进度或审核筛选的“当前筛选”队列，确认队列控件第二行持续显示生效条件与排序；窄屏可以截断显示，但完整说明仍具有可访问名称和悬浮提示。
2. 打开队列最后一张且确认“剩余”大于 0，检查主动作仍为“下一张 / 完成并下一张 / 确认无目标并下一张”；操作后应回到队列起点，而不是显示队列完成或把剩余数错误改成 0。
3. 点击顶栏右箭头或按 `]`，确认提示为“已跳过上一张，样本完成状态未改变”；有未保存修改时仍先出现保存、放弃、取消三选一，不允许静默丢失。
4. 在未保存确认框中检查初始焦点位于取消，`Shift+Tab` 循环到“保存并继续”，`Tab` 返回取消，`Esc` 关闭并把焦点还给触发控件。
5. 在干净的已完成样本按 `Ctrl+Enter`，确认不重复保存而直接打开队列下一张，底栏显示本次推进耗时。
6. 从任意样本返回一个全部完成的数据集，再点击“复查标注结果”，确认恢复刚才的队列、排序和样本；若最近队列是已清空的待处理队列，应转换为可复查范围而非进入空队列。
7. 分别以 1440×900 和 390×844 验收：页面无横向/整页纵向溢出，对象列表内部滚动；390px 下队列、剩余、保存和进度仍可见，画布高度不少于约 240px，主动作保持 48px 高。
8. 对 10 张人工集和 20k 级数据集读取导航接口，确认末位 `remaining` 包含前面的其他样本，大队列首屏可在可接受时间内返回；运行真实目录只读烟测并确认原图哈希不变。

通过标准：用户能说清当前处理范围、跳过与完成的区别以及真正的队列结束状态；恢复入口不丢位置；桌面与 390px 都能连续操作；测试不修改原始文件。

## [v0.4.0 UX-B 连续标注流 · 验收打磨中] - 2026-07-24

验收范围：可恢复标注入口、轻量队列、草稿/完成语义、类别面板和有界预取。

步骤：

1. 打开尚有待处理图片的数据集，确认首屏只有一个“开始标注”主入口；进入任意队列和样本后返回详情页，确认入口变为“继续标注”，再次点击可恢复上次队列、筛选上下文和样本。
2. 在标注页依次切换“全部待处理、当前筛选、当前划分”，确认顶栏同步显示队列范围、当前位置和剩余数量；当前划分只包含当前 split，当前筛选继续保留搜索、标签、进度、审核等上下文。
3. 绘制对象后点击“保存草稿”，刷新确认对象仍在、标注进度为“处理中”，且样本仍属于待处理队列；自动保存开关不应把样本自动标记完成。
4. 对有对象样本点击“完成并下一张”，确认保存成功、当前样本进入“已完成 · 有对象 / 待审核”，随后自动打开队列中的下一张，并在底栏显示最近一次推进耗时。
5. 对空对象样本点击“确认无目标并下一张”，确认样本进入“已完成 · 无目标 / 待审核”并自动推进；“仅确认无目标”只完成当前样本，不改变当前页面。
6. 在右侧固定区搜索或新建对象类别，确认颜色提示可见，新对象使用当前类别；点击“将对象类别追加到样本标签”时只追加标签，不删除已有标签，也不会在普通保存时隐式同步。
7. 制造未保存修改后尝试返回、切换队列或上/下一张，确认仍经过保存、放弃、取消路径；绘制草稿存在时，完成、保存与切换仍需先处理草稿。
8. 在桌面宽度和约 390px 窄屏下检查标注页，确认无横向溢出、对象列表内部滚动、主完成动作保持可见且画布仍有可操作空间；页面无新增运行时错误提示，相邻预取仅覆盖前后各一张，不修改原始图片。
9. 从一个未限制 split 的“当前筛选”切换到“当前划分”，再切回“当前筛选”，确认原筛选范围和排序恢复；恢复到队列中段时，“剩余”应等于除当前样本外的全部队列项，而不只是当前项之后的数量。
10. 在已完成且没有新修改的样本上确认主动作变为“下一张”，不会再次写入完成状态；到队尾或空队列时主动作禁用并显示明确说明。
11. 快速连续切换队列或样本，确认最终页面始终对应地址栏中的样本；用键盘触发未保存/绘制中确认框，确认焦点进入对话框、Tab 不会跑到背景、Esc 可返回编辑。

通过标准：从详情页到恢复标注不超过一次主操作；连续处理可由一次“完成并下一张”推进；三种队列范围准确；保存草稿、完成有对象和确认无目标互不替代；类别与样本标签边界清晰；测试只修改 SQLite 元数据且可恢复。

## [v0.4.0 UX-A 完成入口与文案收敛] - 2026-07-24

验收范围：详情页首屏信息架构、上下文主操作、二级功能分区和统一产品文案。

步骤：

1. 打开含正常图片的目标检测或分割数据集，确认首屏只突出一个“继续标注”或“复查标注结果”按钮，并显示完成度、审核状态、文件状态和关键阻断项。
2. 打开含不可用文件的数据集，确认首屏主操作切换为“处理不可用文件”；点击后进入缺失/无权限文件处理路径，而不是直接开始标注。
3. 打开未设置扫描目录、尚无样本或旧任务类型数据集，确认主操作分别切换为“设置扫描目录”“扫描样本”“修改任务类型”。
4. 打开分类整理数据集，确认主操作为“整理未分类样本”或“查看分类结果”，点击后滚动到样本工作区并应用对应样本标签筛选。
5. 首次打开详情页时确认“数据概览”和“管理与导出”保持折叠；展开后分别看到统计/分布/扫描结果，以及数据质量/划分/标签/导入/导出入口。
6. 检查筛选、按钮、空状态和导出界面，确认统一使用“数据质量、审核状态、标注进度、无标签、已完成 · 无目标、无目标图片、导出检查”，不再显示旧的并行术语。
7. 在桌面宽度与约 390px 窄屏下检查详情页，确认无横向溢出，首屏主按钮和折叠区标题具有清晰点击区域，样本工作区不会与二级功能重复入口。

通过标准：首屏能够回答“当前状态、下一步、关键阻断”三个问题；同一作用域只有一个主操作；低频功能具有明确归属；三套状态与类别/标签术语保持独立；界面操作不移动、重命名、删除或覆盖原始数据文件。

## [v0.4.0 UX-A3 任务能力接入] - 2026-07-24

验收范围：任务类型驱动标注工具、状态语义、质量规则与默认导出。

步骤：

1. 创建或打开“目标检测”数据集，确认详情页显示“使用矩形框标注目标”；进入标注工作区后只显示选择、矩形和平移工具，快捷键帮助不显示多边形。
2. 展开“管理与导出”，打开“其他操作 → 标注训练格式”，确认默认格式为 `COCO detection`；保存一个 polygon 等历史不兼容对象后运行数据质量检查，确认出现“对象形状与任务不匹配”并可定位对象。
3. 把任务类型改为“多边形分割”，确认详情页说明和标注工作区只提供多边形工具，进度文案显示“分割进度”，训练格式默认 `COCO segmentation`。
4. 把任务类型改为“分类整理”，确认样本卡和详情面板不再显示几何标注入口，筛选栏不显示几何标注进度，“其他操作”中的标注训练格式不可用，普通导出默认选择 CSV 标签表。
5. 在分类整理数据集中给部分样本添加标签，确认统计卡显示“已分类”，数据质量工作台显示“已分类 / 无标签”和“待分类样本”；点击待分类问题后打开对应样本详情，而不是几何工作区。
6. 直接访问分类整理数据集的 `/annotate` 地址，确认显示任务说明和“返回数据集”，不会加载或保存几何对象。
7. 使用旧任务类型数据库记录打开详情页，确认显示迁移提示和“修改任务类型”，不会静默套用任一标注工具。
8. 在窄屏和桌面宽度下检查任务提示卡、筛选栏、统计卡和标注页顶栏，确认无横向溢出、按钮可点击区域清晰、文字层级稳定。

通过标准：任务类型不再只是设置字段，而是实际控制入口、工具、质量语义和默认导出；分类整理不会被几何“未开始”误报；任何检查与测试均不移动、重命名、删除或覆盖原始数据文件。

## [v0.4.0 UX-A2 完成语义与类别边界] - 2026-07-19

验收范围：合法空样本、保存/完成语义、标注类别与样本标签独立边界。

步骤：

1. 打开未开始的图片，保持对象列表为空并点击“确认无目标”，刷新后确认标注进度为“已完成 · 无目标”、审核状态为“待审核”。
2. 打开数据质量工作台，确认该图片计入“确认无目标”，且不出现“标注尚未开始”“标注仍在处理中”或进度冲突问题。
3. 对同一范围执行训练格式导出检查并关闭“包含无目标图片”，确认显示“确认无目标样本被跳过”，而不是“未完成/漏标样本”。
4. 在另一张图片绘制对象，点击“保存草稿”，确认进度为“处理中”；再点击“完成标注”，确认变为“已完成 · 有对象”。
5. 尝试用空对象列表执行“完成标注”，或在已有对象时执行“确认无目标”，确认操作被禁用或 API 返回 400，刷新后既有对象未丢失。
6. 新输入对象类别并保存，确认数据集出现独立标注类别，但样本标签保持不变；此时用该名称筛选样本标签不应命中。
7. 保存完成后点击“类别同步为标签”，确认只追加当前对象类别，已有样本标签不被删除；再次同步不产生重复标签。
8. 通过类别 API 重命名类别，确认关联对象显示名更新；删除类别后对象文字保留、`class_id` 为空，原始图片内容、大小和修改时间均不变。

通过标准：保存草稿、完成有对象和确认无目标三种动作互不替代；确认无目标是合法完成态；对象类别与样本标签只有显式、可解释、追加式同步关系。

## [v0.4.0 UX-A1 语义契约] - 2026-07-19

验收范围：任务类型、标注进度、审核状态和样本标签四套独立语义。

步骤：

1. 创建数据集，确认任务类型只提供“目标检测 / 多边形分割 / 分类整理”，默认选择目标检测，并显示各任务的用途说明。
2. 打开数据集设置，将任务类型依次改为分割和分类整理，确认列表卡片显示中文任务名；旧 `tabular/other` 数据集应显示迁移提示而不是静默映射。
3. 扫描两张图片，确认标注进度均为“未开始”、审核状态均为“未审核”，样本没有标签时显示“无标签”，三者互不替代。
4. 给其中一张图片保存一个对象草稿，确认标注进度变为“处理中”、审核状态仍为“未审核”；点击“完成标注”后审核状态才进入“待审核”，另一张仍保持“未开始 / 未审核”。
5. 在筛选栏分别选择四种标注进度和四种审核状态，确认请求结果和选项维度一致；输入“无标签”只筛选样本标签为空的记录。
6. 重启使用旧 SQLite 数据库的后端，确认旧 `unlabeled` 审核值迁移为“未审核”，已有对象的旧样本迁移为“处理中”，原始图片大小、时间和内容不变。

通过标准：任务类型只接受三个正式值；API、统计、筛选和 UI 不再用同一个“未标注”表达标签、进度或审核；迁移只写 SQLite 元数据。

## [v0.4.0 Phase 2 批次 D 标注质量工作台] - 2026-07-19

验收范围：

- 统一质量报告与数据健康工作台
- 空标注、几何、重复对象、split 泄漏、类别分布与审查状态检查
- 问题筛选、样本/对象定位和复检
- occluded、truncated、difficult 对象属性与格式导出
- 原始数据只读边界

步骤：

1. 准备至少三张图片：一张空标注、一张含两个完全相同的同类 rectangle、一张正常 polygon；把内容相同的两张图片分别设为 train 和 val。
2. 在数据集详情页点击“数据健康”，确认显示错误/警告/提示汇总，并出现 `EMPTY_ANNOTATIONS`、`DUPLICATE_ANNOTATION`、`SPLIT_LEAKAGE`、极少类别或单 split 类别提示。
3. 按严重度和问题类型筛选，点击“重新检查”，确认汇总和列表保持一致；点击具体几何问题的“定位对象”，确认进入正确样本并聚焦对应对象。
4. 对包含 train/val 相同 hash 的全数据集执行训练格式预检，确认出现 `SPLIT_LEAKAGE` error 并阻断下载；改为仅导出 train 后确认不会因为范围外 val 样本而误报。
5. 点击“空标注图片”卡片，确认工作台关闭，主列表自动切换为“图片 + 空标注”，样本数量与报告一致；从网格进入标注页后，上一张/下一张仍限制在当前空标注范围。
6. 尝试保存超出图片宽高的 rectangle 和零面积 polygon，确认 API 返回 400，刷新后原有标注仍存在。
7. 在对象面板勾选遮挡、截断、困难并保存，刷新确认状态保留；导出 LabelMe、COCO 和 Pascal VOC，确认对应 flags、attributes 或 XML 节点为真值。
8. 运行 `cd backend && .\.venv\Scripts\python.exe scripts\smoke_quality_api.py --root "D:\My Datasets\test"`，确认扫描 5,949 文件、0 错误，报告与空标注筛选计数一致且 `sha256_unchanged=true`。

通过标准：

- 质量报告问题计数不受返回明细截断影响，明细按 error、warning、info 优先展示。
- split 泄漏只比较 train/val/test 中相同文件 hash；重复对象只提示，不自动合并或删除。
- 保存校验发生在替换旧标注之前，非法请求不会导致数据丢失。
- 质量工作台可在桌面宽度内稳定滚动，不挤压详情页；空标注与审查筛选会重置不相关条件。
- 质量检查、属性编辑和导出只修改 SQLite 元数据或响应内容，不移动、重命名、删除、覆盖原始文件，也不在原始目录生成旁车文件。

## [v0.4.0 Phase 2 批次 C 训练格式导出] - 2026-07-19

验收范围：

- COCO detection/segmentation 真实 JSON 导出
- YOLO detection/segmentation 标签 ZIP 导出
- Pascal VOC XML ZIP 导出
- 前端预检与下载向导
- 原始数据只读边界

步骤：

1. 准备包含 rectangle、polygon、point 和空标注图片的数据集，在详情页打开“更多操作 → 标注训练格式”。
2. 依次选择六种格式，切换当前筛选、全数据集、指定 split 和已选样本范围，确认预检样本数随范围变化。
3. 对 detection/VOC 预检确认 polygon 转 bbox 显示 warning；对 segmentation 预检确认 rectangle 转四点 polygon 显示 warning；point/points 显示跳过提示。
4. 构造越界坐标或只有不兼容对象的范围，确认预检显示 error 且下载按钮禁用；修正后重新预检才能下载。
5. 下载 COCO detection/segmentation，确认 category/image/annotation id 稳定，bbox、segmentation、area、iscrowd 字段符合契约。
6. 下载 YOLO detection/segmentation，确认 ZIP 含 `labels/<split>/*.txt`、`classes.txt`、`data.yaml` 和 `export_report.json`，坐标均按图片宽高归一化。
7. 下载 Pascal VOC，确认 ZIP 含 `annotations/*.xml` 和 `export_report.json`，文件名、尺寸、类别与 bbox 正确。
8. 运行 `cd backend && .\.venv\Scripts\python.exe scripts\smoke_annotation_export_api.py --root "D:\My Datasets\test"`，确认六种格式均为 200 且 `sha256_unchanged=true`。

通过标准：

- 导出只生成响应内容，不把图片复制进导出包，也不在原始数据目录写入旁车文件。
- 预检 error 阻断导出，warning 可在用户明确查看后继续，类别映射稳定且只包含可导出对象类别。
- 当前筛选、全量、split 和已选样本范围与后端实际导出内容一致。
- 真实数据烟测使用内存 SQLite，仅读取扫描目录；选中原图的 SHA-256、大小和修改时间保持不变。

## [v0.4.0 Phase 2 批次 C2 labelme 导入导出] - 2026-05-19

验收范围：

- labelme JSON 单文件导入
- labelme JSON 目录导入
- dry-run、replace、append 策略
- labelme 单样本导出和数据集 ZIP 导出

步骤：

1. 准备一个包含正常图片样本的数据集，并准备一个 labelme JSON，包含 rectangle、polygon、point、points 四类 shape。
2. 调用 `POST /api/datasets/{dataset_id}/annotations/import-labelme`，传入 `mode=file`、`sample_id` 和 `dry_run=true`，确认响应中 `created_annotations` 数量正确，但刷新样本标注仍为空或保持原状。
3. 用同一 JSON 再调用一次，设置 `dry_run=false`、`strategy=replace`，确认样本标注被写入 SQLite，四类 shape 坐标能在标注 API 中恢复；默认不修改样本标签，只有明确传入 `sync_sample_tags=true` 才追加类别标签。
4. 调用 `POST /api/samples/{sample_id}/annotations/export-labelme`，确认导出的 `shapes` 中 rectangle、polygon、point、points 坐标均为 labelme 二维点数组，`imageWidth/imageHeight` 与图片尺寸一致。
5. 准备一个 labelme JSON 目录，至少包含一个 `imagePath` 为相对路径的文件，以及一个包含不支持 shape_type 的文件；调用目录导入，确认匹配到对应样本，不支持 shape 被 warning 跳过。
6. 对已有标注的样本用 `strategy=append` 导入一个新 shape，确认旧对象仍存在，新对象追加到对象列表末尾。
7. 调用 `GET /api/datasets/{dataset_id}/annotation-export?format=labelme`，确认下载 ZIP 中包含 `annotations/*.json` 和 `export_report.json`，默认跳过空标注图片。

通过标准：

- labelme 导入只写 SQLite 标注元数据；仅在显式开启同步时追加样本标签关系，不移动、重命名、删除、覆盖原始图片，也不修改导入 JSON 文件。
- dry-run 不产生任何数据库写入。
- replace 会用导入 JSON 替换目标样本现有 annotation；append 会保留旧对象并追加新对象。
- rectangle 会规范为 `[xtl, ytl, xbr, ybr]`，导出 labelme 时再转回两个二维点；polygon、point、points 坐标保持原图像素坐标语义。
- 目录导入能按 `imagePath` / 相对路径 / 文件名匹配样本；无法匹配或匹配歧义时返回 error。
- ZIP 导出默认只包含有标注图片，报告中统计导出文件数和跳过的空标注样本数。

## [v0.4.0 Phase 2 批次 C1 导出预检框架] - 2026-05-19

验收范围：

- 标注导出预检 API
- YOLO detection 与 YOLO segmentation 的格式兼容提示
- 越界坐标和无可导出对象阻断

步骤：

1. 准备一个包含正常图片样本的数据集，至少创建 rectangle、polygon 和 point 三类标注对象。
2. 调用 `POST /api/datasets/{dataset_id}/annotation-export-precheck`，请求体为 `{"format":"yolo_detection"}`。
3. 确认响应中 `blocked=false`，rectangle 和 polygon 计入 `exportable_object_count`，point 计入 `skipped_object_count`。
4. 确认 issues 中包含 `POLYGON_TO_BBOX` warning 和 `INCOMPATIBLE_SHAPE_SKIPPED` warning，说明 polygon 会有损转 bbox，point 不导出。
5. 调用同一接口，请求体为 `{"format":"yolo_segmentation"}`，确认 rectangle 和 polygon 可导出，issues 中包含 `RECTANGLE_TO_POLYGON` warning。
6. 准备一个只有 point 标注的样本，调用 `yolo_detection` 预检，确认 `blocked=true` 且包含 `NO_EXPORTABLE_OBJECTS`。
7. 准备一个坐标超出图片宽高的 rectangle 标注，调用任一导出预检，确认 `blocked=true` 且包含 `COORDINATES_OUT_OF_BOUNDS`。
8. 在大数据集上运行预检时，如果 `info_count` 很大且 `truncated_issue_count > 0`，确认 `issues` 明细仍优先展示 error/warning，而不是只展示 `EMPTY_SAMPLE_SKIPPED`。

通过标准：

- 预检只读取样本、图片尺寸和 SQLite 标注元数据，不移动、重命名、删除或覆盖原始图片。
- detection 格式下 polygon 会被识别为 bbox 有损转换，而不是静默当作 polygon 导出。
- segmentation 格式下 polygon 可直接导出，rectangle 转四点 polygon 会提示 warning。
- 无可导出对象、越界坐标、图片尺寸不可读等问题会阻断后续真实导出。
- 响应中的 class map 稳定按类别名排序，为后续 COCO/YOLO/VOC 真实导出复用。
- issues 明细最多返回有限条数时，关键 error/warning 不会被大量空样本 info 淹没。

## [v0.4.0 Phase 2 标注拖拽稳定性修复] - 2026-05-19

验收范围：

- 已标注对象整体拖拽
- 已标注对象顶点拖拽
- 画布高频 pointer move 下的交互稳定性

步骤：

1. 打开一张已有矩形或 polygon 标注对象的正常图片样本，切换到选择工具。
2. 按住对象填充区域连续拖动较长距离，确认对象跟随鼠标移动，不出现浏览器禁止拖放光标，也不会拖动一小段后停止响应。
3. 按住对象顶点控制点拖动，确认顶点可持续跟随鼠标，松开后对象形状稳定保留。
4. 连续快速拖动对象多次，再点击保存并刷新页面，确认最终坐标按最后一次拖动结果恢复。

通过标准：

- 对象和顶点拖拽不会触发浏览器原生拖放。
- 拖拽预览保持流畅，拖动过程中不会因为父组件高频重渲染而中断。
- 松开鼠标后只提交一次最终拖拽结果，保存后的 annotation 元数据正确。

## [v0.4.0 Phase 2 Copilot 审核修复] - 2026-05-19

验收范围：

- 标注页快捷键监听和画布高频 pointer move 性能
- 对象列表聚焦命令的失效清理
- 数据集标注统计聚合结果

步骤：

1. 打开一个包含多张正常图片的标注页，移动鼠标经过画布和 polygon 草稿状态，确认底部坐标状态正常更新，页面没有明显卡顿。
2. 使用 `Ctrl+S`、`Ctrl+Z`、`Ctrl+Y`、`V/R/P/H` 和 `[` / `]`，确认保存、撤销/重做、工具切换和上下张导航仍按原规则工作。
3. 在包含多个对象的样本中点击右侧对象列表，确认画布仍能高亮并聚焦目标对象；删除对象后再次操作其他对象，确认不会出现重复聚焦或卡住的状态。
4. 保存包含多个标注对象的样本后返回数据集详情页，确认标注样本数、标注对象数和按标注类别统计仍与保存结果一致。

通过标准：

- 快捷键监听不会随画布状态刷新反复注册，交互行为保持不变。
- polygon 空移动不会制造无意义的草稿状态更新。
- 失效的对象聚焦命令会被清理，不影响后续对象定位。
- 标注统计字段保持正确，且统计过程不依赖加载全部 annotation 行到内存。

## [v0.4.0 Phase 2 批次 B 验收修复] - 2026-05-18

验收范围：

- 标注页类别下拉完整显示数据集标签
- 标注画布底图加载稳定性

步骤：

1. 在数据集详情页进入标签管理，确认数据集内存在多个标签，例如 `object`、`666`、`777`。
2. 打开任意正常图片样本的标注页，点击左侧“当前类别”下拉，确认所有数据集标签都可选，不会因为当前值是 `666` 而只显示 `666`。
3. 选择不同类别并新建矩形、polygon 或点对象，确认新对象类别跟随当前类别。
4. 选中已有对象，在右侧对象类别下拉中切换为另一个标签，保存并刷新，确认类别保留。
5. 连续打开或切换多张图片，确认底图和标注层一起显示；图片加载中不应只显示缩小标注框。
6. 如果图片请求失败，确认画布显示加载失败提示和“重试加载”按钮，而不是展示无底图的标注层。

通过标准：

- 标注类别来源与数据集标签字典一致。
- 类别选择不再依赖浏览器 datalist 的模糊过滤行为。
- 底图未加载完成或加载失败时不会单独显示标注 overlay。
- 修复只影响前端显示与标注元数据编辑，不移动、重命名、删除或覆盖原始图片文件。

## [v0.4.0 Phase 2 批次 B] - 2026-05-18

验收范围：

- 统计卡点击后的全局筛选作用域
- 重复样本 hash 分组弹窗
- 重复样本列表相邻排序
- 审查状态筛选入口
- 标注统计摘要

步骤：

1. 打开包含图片、视频或 CSV，且存在搜索、类型、状态、标签或 split 筛选条件的数据集详情页。
2. 先设置一个局部筛选，例如只看视频或某个标签；再点击顶部“图片”“视频”“样本”等统计卡，确认样本列表进入对应全局结果，旧的搜索、状态、标签、split 和审查筛选被清空。
3. 准备至少两组重复文件，点击“重复”统计卡，确认弹窗按 hash 分组展示，并以左右对照方式显示同组样本路径。
4. 在重复弹窗点击“查看重复样本”，确认样本列表只显示重复样本，且同一 hash 的样本相邻出现；卡片上的真实文件状态仍显示为正常、缺失或无权限等原始状态，而不是被改写成重复。
5. 在筛选栏选择“未标注 / 待审核 / 已通过 / 已拒绝”，确认列表按 `review_status` 过滤，并可与其他手动筛选条件组合。
6. 保存一张图片的多个标注对象后返回详情页，确认“标注”统计卡显示已标注样本数和对象数，审查状态摘要更新，标注类别摘要显示对象类别分布。
7. 点击审查状态摘要或标注类别摘要中的条目，确认会进入对应全局筛选结果，不保留先前不相关筛选。

通过标准：

- 全局统计数字对应的点击结果不会被旧筛选条件污染。
- 重复样本弹窗能以 hash 组为单位帮助人工对照。
- “重复”仍是虚拟查询条件，不改变数据库中的 `file_status`。
- 审查状态作为筛选能力暴露在主筛选栏或摘要区，而不占用批量工具栏。
- 标注统计字段和详情页摘要能反映已保存的 annotation 元数据。

## [v0.4.0 Phase 2 批次 A] - 2026-05-18

验收范围：

- 标注页上一张/下一张图片导航
- 大数据集标注切换性能
- 标注入口保留筛选上下文
- 未保存修改三选一确认
- 切换自动保存
- 上一张/下一张快捷键
- 绘制草稿提交/取消互斥确认
- 对象列表点击聚焦
- 标注对象标签同步样本标签
- 切图加载状态不造成标注区抖动
- 当前类别输入显示
- 快捷键帮助入口

步骤：

1. 打开包含多张正常图片、至少一个非图片或异常文件的数据集，筛选为图片、正常状态，并可叠加搜索、标签或 split 条件。
2. 从样本网格点击某张图片的标注入口，确认 URL 中保留筛选与排序上下文，标注页顶部显示当前图片在筛选结果中的序号。
3. 点击“下一张/上一张”，确认只在当前筛选后的正常图片中切换，不跳到视频、CSV、缺失或无权限样本。
4. 新增或编辑一个标注对象但不保存，点击下一张或返回，确认出现“保存并继续 / 放弃修改 / 取消”三种选择。
5. 选择“取消”后确认仍停留在当前样本；选择“放弃修改”后确认可以切换且原样本未写入新标注；选择“保存并继续”后确认保存成功再切换。
6. 开始绘制 polygon 但不完成，尝试切换工具、保存、返回或切换样本，确认先出现草稿处理弹窗，可提交草稿、取消草稿或返回继续绘制；点数不足时提交按钮不可用。
7. 在已有多个对象的样本中点击右侧对象列表不同对象，确认对象被高亮，画布视图居中聚焦到对应对象附近。
8. 打开“切换自动保存”，新增或编辑对象后点击下一张，确认不再弹出 dirty 三选一，而是先保存成功再切换；关闭后恢复三选一确认。
9. 使用 `[` 和 `]` 确认可以切换上一张/下一张，并且草稿与 dirty 状态仍按上述规则处理。
10. 保存带有类别的标注后返回数据集详情页，确认标签统计、标签筛选和样本卡片标签能看到刚保存的对象类别。
11. 连续点击上一张/下一张，确认不会在顶栏下方插入“正在加载当前图片和标注”的横条，中央画布区域不会因加载提示上下跳动。
12. 开始拖拽矩形但不松开鼠标时按 Esc，确认矩形草稿被取消；松开鼠标已生成的矩形应使用撤销或删除处理。
13. 在左侧当前类别输入中输入或选择 `object`，确认完整可见；较长类别可通过悬停查看完整 title。
14. 打开右上角快捷键帮助，确认 V/R/P/H、`[`/`]`、Ctrl+S、Delete、Esc 等提示可见；底部状态栏不再显示长串快捷键文本。

通过标准：

- 连续标注导航遵守当前筛选上下文，并只处理正常图片样本。
- 大数据集切换不再因前端全量分页拉取当前筛选结果而等待数秒。
- dirty 状态切换样本/返回不会无提示丢失标注。
- 自动保存只在保存成功后切换样本，保存失败时保留在当前样本。
- 绘制中草稿不会在未选择提交或取消时污染保存和历史状态。
- 切图加载状态不改变标注区布局高度。
- 当前类别输入至少能完整显示常见短标签，后续标签管理布局按 TODO 继续优化。
- 标注对象类别能同步成为样本标签，外部标签统计和筛选随保存结果更新。
- 对象定位、保存、撤销/重做和原始文件安全策略保持可用。

## [v0.4.0 P0 布局修复] - 2026-05-18

验收范围：

- 样本网格卡片底部信息布局
- 图片标注入口位置
- 标注页对象列表独立滚动
- 标注画布稳定视口

步骤：

1. 打开一个包含正常图片样本的数据集，选择每页 60 或 120 项，并使用较窄浏览器宽度查看样本网格。
2. 确认图片样本卡片的信息区中文件名、扩展名、文件大小和标注按钮互不重叠。
3. 点击卡片缩略图或文件名区域，确认仍能打开样本详情；点击标注按钮，确认进入标注工作区。
4. 打开一个已有 10 个以上标注对象的图片样本，或连续创建多个点/框对象。
5. 确认右侧“标注对象”列表出现自己的滚动条，页面整体滚动条不会随对象数量异常变长。
6. 滚动右侧对象列表时，确认中央画布、左侧工具栏、顶部保存区和底部状态栏保持稳定，不随列表滚动或对象数量变化缩放。

通过标准：

- 样本卡片在长文件名和窄列宽下没有文字/按钮遮挡。
- 标注对象数量增加只影响右侧列表内部滚动。
- 画布尺寸和当前缩放状态不会因为对象列表高度变化而异常放大。
- 修复只调整前端布局，不移动、重命名、删除或覆盖原始数据文件。

## [v0.4.0 标注 Phase 1] - 2026-05-15

验收范围：

- 图片标注工作区入口
- 矩形、多边形和点标注
- 对象列表、类别编辑、删除、撤销/重做
- 标注保存、刷新恢复和 manifest 导出
- 原始文件安全

步骤：

1. 打开一个包含正常图片样本的数据集，确认图片卡片右下角有标注按钮。
2. 点击标注按钮进入 `/datasets/{id}/annotate?sample={sampleId}`，确认图片能在中央画布显示。
3. 在左侧工具栏选择矩形，拖拽画一个框；选择多边形，连续点 3 个以上点后点击“完成多边形”；选择点，在图片上点选一次。
4. 在右侧对象列表中切换对象，修改类别和备注，尝试隐藏/显示、锁定/解锁和删除对象。
5. 使用撤销/重做按钮确认对象变化能回退和恢复。
6. 点击保存，刷新浏览器页面，确认标注对象仍然存在。
7. 返回数据集详情页，导出 manifest，确认对应样本包含 `annotations`，且每个对象有 `label`、`shape_type`、`points`。
8. 对 CSV/视频样本确认不会出现可用的几何标注工作区。

通过标准：

- 标注页能完成矩形、多边形、点三类对象的创建和编辑。
- 保存后刷新仍能从数据库恢复标注。
- Manifest 包含标注对象，坐标为原始图片坐标。
- 所有标注操作只写 SQLite 元数据，不移动、重命名、删除或覆盖原始图片。

## [v0.3.0 分层划分修复] - 2026-05-03

验收范围：

- 小样本多标签数据集的按标签分层划分
- 验证集标签覆盖下限
- 随机种子和 train/val 数量稳定性
- 极端比例下的容量不足提示
- 标签数量统计

步骤：

1. 打开数据集 `1`，进入“数据集划分”。
2. 设置 `train=70`、`val=30`，不勾选“划分 test”，勾选“按标签分层”和“只处理正常文件”，随机种子填 `42`。
3. 点击应用划分，确认结果为 `处理：15 / 15`、`train：11`、`val：4`、`test：0`。
4. 确认不再出现 `ok: 3 eligible samples, but none landed in validation split.`。
5. 在样本筛选中分别查看 `ok`、`father`、`ng` 标签，确认每个标签至少有一个样本位于 `val`。
6. 再设置 `train=90`、`val=10`，不划分 test，确认结果提示验证集名额不足，而不是随机落点失败。
7. 勾选“划分 test”，设置 `train=80`、`val=10`、`test=10`，确认测试集名额不足时会提示至少需要的名额数量。
8. 点击顶部“标签”统计卡，确认能看到每个标签的样本命中数、未标注样本数，并可点击单个标签筛选样本。

通过标准：

- 比例计数保持稳定，且可覆盖标签不会在验证集中完全缺失。
- 多标签样本可作为多个标签的验证集覆盖保底。
- 极端比例导致不可覆盖时，提示说明 split 名额不足。
- 标签统计清楚说明多标签会重复计数。
- 操作只更新样本 split 元数据，不移动、重命名、删除或覆盖原始文件。

## [v0.3.0 收尾优化] - 2026-05-02

验收范围：

- 扫描按钮与扫描目录绑定
- 体检内修复缺失
- 缺失/重复统计卡详情
- 重复样本筛选
- 样本/图片/视频统计卡筛选
- 样本网格选择本页
- 打开数据集自动扫描设置

步骤：

1. 打开任一数据集，确认顶部扫描目录旁有蓝色“扫描”按钮，筛选栏里不再出现扫描按钮。
2. 点击蓝色“扫描”，确认执行当前数据集扫描目录的增量扫描，扫描报告数量正常更新。
3. 打开“设置”，勾选“打开数据集时自动扫描”并保存；重新进入该数据集，确认会自动执行一次扫描。
4. 制造缺失文件后重新扫描，确认“缺失”统计卡变色；点击统计卡后只显示缺失/无权限信息和“查看/修复”操作。
5. 点击“缺失”详情里的“修复”，确认打开缺失修复弹窗；样本标题区外部不再有独立“修复缺失”按钮。
6. 准备两个内容完全相同的文件并扫描，确认“重复”统计卡变色；点击统计卡后只显示重复摘要和“查看重复”。
7. 在“全部状态”下拉中选择“重复”，确认列表只显示重复 hash 涉及的样本。
8. 没有重复样本时点击“重复”统计卡，确认不显示“查看重复样本”按钮。
9. 点击“样本/图片/视频”统计卡，确认分别切到全部类型、图片、视频筛选。
10. 打开“体检”，确认“数据划分”块使用蓝色系，且仍可进入划分工具；文件状态块中仍能进入缺失修复。
11. 在样本区域点击“选择本页/取消本页”，确认只影响当前页样本，批量栏数量同步变化。
12. 对多标签样本执行按标签分层划分，确认某标签实际有多个可处理样本时，不再错误提示该标签 only 1 sample。

通过标准：

- 扫描、体检、统计卡、筛选栏没有重复入口或布局重叠。
- 自动扫描只读取文件并更新元数据，不移动、重命名、删除或覆盖原始文件。
- “重复”是查询筛选条件，不改变样本真实 `file_status`。
- “选择本页”不跨页隐式选择，避免误批量编辑不可见样本。

## 核心回归清单

每轮发布前至少抽查：

1. 数据集：创建、编辑、删除元数据，根目录扫描保护有效。
2. 扫描：新增、变更、缺失、无权限和不支持文件能生成报告。
3. 样本：分页、排序、搜索、文件类型/状态/split/标签筛选稳定。
4. 标签：创建、编辑、删除、颜色、层级、别名和冲突检测可用。
5. 批量：批量标签、批量 split、单选/多选删除样本记录可用，且不删除本地文件。
6. 预览：图片、视频、CSV 详情预览可用；缺失文件有明确提示。
7. 导入：CSV/JSON 可按 `relative_path`、`absolute_path`、`filename`、`file_hash` 匹配并写入标签、split、备注和自定义元数据。
8. 导出：manifest、CSV 标签表、COCO/YOLO 骨架先预览再下载，图片样本 manifest 可包含 annotations。
9. 划分：随机/分层 train/val/test 支持 test 开关、随机种子、只处理正常文件和只处理未划分样本。
10. 安全：所有扫描、导入、修复、删除记录操作都不修改、不移动、不覆盖原始数据文件。
